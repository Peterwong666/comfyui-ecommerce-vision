# 前端环境变量示例。复制成 .env.local 后按需修改（.env.local 已被 .gitignore 排除）。
#
# ⚠️ 只有 VITE_ 前缀的变量会暴露给浏览器。**不要把密钥放进来** ——
# 它们会被打进产物，任何访客都能看到（NFR-4）。
#
# 后端地址。默认 http://127.0.0.1:8000（见 docs/sop/runbook.md）。
# 本机 8000 已被另一个项目占用时，改用 8010 并同步调整后端启动端口：
#   VITE_API_BASE_URL=http://127.0.0.1:8010
VITE_API_BASE_URL=http://127.0.0.1:8000
