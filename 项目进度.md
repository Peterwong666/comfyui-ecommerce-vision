# 项目进度跟踪（项目进度.md）

> 配套文档：`todolist.md`（任务清单）· `docs/`（产品与工程文档）
> 更新频率：**每天收工更新「今日记录」；每周五更新「周记」与下周计划**
> 当前版本：V1 规划期 · 起始日 2026-09-14 · 目标发布 2026-11-22

---

## 0. 一屏速览

| 项 | 状态 |
|---|---|
| **当前阶段** | 🔵 **P2/P8/P11 已收口 + P8-09 5/5 可复现性补齐**；真实 PG/Redis/MinIO/CI 仍未验证；P8 画质评审待人工 |
| **本周目标（W1 09-14~09-20）** | 仓库与规范 ✅；SDXL 出图 ✅；引擎升级到支持 klein ✅；后端骨架 ✅；工作流内核 ✅；三条新工作流 L4 + per-workflow baseline + verification 升到 `gpu_verified` ✅（`status` 仍 disabled 待 P8 画质评审）；前端骨架 + 动态表单引擎 ✅；P6-10 产物消费链路 + 生命周期清理 ✅；P7-05 画廊与素材库 ✅；P2-11 CI 流水线 ✅（⬜ 但从未运行）；P2-07 环境复现脚本 ✅；P2-09 docker-compose ✅；P6-08 完成 ✅；P6-13 内容安全输入侧 ✅；**P8 质量体系：P8-08a 离线门禁接入 CI/pre-push ✅ / P8-09 可复现性 5/5 ✅ / golden set 真实出图 42/42 ✅（评分待人工）**；P11-02 部署文档 ✅；ADR-008 三条范围裁定 + 跨目录同源口径清理 ✅；蒙版极性人工判读结案 ✅ |
| **总体进度** | ▓▓▓▓░░░░░░ **43%**（P0 8/8；P1 18/20；**P2 12/13**；P3 8/12；P5 4/11；**P6 8/13**；**P7 3/12**；**P8 5/10**；P11 1/13；其余 0）<br>⚠️ **分母仍是 153，未变** |
| **已完成任务** | **66 / 153**（按 §2 各阶段任务表各行求和：8+18+12+8+0+4+8+3+5+0+0+1+0＝**66**） |
| **当前阻塞** | 🟡 **GPU 已关机（2026-09-19 约 11:25，机时约 10 分钟）**。P8-09 5/5 可复现性已补齐。`inpaint_v1` 核心语义已验证（带 alpha PNG 走 `/upload/image` 出图，输入/输出 162038 像素不同）。`status` 仍保持 disabled：技术证据已足，画质/场景适配待 P8 golden set 人工评分。⚠️ 真实 PG / Redis / MinIO / CI 仍未验证 |
| **未决项** | P1-08 用户访谈 · P1-17 高保真原型 · 初始迁移未在真实 PG apply · 条件 UPDATE 原子性只在 SQLite 验证 · 6 个模型 LICENSE 原文 0/6 · P3-07 缺权重 · 真实 MinIO/PG/Redis / `maintenance` 队列监听 / 画廊浏览器走查 / CI 从未运行 均未验证 · P6-13 NSFW 视觉检测 V1 未实现 · **P8-01/P8-08b 真实出图已跑，但评分与 expected 回填待人工** · V1 三条新工作流 `status` 仍 disabled ·（详见 §0.2）|
| **最大风险** | **R4 模型/节点商用许可（已查出真红线）** · **R2 环境不可复现（P2-07/P2-09 已补，但真实重建未跑）** · **R15 已降级**：GPU 侧凭据用完即毁，下次仍需重新就位 · **R16 内容安全只有输入侧文本过滤、无视觉检测** · 权重缺口卡住 P3-07 与两段式放大 · 用户痛点未验证 |
| **下一步 3 个动作** | ① **P8 画质评审**：对 42 例 golden set 产物人工打分，一次通过率 ≥80% 后再把三条工作流 `status` 改为 `enabled`。② **基础设施验证**：真实 PG 上验条件 UPDATE 与两道闸门的并发行为 + 真实 MinIO/Redis + CI 首次运行 + 画廊浏览器走查。③ **P4 控制体系**：ControlNet / IP-Adapter / LoRA 控制能力矩阵与推荐权重区间。 |
| **详细纪实** | [`项目进展.md`](./项目进展.md)（台账 **56 条**）· [`debug_log.md`](./docs/sop/debug_log.md)（G1~G9 + 各工作流记录） |

### 0.2 新会话交接快照（2026-09-19 · P2/P8/P11 收口 + P8-09 5/5 可复现性补齐，revision `f3824a0`）

> **给下一个会话的最小上下文**。细节都在 `项目进展.md` 与 `docs/sop/debug_log.md`。
> 2026-09-19 新增：① **P2-07 环境复现脚本** `scripts/reproduce_env.sh`（CPU 侧一键重建）② **P2-09 docker-compose.yml**（PG + Redis + MinIO）+ **P11-02 部署文档** `docs/deploy.md` ③ **P8-08a 离线门禁接入 CI**：`.github/workflows/ci.yml` 新增 `quality-gates` job + `scripts/pre-push.sh` 本地可选钩子 ④ **P8-09 可复现性补齐至 5/5**：`i2i_v1` / `inpaint_v1` / `upscale_v1` 由 `40_repro_workflow.py` 验证「同 seed 两次出图 IDAT 一致」，证据在 `deploy/comfyui/evidence/repro_20260919/`。
>
> 历史快照（2026-09-17）：① **三条新工作流（i2i_v1 / inpaint_v1 / upscale_v1）的「渲染路径 L4」首次跑通** —— 各 **通过 11 · 失败 0 · 跳过 0** ② **蒙版极性人工判读结案：没有反** ③ **registry 回填**：三条 `render_path_l4: false → true` ④ 后续 09/18 baseline 已补、verification 已升 `gpu_verified`。
> 🔴 **最重要的口径（最容易被下一个会话误读）**：**渲染路径已验证 + baseline 已补 ≠ 工作流可放行**。三条新工作流登记牌仍是 `status: disabled`，因为 **P8 画质评审未做**（golden set 真实出图 42/42 已完成，但 `expected` 仍为 `null`、未人工评分）。**禁止**把它读成「三条工作流全部可用」。
> ⚠️ 另两句照旧：**P8-01/P8-08b 的评分未做**；**任何「通过率 / 得分 / 良品率 / 覆盖率」数字一律不存在**。
>
> **2026-09-18 新增**：6 项拍板已落地 —— `(a)` `verification` 新增 `render_path_l4_passed` `(b)` `inpaint_v1.reference_image` 声明 `requires_alpha: true` `(c)` 整型字段按 Schema `min/max` 校验 `(d)` `Settings` 强制 `task_max_retries ≤ hard_limit` `(e)` `37_probe_*.py:398` 错文案已修 `(f)` `engine/validate.py` tiered hint 已修。分析文档见 `docs/review/analysis_20260918.md`（**不入 Git**）。

**环境现状**
| 项 | 值 |
|---|---|
| 引擎 | ComfyUI **v0.36.0**（commit `ee71d5c4`）+ torch **2.13.0+cu130** |
| 生产启动参数 | **`--highvram`**（必须！去掉会让 SDXL 慢 1.58x，见台账 #17/#18）。⚠️ 本轮 L4 的生效开关已留痕：`argv` 含 `--highvram`、日志有 `Set vram state to: HIGH_VRAM`、且**没有** dynamic VRAM 那行（证据 `deploy/comfyui/evidence/36_comfyui_effective_switches.txt`）|
| 实例 | **已关机**（2026-09-19 约 11:25 由本轮关机；SSH 端口已不可连）。SSH `connect.westc.seetacloud.com:22910` |
| GPU 可用性 | ✅ **2026-09-19 实跑约 10 分钟后已关机**：用户开机并提供 SSH 密码，本轮回只跑了 P8-09 剩余 3 条可复现性验证（i2i/inpaint/upscale），未跑 baseline。⚠️ **凭据由用户本次提供**；下次跑 GPU 仍需重新就位。 |
| 节点 | 32 个自定义节点；**2534 个节点类**（卸载 sageattention 后从 2530 回升） |
| 前端 | **已从 0 到可运行**：React 18 + TS + Vite 5 + antd 5 + pnpm 12.4.2 |
| ⚠️ 本机端口 | **8000 已被另一个项目占用**（ToyVerse Cloud）→ 本项目本地用 **8010** |

**测试与校验的权威命令**（数字为 **2026-09-17 · registry 回填后** 在 revision **`9c2e0dc`** 上的实测；本批只改文档与 `registry.yaml` 的验证字段，**不改节点图**，故数字应与上一批一致）
```bash
cd backend && .venv/bin/python -m pytest -q        # 557 passed
timeout 120 .venv/bin/python -m pytest engine/tests -q         # 169 passed / 1 skipped（**仓库根与 backend/ 两个 CWD 数字必须一致**）
.venv/bin/python -m engine                         # 5 条工作流 L1/L2/L3 → VALIDATE=PASS
.venv/bin/python -m engine.tools.check_defect_kb   # 缺陷库机械校验（44 条解析；只证明参数合法）
.venv/bin/python -m engine.tools.check_golden_set  # golden set 可执行性（42 条；只证明能渲染过 L1/L3）
.venv/bin/python deploy/autodl/26_validate_workflow.py workflows/<id>.json \
    --object-info deploy/schemas/object_info.v0.36.0.json   # 对模板原文件的 L3
.venv/bin/ruff check .                             # 必须在**仓库根**跑 → All checks passed
cd web && corepack pnpm typecheck && corepack pnpm lint && corepack pnpm exec vitest run   # 83 passed（7 个文件）
cd web && corepack pnpm build && corepack pnpm lint  # build exit 0；lint 0 error / 1 warning（既有，router.tsx:17）
# 中间件一键起（需 docker）
docker compose up -d
```
**GPU 侧（本轮新增，需凭据 + 有卡；⚠️ 本次已跑、凭据已销毁）**
```bash
# 三条新工作流的渲染路径 L4（一次跑完，含汇总表；--launch-args 必须用「等号」形式）
bash deploy/autodl/36_l4_three_workflows.sh
# 稳态基准（per-workflow baseline）
python deploy/autodl/38_baseline_workflow.py <workflow_id> --asset 1=/path/to/ref.png
# golden set 真实出图跑批
python deploy/autodl/39_golden_set_run.py --asset-map /path/to/asset_map.json --outdir ... --results ...
# 单工作流同 seed 两次出图可复现性验证
python deploy/autodl/40_repro_workflow.py <workflow_id> --asset-map /path/to/asset_map.json --case-id gs-<workflow>-001
# 蒙版极性专项
python deploy/autodl/37_probe_inpaint_mask_polarity.py ...
```
⚠️ **后端 557 / ruff 全绿是本轮实测**；引擎 169 / 1 skipped 与离线门禁均实测通过。前端 83 passed 为最近一次全量记录，本轮未复跑。
⚠️ **前端 83 不是本轮实测**：本轮只改了 `web/src/features/workflow-form/validate.ts` 的**注释**（上一批），该批**记录**为「复跑单文件 25 条通过」；全量 **83 passed（7 files）** 是**最近一次全量 vitest 的实测值**，**本轮未复跑全量**（也未复跑 typecheck / lint / build）。
⚠️ **前端 flaky 的诚实附注（仍然有效）**：`fb6da15` 后连跑两次全量 —— 06:50 那次 **82 passed / 7 files（全绿）**；06:58 复跑出现 **1 failed（81 passed）**：`WorkflowForm.test.tsx > 分组与折叠 > advanced 字段收进「高级设置」，默认不可见` 报 **`Test timed out in 5000ms`**；**单独重跑该文件 → 11 passed（1 file）**，且该文件与改动无关 ⇒ 这是已知的**本机 CPU 争用抖动**（本机 4 核常被并行负载占满，同一用例净耗时已到 8~14s），**不是回归**。
⚠️ 未做的加固：给这些重交互用例**显式放宽超时**（属既有测试配置，本轮未动）。

**本轮新建的本地无 PG 起库流程**（此前根本跑不起来，见台账 #28）
```bash
cd backend
export DATABASE_URL='sqlite+pysqlite:///./dev.db'
.venv/bin/python -m app.cli.init_db          # 建表；非 SQLite DSN 会被拒绝（exit 2）
.venv/bin/python -m app.cli.seed_workflows   # registry.yaml → workflows 表（幂等）
.venv/bin/python -m uvicorn app.main:app --port 8010
```

**本轮挖出并已修的关键问题（都在台账里有完整「现象→分析→决策→处置→验证→沉淀」）**
| # | 一句话 |
|---|---|
| **#17/#18** | SDXL 升级后 1.58x 回归 → 元凶是**新版默认开启的 dynamic VRAM** → 固定 `--highvram` 修复 |
| **#21** | `.gitignore` 未锚定的 `models/` 规则**误挡住 8 个源码文件**（从未入库）→ 根锚定 + 干净克隆验证 |
| **#22** | 三流并行：**接口冻结的价值** 与「重复实现」的必然性（A 的 injector 与 B 的 render 撞车） |
| **#23** | `smZNodes` 真因是 **sageattention ABI 失配**（非 diffusers）→ 卸载后**连带修好 nunchaku**，2530→2534 |
| **#24** | 🔴 **合规红线**：`ComfyUI-Upscaler-Tensorrt` 是 **CC BY-NC-SA 非商用**，且已装在环境里 |
| **#25** | 并发写同一文件：**显性层炸了、隐性层连测试都抓不到**（重复定义常量靠定义顺序侥幸正确） |
| **#26** | **G7 定案**：klein 上写 `(word:1.2)` **不被解析且污染提示词**，且换 `BNK_*` 节点也救不回来 |
| **#27** | 校验器把**运行时才确定的值**也拿去比静态快照 → **堵死了所有带参考图的工作流** → 加精确豁免 |
| **#28** | 🔴 **平台级空洞**：**全仓库没有任何代码把 `registry.yaml` 灌进 `workflows` 表** → `GET /workflows` 恒为空 |
| **#29** | `pkill -f "vite"` 又一次**自匹配杀掉自己的 shell**（旧坑复发，见纪律 6） |
| **#30** | 🔴 **平台级空洞**：`GET /assets/{id}/content` **从未实现**（docstring 却长期写着"取图应走受控的下载接口（P6-10）"）→ **画廊拿到 200 条素材记录也渲染不出任何画面**。教训：**注释里的「归属声明」不等于交付**（与 #28 同类事故） |
| **#31** | 🔴 **平台级空洞**：`is_adopted` **全仓库没有写入口**（良品率恒为 0），且契约 §4 冻结的 `image_adopted`（良品率分子）与 `image_downloaded`（北极星分子）**全仓库零处触发** → 两个核心 KPI **结构性无法产出数据**。教训：「定义了指标」与「指标能产出数据」之间隔着一条链路 |
| **#32** | ⚠️ **「静默 skip」形态的假通过**：引擎 4 条 L3 用例因 **CWD 相对路径**在 `backend/` 下静默跳过，整体仍全绿（`131 passed / 5 skipped` vs 仓库根 `135 passed / 1 skipped`）→ 改用 `engine/registry.py` 已有的 `REPO_ROOT`（**不新造第二份实现**），两个 CWD + `/tmp` 三处数字一致。教训：**skip 也是"没验证"，绿色里的 skip 数要当成信号** |
| **#33** | ⚠️ **「注释级承诺」不等于「机器可校验」**：`extract_models(rendered.workflow)` 的注释反复强调不能取 `workflow.definition`，但**变异测试证明没有任何测试能检出这个回归**（现有 5 条工作流都不用 `_meta.switches`，两种取法结果相同）→ 补一条带 bypass 分支的用例把承诺变成判据。教训：**凡"这里必须用 A 不能用 B"的注释，都要问一句"用错了会不会变红"**（与 G9 同源） |
| **#34** | 🔴 **配额扣减是 Python 层读-改-写 ⇒ 并发提交下额度被超发**（`user.quota_used += N`；`get_db()` 每请求一个 session、同步路由跑线程池 ⇒ 丢更新；全仓库**零条件 UPDATE**，这个模式根本不存在）→ 改为 `app/services/quota.py::charge()` 的**单条条件 UPDATE**（`WHERE quota_used + n <= quota_total` + `rowcount`；**不用 `FOR UPDATE`**，因 SQLite 不支持而本地全部单测跑 SQLite）；**显式回读** `quota_used`（Core 语句绕过 ORM + `expire_on_commit=False`，连 commit 都不刷新）；402 的 `detail` 改**对象形式**；补 `test_quota.py` **8 条**（此前**零覆盖**）。教训：**"读-改-写"在并发下就是丢更新**；**原子写之后还要管读**；**"只在单请求下正确"是一种测试不会红的假通过**。⚠️ **后续（2026-09-17 同轮收口）**：「按接口限流」经裁定 **V1 不做**（需求无 QPS / 频次 / 窗口条款；GPU 已全局串行化），真正有意义的那一半改由 **EX-5 队列闸门 + 每用户在途任务数上限**承担（见 #43 与 `todolist.md` P6-08）；原子性**仍只有 SQLite 证据**（真实 PG 未验证） |
| **#35** | 🔴 **空回执 worker 留下的产物是坏的**（`engine/tools/check_defect_kb.py`）—— `:514` 中文字符串里嵌了 ASCII 双引号导致字符串被提前截断，**模块根本不能导入**、`ruff` 也报 `F401`，但**读代码完全看不出来**（docstring 判据表、`__all__` 都很齐）。**归因已查实**：502 发生在「AI Agent 已创建、执行中途」，不是「没执行」⇒ 本仓库连续三次（P6-10 接口 / P7-05 画廊页 / 本轮）出现「回执丢了但活干了」。教训：**凡拿到"别人写的、从没跑过的"产物，第一步必须真跑一遍 + 真 lint 一遍** |
| **#36** | ⚠️ **「改了 A 没改 B」**：`1b04106` 的提交信息说「更正 `validate.py` **顶部**关于 L3 的过期描述」—— 它**修对了顶部 docstring，却漏了 argparse 的 `description`**，于是 `python -m engine --help` 仍在说谎（"不含 L3"），已由 `6e5308a` 补上。教训：修一处陈述要顺手 grep 同一陈述的**全部副本**（docstring / argparse / README / 契约），否则"修好了"本身是幻觉 |
| **#37** | ⚠️ **计数陷阱**：`negative.yaml` 的 `severity: hit` **YAML 解析是 20 条**，`grep -c` 得 **21** —— 多出来的是 `:17` **注释**里的那一条。教训：数结构化数据的条目要用**解析器**，不要用 grep 数文本行 |
| **#38** | 🔴 **「跳过」被计入「通过」**：`engine/tools/check_golden_set.py` 曾把「跳过 L3」记进 `levels_run`（**谎称已执行 L3**），且"跳过"不算不通过 ⇒ `--no-l3` 会 **exit 0**，即 CI 会把「少做一层」当全绿。已修 + 有测试钉住。教训：**「跳过」绝不能被计入「通过」，退出码要把"少做了一层"当失败** |
| **#39** | ⚠️ **`.gitignore` 空转规则的实际行为被纠正**：此前流传「放进 `input/`、`expected/` 的大图不会被忽略」是**不准确的**；实测 `tests/golden_set/reference/input/big.png` **也被忽略**（`*` 覆盖子目录），真正不被忽略的是 `reference/` **之外**的路径。教训：`.gitignore` 的判断要用 `git check-ignore --no-index -v` 对**真实文件**做，不要靠推理 |
| **#40** | ⭐ **机械断言抓到了「把黑名单印在错误提示里」**：内容安全（P6-13）的隐私红线断言（审计不得含命中原词）顺带发现 `reason` 文案里**抄了 5 处 `neg-027/030/031` 的原文**，而那几处恰好就是**词条本身** —— 等于把黑名单写进了给用户看的报错里。已改写并加**两条长期盯防断言**。教训：**"不记原词"的断言要顺带覆盖"报错文案里也不能出现原词"**，否则红线只守住了数据库这一侧 |
| **#41** | ⚠️ **又一次「夹具假象」**：内容安全的测试里读到「额度泄漏了 1」—— 实为**测试夹具是单连接**（内存 SQLite + `StaticPool`），路由线程里**未提交**的 UPDATE 对测试线程可见。用 `rollback()` + 另开 session 验证为 0，**再用一次变异确认 `rollback()` 不会掩盖真泄漏**。教训：**单连接夹具下"看得到未提交改动"是假象，不是泄漏**；这类结论必须先证伪夹具（与 #25/#32/#34 同族）|
| **#42** | ✅ **配额原子化的判据被变异坐实**：把实现**还原成原始的非原子读-改-写**后，`test_quota.py` 里**只有** `test_stale_cached_remaining_cannot_overdraft` 变红（`assert 202 == 402`）⇒ 它钉的**正是原 bug 形态**，而不是某种顺带的副作用。**意义**：一条"看起来对"的并发测试和一个真的能抓住漏洞的测试，差别只有靠变异才看得见 |
| **#43** | ⚠️ **变异逃逸：期望值从被测实现推导** —— `test_queue_guard.py` 起初把硬阈值写成 `_SOFT * api_tasks._QUEUE_HARD_LIMIT_FACTOR`，于是**把系数从 2 改成 1000 时，按公式推导的用例照旧全绿**（期望值跟着实现一起变）。改成**字面量 4** 后，同一个变异体让 **6 条变红**（含「恰好等于硬阈值放行」「预计等待公式」两条）。教训：**测试里的期望值必须钉「规格」，不能从被测实现里算出来** —— 否则断言恒真（与 G9「判据要能被检出」同源，但这次被检出的是**期望值的来源**）|
| **#44** | 🔴 **`AC-N5` 的路由遍历踩到 FastAPI 的 `_IncludedRouter`**：`app.include_router(...)` 现在被包成一个**没有 `path` / `methods`** 的容器节点，前缀挂在 `include_context` 上、内层路由的路径**不带前缀** ⇒ 直接遍历 `app.routes` **一条 API 路由都看不到**，参数化为空 ⇒ **全部用例消失 ⇒ 假绿**。改用 **`app.openapi()`**（同一份路由表生成、带正确前缀、随新路由自动增长），文档端点仍从 `app.routes` 取。**该坑由本文件自己的元测试负责当场抓出**（`test_enumeration_is_not_empty_and_sees_the_known_routes` 断言已知路由必须被枚举到）。教训：**"遍历式"用例必须配一条"枚举非空"的元测试**，否则它静默退化成"一条都不跑" |
| **#45** | 🔴 **测试基础设施也能制造假证据**：`alembic/env.py` 的 `fileConfig()` 默认 `disable_existing_loggers=True`，会把进程里已存在、又没写进 `alembic.ini` 的 logger（含 `app.api.v1.tasks`）**永久置为 disabled**；而 `caplog.at_level()` 只处理 `logging.disable()` 那一路、**不碰 `logger.disabled`** ⇒ 一旦 `test_migration` 先跑过，**此后任何 `caplog` 断言都静默拿到空列表**（`assert msgs == []` 假绿、"必须有告警"假红）。修法：`disable_existing_loggers=False` + 真的走一遍 `alembic upgrade` 的回归用例 + 一条用旧行为证明断言非空的**元测试**。教训：**污染源可能不在被测代码里，而在测试基础设施里** |
| **#46** | ⚠️ **`conftest.py` 的拆卸缺陷被 `templates` 零覆盖掩盖**：模型间存在外键环（`assets ↔ batches ↔ tasks ↔ templates`），`drop_all` 排不出合法删表顺序（只发一句 SAWarning），而 conftest 已开启 SQLite 外键 ⇒ "先删父表"会真的报 `IntegrityError`。**这个坑此前从未暴露，因为全仓库没有任何用例创建过 `templates` 行** —— 正是 `AC-F6` 的零覆盖（本轮补上后立刻炸出来，且错误记在 **teardown** 上，看起来像用例自己坏了）。修法：只在拆卸这条一次性内存库时关外键，运行期约束照常。教训：**零覆盖不只意味着"没测"，还意味着"错误从未被触发"** |
| **#47** | ⚠️ **同一口径在十几处各写一遍，清账时才发现**：ADR-008 把 V1 口径定为「5 条上线 + 1 条顺延 V2」，但「**6 条工作流**」这一旧计数仍散落在 `README.md` / `gantt.md` / `resource.md` / `workflow_spec.md` / `项目进度.md` / `workflow.py` 注释 / `test_plan.md` / `PRD_v1.md` / `contracts.md` / `tests/README.md` / `todolist.md` / `engine/model_registry.yaml` / `workflows/registry.yaml` / `docs/adr/0005` 里（上一批清了大部分，本轮补掉最后 3 处 + 给 ADR-0005 加勘误注）。**这不是"漏改"，是结构性必然**：同一件事写在 N 个地方，改口径时必然只改到手边的几个。教训：**口径类事实只应有一个权威落点，其它地方要么引用、要么明确标注"原口径"** |

**2026-09-17 · GPU 侧本轮新增的台账条目（#48~#51 —— 都不是「已修」，而是「已核实的边界」）**
| # | 一句话 |
|---|---|
| **#48** | ✅ **三条新工作流的渲染路径 L4 首次跑通**（各 **通过 11 · 失败 0 · 跳过 0**）：判据是**渲染路径**（注册表 `param_schema` → `engine/render` 六步 → `/upload/image` → `/prompt` → 出图 → 确定性 → 元数据闭环），**不是裸提交模板**；同 seed 两次出图 IDAT 逐字节一致；基准复测地板 SDXL **4.54s** / klein **1.52s**（与历史基线 4.59 / 1.52 一致，**无回归**）。⚠️ **不证明画质 / 不证明性能达标 / 不证明 upscale 的画质增益** |
| **#49** | 🔴 **`inpaint_v1` 本次 L4 是一次 no-op**：「成功但没干活」比报错更危险 —— 参考图 RGB 无 alpha ⇒ `LoadImage` 返回**全零 mask** ⇒ 非选区贴回覆盖整张图 ⇒ **产出与输入逐像素相同**。⇒ 11/11 **只证明渲染路径能出图**。⚠️ 放行前置还剩：探针**绕过了平台素材管线**（`lambda` 直注本地文件名，未走 `/upload/image` / `asset_resolver`）、`engine/` 与后端**都没有校验 inpaint 输入必须含 alpha** |
| **#50** | ✅ **蒙版极性结案：没有反**（三条同向证据：源码 `mask = 1 - alpha` / 出图实测 `repaint_core` eq=`0.0`(A) 与 `1.0`(B) / registry 原假设一致）。⭐ **方法论**：「保留区逐像素等于参考图」**不算证据**（那是节点 31 `ImageCompositeMasked(destination=原参考图)` 的必然结果）；判别力只在 `repaint_core` 那两个数。⚠️ 用户一句「所有图片都不自然」经拆解为 **5 张探针可视化问题 + 1 张设计使然 + 1 张真实质量信号 + 0 张极性错** ⇒ **判读结论要能拆解，否则会把悬案留成结论** |
| **#51** | ⚠️ **登记牌回填撞上「三重门 + 枚举缺口」**：`gpu_verified` 被 `engine/registry.py:219-224` **无条件**强制要求 `baseline.measured_at`；`status: enabled` 是三重门（`render_path_l4` ∧ `gpu_verified` ∧ `baseline`）；**`verification` 的三个枚举值没有一个能表达「已出图但无本口径基准」⇒ 契约缺口**。⇒ 三条统一填 `render_path_l4: true` / `status: disabled` / `verification: pending_gpu` / 无 `baseline`，**真相由 `verification_note` 承载**。⚠️ 副作用已登记：`backend/tests/test_registry_loader.py:246` **硬编码 `len(active) == 2`**，**放行任何一条都会让后端测试变红**；`engine/validate.py:280` 强制 `_meta.version == registry.version` ⇒ **`version` 不能递增**，changelog 只能标 `version: 1` |

**仍未验证的清单（交接给下一个会话，不要当成已验证）**

| # | 未验证项 | 验证方法 |
|---|---|---|
| 1 | **真实 MinIO 未验证** —— 全部存储路径由 `InMemoryAssetStorage` 替身覆盖 | 起 MinIO（P2-09 compose）后跑一次 put/get/delete + 取图/打包 |
| 2 | **真实 PostgreSQL 未验证** —— 迁移与清理的时间比较只在 SQLite 上跑过 | 在真 PG 上 `alembic upgrade head` 并跑清理任务（`DateTime(timezone=True)` 在 PG 上是真 `timestamptz`）|
| 3 | ⚠️ **worker 是否监听 `maintenance` 队列未验证** —— 仓库里**没有 Celery worker 的启动脚本 / compose / runbook** | 起 worker 时确认 `-Q` **包含 `maintenance`**；否则清理任务会静静躺在队列里永不执行（**部署层 TODO**）|
| 4 | **ETag 语义无测试钉死** —— 变异测试「ETag 改成用 `asset.id`」是**唯一 SURVIVED** | 补一条断言 `ETag == sha256(响应字节)` 的测试 |
| 5 | **画廊页浏览器人工走查仍未做** —— `GalleryPage.tsx` 已实现并调用 `/content`、`/assets/pack`，但"图能不能在浏览器里显示出来"**只有 jsdom 级证据**，没有端到端证据 | 起前后端，在浏览器里完成 上传 → 取图 → 采纳 → 打包 一遍 |
| 6 | **打包的流式路径未做慢客户端 / 断连实测** —— `finally` 关闭临时文件只有代码结构保证 | 用限速客户端拉一次大包并在中途断连，确认临时文件被回收 |
| 7 | ✅ **三条新工作流的 per-workflow baseline 已补**（2026-09-18）：i2i 3.23s / inpaint 4.03s / upscale 10.17s，`verification` 已升 `gpu_verified`。⚠️ **但「出图+baseline」不等于「放行」**：`status` 仍 `disabled`，等待 P8 画质评审 | 对 golden set 产物人工评分，一次通过率 ≥80% 后再改 `enabled` |
| 8 | ✅ **蒙版极性已结案：没有反**；`inpaint_v1` 带 alpha PNG 走 `/upload/image` 的核心语义已验证（输入/输出 162038 像素不同） | 已验证 |
| 9 | ⚠️ **CI 流水线从未运行** —— `.github/workflows/ci.yml` 已建并新增 `quality-gates`，但 `git remote -v` 为空（仓库无远端） | 配置远端并 push 一次，观察 Actions 结果 |
| 10 | ⚠️ **产物元数据未记录模型哈希** —— `meta["models"]` 只记 `name`，`engine/model_registry.yaml` 里登记的 6 个模型 sha256 **无任何消费方**去校验 | 在 `extract_models` 或写入侧补 sha256 关联，并加一条断言 |
| 11 | ⚠️ **条件 UPDATE 的原子性只在 SQLite 上验证过**（#34 新增）—— 本机全部后端测试跑 SQLite；`charge()` 在 **PG**（默认 READ COMMITTED）下的并发行为是**分析结论，不是实测结论** | 真 PG 上并发发两个提交，断言 `quota_used` **恰好**等于应收；顺带验证 `rowcount` 语义在 PG 上确为"被 WHERE 匹配到的行数" |
| 12 | ⚠️ **NSFW 视觉检测 V1 未实现**（P6-13）—— `NullNsfwDetector` **永远返回 `unknown`、永不返回 `safe`**（有测试钉死，这是**刻意不造假**）。所以「内容安全」当前只覆盖**输入侧可枚举文本** | 需先决策是否引入图像分类依赖（当前纪律是「避免重依赖」）；在决策前**不得声称"NSFW 过滤已上线"** |
| 13 | ⚠️ **P8 一次真实评测的部分已完成**：golden set 真实出图 42/42（2026-09-18），但 `expected` 仍为 `null`、rubric 锚点未标定、缺陷库 `verified_by=gpu` 仍为 0。⇒ **任何「通过率 / 得分 / 良品率 / 覆盖率」数字仍不存在** | 人工评分回填 expected；缺陷库真实验证需 GPU 窗口 |
| 14 | ⚠️ **P8-04 抽检 SOP 从未执行过一次**；**P8-09 已覆盖 5/5** | 人工抽检需真人 + 真实产物 |
| 15 | ⚠️ **P8 的打勾口径需显式声明** —— P8-02 / P8-04 / P8-06 打勾的依据是「**可验收产出存在**」（定义成稿 / 条目数 ≥30 / 参数经 `check_defect_kb.py` 机械校验），**不是**「已被真实数据验证」 | 已在 `todolist.md` 的 P8 阶段结果与 §2 增量里写明；后续任何引用 P8 完成度的地方都要带上这句 |
| 16 | ⚠️ **真实 Redis 未验证**（本轮补）—— GPU 独占锁（`gpu_lock_key`）、beat 调度、孤儿任务补投、队列深度闸门所依赖的队列**全部只在单测 / 替身上跑过** | 起 Redis 后跑一次"提交 → 排队 → worker 取锁 → 完成"的端到端；确认 `worker_concurrency=1` 与锁语义一致 |
| 17 | ✅ **生成分辨率的 `params` 通道缺口已补**（2026-09-18）：API 按工作流 Schema `min/max` 对整型字段二次校验，返回 422 `SIZE_OUT_OF_RANGE`。`width`/`height` 越界不再落库 | 已解决 |
| 18 | ✅ **`task_max_retries_hard_limit` 已有消费者**：`Settings` 加 `model_validator` 强制 `task_max_retries ≤ hard_limit`（2026-09-18 决策 3.4） | 已解决 |
| 19 | ⚠️ **本轮新增的两道闸门也只在 SQLite 上验证**（EX-5 队列深度闸门 / 每用户在途上限）—— 与 #34 同一局限：`in-flight` 计数是**读**，判据是**提交前的一次查询**；在真并发提交下"两个请求同时读到相同在途数"的竞态**未在真 PG 上验证** | 起真 PG 后并发提交多批，断言在途数不会突破上限（或在文档里明确"该闸门是尽力而为、非硬保证"） |

**必须继承的纪律（每条都有代价换来的）**
1. **不猜，先验证**：机制推断已连续翻车 3 次（cfg 推断 / ADV_CLIP_emb 推断 / 校验器归因），最终都靠**读源码或做实验**定案
2. **判据本身要能被检出**：凡新增校验/探针，**交付前故意让它失败一次**（G9；本轮枚举 parity 门禁做了 3 个变异验证）
3. **不谎报验证状态**：没出过图就写 `pending_gpu`；无卡模式下所有 L4 结论标「待 GPU 验证」
4. **「测试全绿」≠「没有冲突」**：工作区绿 ≠ 仓库完整（#21）、测试绿 ≠ 无重复定义（#25）
5. **文件单一所有者**：跨流改动走消息请求，由所有者落笔；**同一实现不得留两份**（#22，本轮据此把 SQLite 适配合并成一份）
6. **远端多步逻辑一律写成 `.sh` 上传执行**；停进程用**锚定正则**（`pkill -f` 会自杀 —— 本轮又踩了一次，改按 PID 杀）
7. **ruff 必须在仓库根跑**：根 `pyproject.toml` 与 `backend/pyproject.toml` 各有一份配置，CWD 不同结论不同；**因此 `web/` 内不得再放任何 Python 配置**
8. **三条进度文档同步**：`todolist.md`(计划) / `项目进度.md`(度量) / `项目进展.md`(纪实，六段格式)
9. 🔴 **"读-改-写"在并发下就是丢更新**（#34）：额度 / 计数 / 库存这类**有上限的数值字段**，判据必须写进 **SQL 的 `WHERE`** 再看 `rowcount`；`应用层读+if+写` 在并发同步路由下等于没有校验。**别用 `FOR UPDATE` 硬凑**（SQLite 不支持 ⇒ 开发/测试与生产分叉）
10. 🔴 **凭据必须在"将要执行任务的进程启动时"就被继承**：在别的终端 `export` 对**已在运行**的进程无效（`/proc/<pid>/environ` 扫描坐实）。GPU 动作的顺序永远是 **先验凭据 → 再确认有卡 → 才点开机**
11. 🔴 **"跳过"绝不能被计入"通过"**（#38）：`check_golden_set.py` 曾把"跳过 L3"记进 `levels_run`（谎称已执行），且跳过不算不通过 ⇒ `--no-l3` 会 exit 0，CI 会把「少做一层」当全绿。**退出码必须把"少做了一层"当失败**
12. 🔴 **数结构化数据的条目要用解析器，不要用 `grep -c` 数文本行**（#37）：`negative.yaml` 的 `severity: hit` YAML 解析 **20** 条、`grep -c` 得 **21**（多的是 `:17` 注释里那条）
13. 🔴 **拿到"别人写的、从没跑过的"产物，第一步是真跑一遍 + 真 lint 一遍**（#35）—— 读代码只能判质量、不能判可用性（那份 630 行校验器连**导入**都做不到）；**修一处陈述要顺手 grep 同一陈述的全部副本**（docstring / argparse / README / 契约），否则"修好了"本身是幻觉（#36）
14. 🔴 **`.gitignore` 的判断要用 `git check-ignore --no-index -v` 对真实文件做，不要靠推理**（#39）；**「不记原词」的断言要顺带覆盖"报错文案里也不能出现原词"**（#40）
15. 🔴 **测试里的期望值必须钉「规格」，不能从被测实现里算出来**（#43）：`test_queue_guard.py` 起初把硬阈值写成 `_SOFT * 实现里的常量`，于是**把系数改成 1000 时用例照旧全绿**；改成**字面量**后同一个变异体让 **6 条变红**。凡"期望值跟着实现一起变"的断言，等于没有断言
16. 🔴 **「遍历式」用例必须配一条「枚举非空」的元测试**（#44）：`AC-N5` 若直接遍历 `app.routes`，FastAPI 的 `_IncludedRouter` 会让**一条 API 路由都看不到** ⇒ 参数化为空 ⇒ **全部用例消失、静默假绿**（正确做法：`app.openapi()`）。**"一条都没跑"和"全部通过"在报告里长得一模一样**
17. 🔴 **污染源可能不在被测代码里，而在测试基础设施里**（#45）：`alembic/env.py` 的 `fileConfig()` 默认会**永久禁用**应用 logger，而 `caplog` 不感知 `logger.disabled` ⇒ 此后所有日志断言静默拿到空列表（假绿/假红）。**"没有输出"要先怀疑测量工具**
18. 🔴 **零覆盖不只意味着「没测」，还意味着「错误从未被触发」**（#46）：`conftest.py` 的外键环拆卸缺陷**一直存在**，只是全仓库从没有用例创建过 `templates` 行；`AC-F6` 一补上就炸，且错误记在 **teardown** 上、看起来像用例自己坏了
19. 🔴 **口径类事实只应有一个权威落点**（#47）：「6 条工作流」在十几处各写一遍，改口径时必然只改到手边的几个 ⇒ 其它地方要么**引用**、要么明确标注"**原口径**"，不要复述数字
20. 🔴 **「验证通过了」与「可以放行了」是两件事**（#51）：登记牌的三重门（`render_path_l4` ∧ `gpu_verified` ∧ `baseline`）意味着**出图只是第一关**；`gpu_verified` 被**无条件**强制要求 `baseline.measured_at`，而枚举里**没有一个值**能表达「已出图但无本口径基准」⇒ 遇到这种空洞要**如实记录缺口**，**不要改枚举绕开、更不要拿跨口径的数字顶替**
21. 🔴 **「跑通了」要先问「跑的是不是它该干的事」**（#49）：`inpaint_v1` 的 L4 **11/11 全绿，实际却是一次 no-op**（无 alpha ⇒ 全零 mask ⇒ 产出与输入逐像素相同）。**绿灯只说明判据被满足，不说明语义被行使** —— 对「成功但没干活」这类失败，**报错反而更好**
22. 🔴 **判读结论必须能被拆解**（#50）：用户一句「所有图片都不自然」，拆开是 **5 张探针可视化问题 + 1 张设计使然 + 1 张真实质量信号 + 0 张极性错**。**不拆解就会把悬案留成结论**；⭐ 同时记住**有些"证据"是恒真的**：「保留区逐像素等于参考图」是节点 31 `ImageCompositeMasked(destination=原参考图)` 的**必然结果**，判别力只在 `repaint_core`

### 0.1 今日操作节点（2026-09-16）

> 上午：引擎升级与性能回归收口（节点 1~39，见下）。下午：并行开发与三流产出（节点 40~54）。
> **夜：P6-10 产物消费链路 + 生命周期清理（节点 55~66）—— GPU 未使用，全程本地 / 离线完成。**

| # | 节点 | 结果 |
|---|---|---|
| 1 | GPU 开机后核查下载状态 | 发现全精度文本编码器**只下到 29%**（09-15 22:04 关机时被中断），aria2c 进程已被杀 |
| 2 | 续传文本编码器 | ✅ 完成 7.4GiB（09:15:22，峰值 17 MiB/s，约 6 分钟） |
| 3 | 启动 ComfyUI + SDXL 性能标定 | ✅ **P95 = 5.07s**（1024²/30 步，N=12）；⚠️ 发现此前记录的 12.0s 是**冷启动值**（含模型加载），非稳态 |
| 4 | FLUX.2 klein 冒烟测试 | ❌ 失败：`CLIPTextEncodeFlux` 报 `KeyError: 't5xxl'` |
| 5 | 根因定位（逐层读 ComfyUI 源码） | **v0.3.75 早于 klein 发布 2 个月**；`Flux2.clip_target` 直接 `return None # TODO`；`QWEN3_4B` 被路由到 `z_image` 而非 flux2 |
| 6 | 升级决策 | 用户选定：**升级到最新稳定版 v0.36.0**（并顺带做 P2-05 节点白名单） |
| 7 | 切换无卡模式 | ✅ 确认（cgroup：`memory.max=2GB` / `cpu.max=0.5核` / GPU 不可见）。策略：**慢而便宜的升级放无卡，需 GPU 的验证留到最后一次性切** |
| 8 | 升级前只读侦察 | ✅ 回退锚点可靠（tag `v0.3.75`）· ⚠️ **发现软链隐患**（`checkout -f` 会顶掉软链、切断数据盘模型挂载）· ⚠️ 发现 `custom_nodes/$LOCK` 脏文件 |
| 9 | 拉取 v0.36.0 | ✅ 1696 个提交。给 git 加**内存护栏**（单线程/32m 窗口）防 2GB OOM，实测峰值 357MB |
| 10 | 依赖预演（不落地） | ⚠️ 发现 `comfyui-workflow-templates==0.11.62` **阿里云镜像未同步**（镜像最高 0.11.60）→ 加 `--extra-index-url https://pypi.org/simple` 兜底 |
| 11 | 代码切换到 v0.36.0 | ✅ HEAD `ee71d5c4`。**软链保护生效**：摘链 → checkout → 校验占位目录 <10MB 才删 → 挂回软链；4 个模型文件全部可见 |
| 12 | 安装依赖 | ✅ 16 个包 `pip exit=0`；torch/numpy 未变 |
| 13 | ⚠️ **发现 torch 真实下限** | 导入报 `infer_schema: stride has unsupported type list[int]`。追链到 `comfy_kitchen/backends/eager/conv3d.py`。**验证「降级 comfy-kitchen」不可行**（早期版本根本没这个文件）。查 README:195 确认 **最低 torch 2.7、20 系以上必须 cu130+** |
| 14 | torch 升级 | ✅ **2.5.1+cu124 → 2.13.0+cu130**（回避发布仅 14 天的 2.14.0）。下载约 3.2GB，先卸旧释放 4.4G 再装；系统盘余 9.4G |
| 15 | CPU 模式节点审计（省 GPU 机时） | ✅ 节点 **2102 → 2530**；16 个关键节点齐备。❌ 发现 `flash_attn_2_cuda.so` **ABI 失配**拖垮 3 个核心模块 + `ComfyUI_essentials` |
| 16 | 修复 flash-attn 连锁故障 | ✅ 卸载 flash-attn（可选加速，自动回退原生注意力），4 个模块全部恢复；顺带清理 `$LOCK` |
| 17 | 确认 klein 路由已打通 | ✅ `sd.py:1888` 中 `QWEN3_4B + CLIPType.FLUX2 → klein_te + KleinTokenizer`；`Flux2.clip_target` 不再是 TODO |
| 18 | 工作流纠错 | ⚠️ 原 `CLIPTextEncodeFlux` 不适用于 klein → 依**官方模板**改为 `CLIPTextEncode + CFGGuider + ConditioningZeroOut`；**并发现我们下的是蒸馏版（4 步、cfg=1）** |
| 19 | 提交前静态校验 | ✅ 新增 `26_validate_workflow.py`，两个工作流 `VALIDATE=PASS`（不花 GPU 机时就查出节点/入参/连线错误） |
| 20 | 待 GPU 验证 | ✅ 已切回有卡并完成（见 21~23），脚本 `27_gpu_verify.sh` 一条命令跑完 |
| 21 | 切回有卡 → GPU 侧一次过验证 | ✅ torch 2.13.0+cu130 真正用上 4090（`sm=(8,9)`、矩阵乘自测通过）；ComfyUI v0.36.0 启动正常 |
| 22 | SDXL 防回归冒烟 | ⚠️ 出图成功，但 **median 7.26s / P95 7.72s**，较升级前 4.60s **回归 1.58x** |
| 23 | **FLUX.2 klein 首次出图** | ✅✅ **本轮升级目标达成**：蒸馏版 4 步/cfg=1 出图成功，**median 1.83s / P95 2.36s**（显存峰值 15.8G/24G） |
| 24 | 尝试恢复注意力加速 | ❌ 死路：flash-attn 与 SageAttention 均因 torch ABI 失配（`undefined symbol: torchCheckFail`）无法加载；脚本已自动回退默认注意力 |
| 25 | 排除「注意力」嫌疑 | ✅ 日志确认升级前后**同为 `Using pytorch attention`**（xformers 从未安装、flash-attn 仅在显式 `--use-flash-attention` 时启用，启动脚本从未传过）→ **注意力不是回归原因** |
| 26 | 锁定新嫌疑：v0.36 新增的默认开关 | `Using async weight offloading with 2 streams` + `DynamicVRAM support detected and enabled` —— 两者均为**本次升级新增**且 **Nvidia 上默认开启** |
| 27 | 本地会话被网络中断打断（API 502） | 实例与 ComfyUI 未受影响（仍在 GPU 模式运行）；恢复后核查状态全部正常 |
| 28 | 打通网页访问（用户要手动生图测试） | ✅ 建立本机 SSH 本地转发 `127.0.0.1:8188 → 服务器 127.0.0.1:8188`，实测 HTTP 200 |
| 29 | SDXL 回归 A/B 定位实验 | ✅ **元凶锁定 dynamic VRAM**：单关 async offload 无效（7.18s≈默认 7.10s），关掉 dynamic VRAM 即回 4.7~4.9s |
| 30 | 生产配置收口验证 | ✅ **定案 `--highvram`**。用「地板」判读：4.59s vs 升级前基线 4.54s（**差 1%**）；`--disable-dynamic-vram` 地板 4.85s，明显更差。**klein 两档均保持 median 1.83s，未被牺牲** |
| 31 | 配置固化进启动脚本 | ✅ `03_start_comfyui.sh` 新增 `COMFY_EXTRA_ARGS`（默认 `--highvram`），注释写明理由/实测/失效条件；按生产路径重启并核对 `argv` |
| 32 | ⚠️ `pkill -f` 自匹配事故 | 内联 ssh 命令里的 `pkill -f "main.py --listen"` 把**执行它的父 shell 自己也杀了**（该 shell 命令行含此串），ComfyUI 被杀后未重启 → 服务空窗。改锚定正则 + 脚本化（`31_restart_prod.sh`） |
| 33 | 文档同步 | ✅ `项目进展.md` 新增 #17/#18/#19 三条台账；`PRD_v1.md` NFR-1 按稳态口径重写（含地板口径说明）；`项目进度.md`/`todolist.md` 同步 |
| 34 | 重做版本锁（P2-06） | ✅ `deploy/versions.lock` 426 行，含 `[launch]`/`[models]` 两段，已回传本地 |
| 35 | 恢复中断的会话 | 核查实例状态：仍在有卡模式、ComfyUI 正常运行 —— 中断只影响本地会话，服务器无损失 |
| 36 | 分步提交本次成果 | ✅ 5 个语义化提交：引擎升级脚本 / 验证与基准脚本 / 生产配置+版本锁 / 后端骨架 / 文档 |
| 37 | ⚠️ 发现「未提交且未入文档」的既有工作 | `backend/` 完整 FastAPI 骨架（3100 行 / 31 测试全绿 / 18 条路由）建于 09-15 21:53，但**既未提交也未进任何文档**，且 ruff 有 6 个问题、mypy 未安装 → 补交、修 lint、补文档（见 `项目进展.md` #20） |
| 38 | 撰写开发环境操作手册 | ✅ `docs/sop/quickstart.md`（15 分钟从零到出图）+ `docs/sop/runbook.md`（15 节详细说明：连接/生命周期/出图/性能口径/加模型/版本锁/后端/排错/脚本索引/文档纪律/红线）；43 条内部链接全部校验通过 |
| 39 | 修正 README 坏引用 | 原「快速开始」指向不存在的 `docs/deploy.md` → 改为指向新手册，并补上当前环境基线与实测性能 |
| 40 | **冻结跨流接口契约**（并行开发前置） | ✅ `docs/sop/contracts.md`（515 行）：任务状态机 / REST API / 工作流参数 Schema / 埋点事件名 四项。查并修正 2 处 PRD 未写清、前端必踩的行为事实（提交后状态是 `queued` 非 `pending`；`canceled` 亦可重试） |
| 41 | ⚠️ **发现仓库根本不完整** | `.gitignore` 的 `models/` 规则不带前导斜杠 → 误挡 `backend/app/models/` 下**全部 8 个 ORM 文件**，从未入库。已根锚定修复 + 补交（见 `项目进展.md` #21） |
| 42 | 建立「干净克隆验证」纪律 | ✅ 判据：克隆到 `/tmp` 后跑测试 `31 passed` + lint 通过 + 7 个模型模块可导入 + 枚举数量与契约吻合（7/7/13）。**这四项只有仓库真的完整才可能同时成立** |
| 43 | 启动 A/B/C 三流并行 | 🔵 契约冻结完成，三流进入并行 |
| 44 | GPU 窗口：跑通两条工作流的 L4 | ✅ `t2i_v1` 11 项 0 失败 / `flux2_klein_t2i_v1` 13 项 0 失败；注册表放行 `enabled`（**渲染路径**打通，此前只验过裸提交模板） |
| 45 | GPU 窗口：G7 权重探针 | ✅ 用户判读为**情况 C 字面污染**；并定位 `klein-bnk` 崩溃根因到文件行（`adv_encode.py:266` 硬编码 `tokenized['l']`） |
| 46 | GPU 窗口：卸载 sageattention | ✅ **修好 2 个节点**（`smZNodes` + `nunchaku` 同源），节点类 2530→2534；`TeaCache` 仍坏（真因不同） |
| 47 | GPU 窗口：稳态基准复测 | ✅ 地板 SDXL 4.55s / klein 1.52s，与升级前一致 → **无回归**（判据用地板，见 #18） |
| 48 | GPU 窗口：验证 ComfyUI 端点 | ✅ `/queue`、`/upload/image`（字段名确为 `image`）、`/upload/mask`、`/queue`(delete) 4/4 通过 |
| 49 | A 流清理重复实现 | ✅ `injector.py` 已删、`param_bindings` 已删（留注释说明为何删）、`.env.example` 重写 63 项 + 防漂移测试 |
| 50 | 方向一：三条新工作流 | ✅ `i2i_v1` / `inpaint_v1` / `upscale_v1`，节点 ID 按规范 §2 分段；均 `L1/L2/L3 PASS`，L4 待卡 |
| 51 | 新增节点概念文档 | ✅ `docs/sop/comfyui_nodes.md`：四规则 + 执行模型 + **节点三身份** + 5 条硬约束 + 最小可用节点集速查表 |
| 52 | 修校验器缺陷（#27） | ✅ `check_object_info` 加了运行时素材豁免（精确到 `(节点,入参)`）+ 正对照测试；此前**任何带参考图的工作流都过不了 L1/L2/L3** |
| 53 | 方向 B：素材上传接口（P6-09） | ✅ 核查发现 `asset_resolver` 已由 A 流实现完毕，**真正缺的是上传接口** → 新增 `POST/GET /assets` + 详情 + 软删除；图片头解析（魔数判格式 + 取尺寸，**不引入 Pillow**）；后端 215 passed |
| 54 | 修正进度计数错误 | ⚠️ P6 原记 `0/13`，但后端早有 18+ 路由与状态机/队列/鉴权 → 校正为 **6/13**；总进度 35% → **41%** |
| 55 | 2026-09-16 夜：读三份文档 + 冻结契约 + memory，确定本轮为 **P6-10 产物存储与生命周期** | ✅ 范围：受控取图 / 标记采纳 / 打包下载 / 生命周期清理 |
| 56 | 实现 `GET /assets/{id}/content` **受控取图** | ✅ 握手定案：**不用 MinIO 预签名 URL**（三层理由见契约 §2.3）。越权 / 回收站 404；**存储读失败 500（刻意不伪装成 404）**；`Content-Type`/`ETag`(sha256 实际字节)/`Cache-Control: private, max-age=300`/`download=1`→attachment；**只有 `download=1` 才埋 `image_downloaded`** |
| 57 | 实现 `PATCH /assets/{id}` 标记采纳 / 收藏 | ✅ 仅 `kind=output` 可标采纳（否则 409）；空 body 422；`image_adopted` **只在 False→True 跃迁**埋点；写 AuditLog |
| 58 | 实现 `POST /assets/pack` 打包下载 | ✅ zip 按 **SKU 分目录**（`params.sku`，空则回退 `task_{id}`）；`SpooledTemporaryFile` + `ZIP_STORED`（整包写完才 `seek(0)` —— 请求级 session 会先关）；`asset_ids` 有找不到的 → **404 不静默少给**；0 张 / 超 `max_pack_assets`(200) → 422；SKU 过白名单防 **zip-slip** |
| 59 | 扩展 `GET /assets` 筛选 + `AssetOut` 补字段 | ✅ `task_id` / `batch_id`(join Task) / `adopted_only` / `favorite_only` / 时间范围（naive 按 UTC 归一化）；`total` 与列表**共用同一组 conditions 与同一个 join**；`AssetOut` 补 `task_id` 与 `meta`（FR-5.4 元数据抽屉的唯一数据源）|
| 60 | 实现生命周期清理 `app.worker.maintenance.cleanup_assets` | ✅ beat **每 6h**（21600s）投到 `maintenance` 队列。两条规则：回收站超期(30d) / 产物超期（仅当 `>0`；**已采纳 / 已收藏永久豁免**）。**先删对象再删记录；对象删失败 → 跳过该条、保留记录**（否则产生永远发现不了的孤儿对象，因为我们没有 list 对象的能力）|
| 61 | 新增 4 个配置项并登记 `.env.example` | ✅ `ASSET_TRASH_RETENTION_DAYS` / `ASSET_OUTPUT_RETENTION_DAYS` / `MAX_PACK_ASSETS` / `CLEANUP_BATCH_LIMIT`；被 `tests/test_env_example.py` 双向门禁自动纳入 |
| 62 | 补 P6-10 回归测试 | ✅ `test_assets.py` 59 条 + `test_maintenance.py` 18 条（**"接口返回 200"不算通过，要看副作用**：字节逐字节相等、events 表真的只多一条）|
| 63 | 全量校验 | ✅ `pytest -q` → **307 passed**（基线 256，+51）；仓库根 `ruff check .` → `All checks passed!`；`python -m engine` → `VALIDATE=PASS`（L1/L2/L3，⚠️ **仍不代表能出图，L4 未执行**）|
| 64 | 变异测试（G9 纪律：判据必须能被检出） | ✅ 取图/采纳/打包那轮 **9 项 8 KILLED / 1 SURVIVED**（SURVIVED＝「ETag 改成用 `asset.id`」→ **ETag 语义没有测试钉死，已知缺口**）；清理那轮 **6/6 KILLED**（含"去掉采纳豁免""去掉收藏豁免""失败也删记录""去掉条数上限""beat 改成 60s"）；zip-slip 防护注入变异后 **5 条用例立即变红** |
| 65 | 核实 P2-12 并打勾 | ✅ `.env.example` ↔ `Settings` **68 ↔ 68**，`pytest tests/test_env_example.py -q` → **5 passed**。原记载「30 个变量全部对不上」**已不存在** → P2-12 标 `[x]`；⚠️ 但 **pre-commit 密钥门禁仍不存在**（并入 P11-11）|
| 66 | 同步四份文档 | ✅ `docs/sop/contracts.md`（§2.1/§2.3/§2.4/§4/§5/§7）+ `todolist.md` + `项目进度.md` + `项目进展.md`（台账 #30/#31）|

**P6-09 完成度**：上传 / 列表 / 详情 / 软删除 ✅ + 筛选扩展（task / batch / 采纳 / 收藏 / 时间）✅；**文件夹 / 标签 / 搜索 / 回收站恢复 待做**
**P6-10 完成度**：`AssetStorage`（MinIO + 内存）✅；**受控取图 / 标记采纳 / 打包下载 / 生命周期清理 ✅ 本轮全部完成**（⚠️ 真实 MinIO 与真实 PG 未验证，见 §0.2）

---

## 1. 里程碑总表

| 里程碑 | 计划完成 | 实际完成 | 状态 | 关键交付物 | 验收标准 |
|---|---|---|---|---|---|
| **M0 计划就绪** | 2026-09-15 | 2026-09-15 | ✅ 完成 | `todolist.md`、`项目进度.md` | 计划可执行、可度量 |
| **M0.5 仓库就绪** | 2026-09-15 | 2026-09-15 | ✅ 完成 | 目录骨架、`README.md`、`LICENSE`、ADR ×4、许可矩阵、贡献规范 | 仓库可克隆，规范与决策成文 |
| **M1 产品定义完成** | 2026-09-20 | — | 🔵 进行中 18/20 | MRD、PRD、画像、流程图、原型、甘特、埋点方案 | 7 份文档成稿且互相自洽 |
| **M2 环境可复现** | 2026-09-27 | — | ⬜ 未开始 | Dockerfile、docker-compose、`versions.lock` | 销毁后重建仍能出同一张图 |
| **M3 工作流可用** | 2026-10-11 | — | ⬜ 未开始 | **5 条**工作流 + 注册表 + 参数模板 | **5 条**全部稳定出图，参数可外部注入（**1 条顺延 V2**，见 `docs/adr/0008-v1-scope-reduction.md` 裁定 1） |
| **M4 控制体系成型** | 2026-10-18 | — | ⬜ 未开始 | 控制能力矩阵 + 一致性方案 | 同商品 10 次生成一致性 ≥4/5 |
| **M5 平台可跑批** | 2026-10-25 | — | ⬜ 未开始 | API + 队列 + 鉴权 + 存储 + 批量 | 1000 张无人值守完成，重试成功率 ≥95% |
| **M6 前端可用** | 2026-11-01 | — | ⬜ 未开始 | 工作台、任务中心、画廊、批量向导 | 非技术用户可自助完成全流程 |
| **M7 质量与性能达标** | 2026-11-08 | — | 🔵 进行中（**离线定义 3/10 · 零实测**） | golden set、良品率看板、压测报告、成本模型 | 良品率 ≥80%；8h 压测零崩溃 |
| **M8 V1 发布** | 2026-11-22 | — | ⬜ 未开始 | v1.0.0 Release、文档、复盘、V2 规划 | 内测 ≥5 人，≥3 人愿继续使用 |

状态图例：⬜ 未开始 · 🔵 进行中 · ✅ 完成 · ⚠️ 有风险 · 🔴 延期

---

## 2. 阶段进度汇总

| 阶段 | 名称 | 计划周期 | 任务数 | 完成 | 进度 | 状态 |
|---|---|---|---|---|---|---|
| P0 | 项目启动与规范 | D1-D2 | 8 | 8 | 100% | ✅ |
| P1 | 产品定义（MRD/PRD/画像/流程/原型） | W1 | 20 | 18 | 90% | 🔵 |
| P2 | 基础设施与环境 | W1-W2 | 13 | 12 | 92% | 🔵 |
| P3 | 工作流内核 | W2-W4 | 12 | 8 | 67% | 🔵 |
| P4 | 控制体系 | W3-W5 | 10 | 0 | 0% | ⬜ |
| P5 | 模型与提示词资产 | W2-W6 | 11 | 4 | 36% | 🔵 |
| P6 | 平台服务化 | W4-W6 | 13 | 8 | 62% | 🔵 |
| P7 | Web 前端 | W5-W7 | 12 | 3 | 25% | 🔵 |
| P8 | 质量体系 | W6-W7 | 10 | 5 | 50% | 🔵 |
| P9 | 性能与稳定性 | W7-W8 | 11 | 0 | 0% | ⬜ |
| P10 | 数据监测 | W7-W8 | 10 | 0 | 0% | ⬜ |
| P11 | 文档与开源化 | W8-W9 | 13 | 1 | 8% | 🔵 |
| P12 | 测试与 V1 发布 | W9-W10 | 10 | 0 | 0% | ⬜ |

> ⚠️ **2026-09-16 晚口径修正**：本表原先与「一屏速览」**自相矛盾** —— 速览写 P2 7/13、P5 4/11，
> 表里却是 P2 5/13、P5 0/11（P5-06 模型注册表 / P5-08 提示词库 / P5-10 许可矩阵的产出其实都在）。
> 已统一到本表。同时修正了分母：原写「约 131 项」，按本表各行求和实为 **153 项**
> —— 所以总进度百分比由 41% 变为 **35%**，**这不是进度倒退，是分母以前算小了**。
> 教训与 #21/#25 同类：**同一件事在两个地方各算一遍，迟早对不上**。
>
> **2026-09-16 夜增量**：P6 6 → **7**（P6-10 完成）、P2 8 → **9**（P2-12 核实后完成）；
> 完成数 54 → **56**，总进度 35% → **37%**。⚠️ **分母仍为 153，未变** —— 本轮不是修正分母，
> 是真实完成两项。后续所有百分比一律以本表各行求和为准。
>
> **2026-09-17 凌晨增量**：P2 9 → **10**（P2-11 CI 流水线 + 仓库卫生门禁完成）、P7 2 → **3**（P7-05 画廊与素材库页完成）；
> 完成数 56 → **58**，总进度 37% → **38%**（58 ÷ 153 = 37.9%）。⚠️ **分母仍为 153，未变**。
> 算式（按本表各行求和）：**8+18+10+8+0+4+7+3 = 58**。P6 本轮**无新增完成项**（P6-10 已在 09-16 夜计入 7）。
>
> **2026-09-17 凌晨·续增量**：**无新增完成项，完成数仍 58/153、总进度仍 38%**。
> P6-08 由「未做」变为**部分完成**（配额扣减的**并发超发缺陷**已修 + 402 结构化错误体 + 8 条测试），
> 但**限流（按接口 / 并发上限 / `queue_max_depth`）一行代码都没有**，且重试二次扣额 / 失败退额两个口径待拍板
> ⇒ **按"任务标题含限流"的口径不打勾**（口径与 P6-09「部分完成不计入」一致）。
> ⚠️ 同类提醒：**"修了缺陷"不等于"完成了任务"** —— 若把本次计入 P6-08，P6 会虚报成 8/13。
>
> **2026-09-17（P6-08 配额 / P6-13 内容安全 / P8 质量体系）增量**：P8 0 → **3**（P8-02 评分标准定义 / P8-04 抽检 SOP 定义 / P8-06 缺陷知识库 44 条且参数经机械校验）。
> 完成数 58 → **61**，总进度 38% → **40%**（61 ÷ 153 = 39.87%）。
> ⚠️ **分母仍为 153，未变**。算式（按本表各行求和）：**8+18+10+8+0+4+7+3+3+0+0+0+0 = 61**。
> **P6 仍为 7/13**（本轮 P6-08 与 P6-13 都是**部分完成/部分交付**，按同一口径**不计入**；若计入会虚报成 9/13）。
> ⚠️ **P8 打勾口径**：`[x]` 三项的依据是「**可验收产出存在**（定义成稿 / 条目数 ≥30 / 参数经 `check_defect_kb.py` 机械校验）」，
> **不是**「已被真实数据验证」—— **P8 十项一次真实评测都没跑过**，任何「通过率 / 得分 / 良品率 / 覆盖率」数字一律不存在。
> ⚠️ **P8-08 拆成 `08a`（离线门禁，可做）/ `08b`（真实 golden set 回归，待 GPU）是子项拆分，不改变阶段任务数**（P8 仍计 **10** 项，**分母不动**）。
>
> **2026-09-17（P6-08 收口 / 四个缺陷修复 / ADR-008）增量**：**P6 7 → 8**（P6-08 完成：配额原子化 + EX-5 队列深度闸门 + 每用户在途任务数上限；「按接口限流」**经裁定 V1 不做**并写明理由）。
> 完成数 61 → **62**，总进度 40% → **41%**（62 ÷ 153 = **40.52%**）。
> ⚠️ **分母仍为 153，未变**。算式（按本表各行求和）：**8+18+10+8+0+4+8+3+3+0+0+0+0 = 62**。
> **P8 仍为 3/10**（本轮没有新增「定义类产出」）；**P6-09 / P6-13 仍不计入**（分别为部分完成 / 部分交付）。
> ⚠️ **为什么这次给 P6-08 打勾**：上一轮不打勾的理由是「标题含限流，而限流一行代码都没写」——
> 现在限流**已明确裁定不做并写明依据**，而真正有意义的那一半（队列闸门 / 每用户在途上限）**有产出、有测试**，
> 与 P6-09「部分完成不计入」的口径不再冲突。
> ⚠️ **P6-08 的三项能力都在 SQLite 上验证** —— 真实 PG 的并发行为**未验证**（见 §0.2 清单第 11 / 19 条）。
> ⚠️ **仍不给任何「通过率 / 得分 / 良品率 / 覆盖率」数字**：P8 十项一次真实评测都没跑过。
>
> **2026-09-19 增量**：P2 10 → **12**（P2-07 环境复现脚本 + P2-09 docker-compose）；P8 3 → **5**（P8-08a 离线门禁接入 CI/pre-push + P8-09 可复现性覆盖 5/5）；P11 0 → **1**（P11-02 部署文档）。
> 完成数 62 → **66**，总进度 41% → **43%**（66 ÷ 153 = **43.14%**）。
> ⚠️ **分母仍为 153，未变**。算式（按本表各行求和）：**8+18+12+8+0+4+8+3+5+0+0+1+0 = 66**。
> ⚠️ **P8-01 / P8-08b 仍不算 `[x]`**：golden set 真实出图 42/42 已完成，但 `expected` 仍为 `null`、未做人工评分，所以只算 `[~]`。
> ⚠️ **P8-09 打勾口径**：5 条工作流「同 seed 两次出图 IDAT 一致」已有真实 GPU 证据，不是定义类产出。
>
> **2026-09-17（三条新工作流的渲染路径 L4 首次跑通 + 蒙版极性结案 + registry 回填）增量**：**无新增完成项，完成数仍 62/153、总进度仍 41%、P3 仍 8/12**。
> 本轮做的是**补验证证据**，不是补产出：三条新工作流（`i2i_v1` / `inpaint_v1` / `upscale_v1`）的**渲染路径 L4** 各 **通过 11 · 失败 0 · 跳过 0**，
> `workflows/registry.yaml` 三条由 `render_path_l4: false` 回填为 `true`；蒙版极性人工判读结案为「**没有反**」。
> ⚠️ **为什么计数不动（三条例由，缺一不可）**：
> ① **登记牌仍 `status: disabled` + `verification: pending_gpu`** —— `status: enabled` 是**三重门**（`render_path_l4` ∧ `gpu_verified` ∧ `baseline`），本轮只满足了第一道；
> ② **契约缺口**：`verification` 的三个枚举值**没有一个**能精确表达「渲染路径已出图、但无本口径基准」（`gpu_verified` 被 `engine/registry.py:219-224` **无条件**要求 `baseline.measured_at`）—— 真相改由 `verification_note` 承载；
> ③ **`inpaint_v1` 的本次 L4 是一次 no-op**（参考图 RGB 无 alpha ⇒ `LoadImage` 返回**全零 mask** ⇒ 非选区贴回覆盖整张图 ⇒ **产出与输入逐像素相同**），**核心语义（蒙版区重绘）没有被行使**。
> ⇒ **禁止**把本轮写成「三条工作流全部可用」；也**不得**拿 #18 的 SDXL **30 步** 4.54s 去顶 `inpaint_v1` 的 **25 步**（那是编造）。
> ✅ 附带确认：基准复测地板 SDXL **4.54s** / klein **1.52s**（N=12 warmup=1），与历史基线（4.59 / 1.52）一致 ⇒ **无回归**；`--highvram` 确认生效（`argv` 有它、日志有 `Set vram state to: HIGH_VRAM`、且**没有** dynamic VRAM 那行）。
> ⚠️ **仍不给任何「通过率 / 得分 / 良品率 / 覆盖率」数字**：P8 十项一次真实评测都没跑过；本轮 L4 **只证明渲染路径能出图**，**不证明画质、不证明性能达标、不证明放大增益成立**。

**关键路径**：P2 → P3 → P6 → P7 → P8 → P12（任一环延期直接推迟发布）

---

## 3. 周计划与周记

### W1 · 2026-09-14 ~ 09-20 · 主题：**定方向 + 打地基**

**计划任务**
- [ ] P0 全部（P0-01 ~ P0-07）
- [ ] P1 全部（P1-01 ~ P1-20）
- [ ] P2 启动（P2-01 ~ P2-04，至少环境跑起来）

**周记**（周五填）

| 项 | 内容 |
|---|---|
| 计划完成 | / |
| 实际完成 | / |
| 产出清单 | / |
| 偏差与原因 | / |
| 数据观察 | / |
| 下周调整 | / |

---

### W2 · 2026-09-21 ~ 09-27 · 主题：**环境可复现 + 工作流起步**

**计划任务**：P2 收尾（P2-05 ~ P2-13）、P3 启动（P3-01 ~ P2-04）、P5 启动（目录规范、底模评测）
**周记**：（周五填）

---

### W3 · 2026-09-28 ~ 10-04 · 主题：**标准工作流 + 控制体系**

**计划任务**：P3-05 ~ P3-12、P4-01 ~ P4-03、P5-02 ~ P5-05
**周记**：（周五填）

---

### W4 · 2026-10-05 ~ 10-11 · 主题：**M3 工作流可用 + 服务化启动**

**计划任务**：P3 收尾验收、P4-04 ~ P4-06、P6-01 ~ P6-04
**周记**：（周五填）

---

### W5 · 2026-10-12 ~ 10-18 · 主题：**M4 控制体系 + 服务化主体**

**计划任务**：P4-07 ~ P4-10、P6-05 ~ P6-09、P7-01 ~ P7-02
**周记**：（周五填）

---

### W6 · 2026-10-19 ~ 10-25 · 主题：**M5 可跑批 + 前端主体 + 质量体系启动**

**计划任务**：P6-10 ~ P6-13、P7-03 ~ P7-07、P8-01 ~ P8-02、P5 收尾
**周记**：（周五填）

---

### W7 · 2026-10-26 ~ 11-01 · 主题：**M6 前端可用 + 质量度量成型**

**计划任务**：P7-08 ~ P7-12、P8-03 ~ P8-10
**周记**：（周五填）

---

### W8 · 2026-11-02 ~ 11-08 · 主题：**M7 性能压测 + 数据监测**

**计划任务**：P9 全部、P10 全部
**周记**：（周五填）

---

### W9 · 2026-11-09 ~ 11-15 · 主题：**文档开源化 + 内测**

**计划任务**：P11 全部、P12-01 ~ P12-05
**周记**：（周五填）

---

### W10 · 2026-11-16 ~ 11-22 · 主题：**M8 发布 + 复盘 + V2 规划**

**计划任务**：P12-06 ~ P12-10
**周记**：（周五填）

---

## 4. 每日记录

> 每天收工 15 分钟内填。格式：`日期 | 做了什么 | 产出 | 卡点 | 明天第一件事`

| 日期 | 做了什么 | 产出 | 卡点 | 明天第一件事 |
|---|---|---|---|---|
| 2026-09-15 | ③～⑤（见上）⑥autoDL 环境盘点与搭建：版本锁定、模型盘迁移、下载测速、SDXL 后台下载启动 ⑦建立 `项目进展.md` 纪实文档 | `todolist.md`、`项目进度.md`、`.gitignore`、`README.md`、`LICENSE`、`CONTRIBUTING.md`、`docs/adr/` ×5、`docs/sop/license_matrix.md`、`.env.example`、`.github/` 模板 ×3、**`项目进展.md`**、`deploy/autodl/` ×2 脚本、`versions.lock`(远端) | 无功能阻塞。**踩坑 2 个**：多层 shell 转义导致文件生成失败（已转为脚本化方案）；Flux VAE 官方源 403（待 HF_TOKEN） | ① 拉 SDXL VAE fp16 fix ② P2-04 首次 ComfyUI API 出图验证 ③ 启动 P1 竞品分析 |
| **2026-09-16** | ① 续传完成 FLUX.2 全精度文本编码器（7.4GiB）② 新增 `07_bench_workflow.py` 做 SDXL 性能标定 ③ FLUX.2 冒烟失败 → 逐层读 ComfyUI 源码定位根因 ④ 决策并启动引擎升级 v0.3.75→v0.36.0 ⑤ 新增只读侦察与内存护栏脚本 | `deploy/autodl/06_resume_tenc_download.sh`、`07_bench_workflow.py`、`08_probe_nodes.py`、`09_recon_upgrade.sh`、`10_fetch_and_inspect.sh`、`11_fetch_target.sh`、`workflows/flux2_klein_t2i_v1.json`、`pip_freeze_before_v036.txt` | 🔴 **ComfyUI v0.3.75 物理上跑不了 FLUX.2 klein**（版本早于模型 2 个月，`Flux2.clip_target` 是 `return None # TODO` 的半成品）。升级本身是小风险动作，但它把 R2（环境不可复现）推到台前：33 个自定义节点需逐个复验 | ① 完成 v0.36.0 升级与依赖装齐 ② 切有卡跑通 SDXL+FLUX.2 双冒烟 ③ 重生成 `versions.lock` |
| **2026-09-16（夜）** | ① 读四份文档 + 冻结契约 + memory 定范围 = **P6-10 产物存储与生命周期** ② 实现**受控取图**（改方案：不用预签名 URL，见契约 §2.3）③ 实现**标记采纳 / 收藏** ④ 实现**打包下载 zip** ⑤ 扩展 `GET /assets` 筛选 + `AssetOut` 补 `task_id`/`meta` ⑥ 实现**生命周期清理**（回收站超期 + 产物超期，beat 每 6h）⑦ 补 4 个配置项 ⑧ 核实 P2-12 并打勾 ⑨ 同步四份文档 | `backend/app/api/v1/assets.py`（+3 接口 + 筛选）、`backend/app/worker/maintenance.py`（新）、`backend/app/schemas/asset.py`、`backend/app/services/storage.py`（幂等 `delete`）、`app/worker/celery_app.py`（beat + include）、`.env.example`（+4 项）、`backend/tests/test_assets.py`、`backend/tests/test_maintenance.py`、文档 ×4 | 无功能阻塞。⚠️ **本轮发现两个平台级空洞**（#30 取图接口从未实现、#31 良品率/北极星零触发）—— 两者都不是"少写个功能"，而是**核心 KPI 结构性没有数据源**。⚠️ 遗留 6 项未验证（真实 MinIO / 真实 PG / worker 是否监听 `maintenance` 队列 / ETag 语义无测试钉死 / 浏览器走查 / 慢客户端断连） | ① **P7-05 画廊接入已交付的取图接口**（把图真正显示出来）② 或 **P6-13 内容安全 / P6-08 配额限流**（P6 两个 P0 缺口）|

| **2026-09-17（凌晨）** | ① 夜间收口会话（承接 09-16 夜多个并行AI Agent 的产出，先复跑基线再动手）② 补一条 **bypass 分支的模型元数据回归测试**：把 `extract_models(rendered.workflow)` 的注释承诺变成**机器可校验的判据**（此前变异测试证明**没有任何测试**能检出错取 `workflow.definition`；现构造含 `_meta.switches` 且被裁分支里有 LoRA loader 的工作流，断言 `meta["models"]` 不含该 loader）③ 更正 `web/src/api/types.ts` 已过期的「**没有**模型字段」注释（后端早已写 `meta["models"]`，前端尚未展示）④ 更正 `backend/pyproject.toml` 里 `email-validator` 的**归因反了**（用 `MetaPathFinder` 屏蔽复现：`import app.schemas.user` / `import app.main` **导入期当场抛 `ImportError`**，服务根本起不来）⑤ 弱化 `CONTRIBUTING.md` 的「本地跑通＝CI 跑通」为准确表述 ⑥ 审阅并订正 `SUMMARY.md`（多处过期/不实）⑦ 三份进度文档同步 | `backend/tests/test_worker.py`（+1 条，含变异验证）、`web/src/api/types.ts`、`backend/pyproject.toml`、`CONTRIBUTING.md`、`SUMMARY.md`（不入库）、`todolist.md`、`项目进度.md`、`项目进展.md`、`nightly-20260916.log`（不入库）| 无功能阻塞。⚠️ **GPU 仍无空闲卡（`GPU空闲/总量：0/10`）** → 三条新工作流的 **L4 真实出图**、**蒙版极性判读**、**稳态基准复测** 全部顺延；本轮 GPU 机时 **0**、实例**保持关机**。⚠️ 本轮**未**重读 autoDL 控制台，仅用本机只读探针（`ps`/`ss` 无 ssh 客户端与连接；TCP 探测可达 ≠ 开机）佐证"无 GPU 工作在跑" | ① 等空闲卡：跑 `deploy/autodl/36_l4_three_workflows.sh` + `37_probe_inpaint_mask_polarity.py`（**两者至今从未执行过**）② 无卡时推进 **P6-08 配额限流 / P6-13 内容安全** ③ 启动 **P8 质量体系**（golden set / rubric / 良品率看板）|
| **2026-09-17（凌晨·续）** | ① **GPU 侧两次尝试**：读控制台得 `GPU空闲/总量 = 3/10`（**有卡**，旧结论 `0/10` 已过期）→ 开机成功到 `运行中` → **但本机无任何可用 SSH 凭据**（`SSHPASS` 未设置、`~/.ssh` 公钥被拒）→ 三次后关机；第 2 次改为**先验凭据再决定开机**，未通过 ⇒ **未开机**（机时 0）② **修配额并发超发缺陷**（#34）：扣减改**单条条件 UPDATE**（新增 `app/services/quota.py::charge()`）+ **显式回读** `quota_used`（Core 语句绕过 ORM + `expire_on_commit=False`）③ 4 个调用点统一、删除 `_assert_quota` ④ **402 的 `detail` 改对象形式** + 契约 §2.2/§7 ⑤ 前端判据改「`detail.code` 优先、402 状态码后备」⑥ 补 `test_quota.py` **8 条** | `backend/app/services/quota.py`（新）、`backend/app/api/v1/tasks.py`、`backend/tests/test_quota.py`（新，+267 行）、`docs/sop/contracts.md`、`web/src/api/errors.ts`、`web/src/pages/WorkbenchPage.tsx`、`web/src/api/__tests__/http.test.ts`、`deploy/comfyui/evidence/20260917_l4_{boot_cycle,rerun_credential}_blocked.log`（前者入库 `b4348b6`、后者 `2c83487`）、三份进度文档 | ⚠️ **GPU 卡点已从"没卡"变为"没凭据"**：有卡（`3/10`）且能开机，但**连不进机器** ⇒ 一条工作流都没跑，**机时约 2 分 47 秒**（第 1 次白烧）+ 0（第 2 次）。⚠️ **前端全量 vitest 复跑出现 1 条超时抖动**（`WorkflowForm.test.tsx` 的 5s 超时，单文件重跑 11 passed）—— 本机 CPU 争用所致，非回归。⚠️ 配额原子性**只有 SQLite 证据**，PG 未验证 | ① **解决凭据**（从导出了 `SSHPASS` 的 shell 重启 CodeBuddy，或你代跑 `36_*`）—— **唯一最高优先阻塞项** ② 无卡时：**P6-13 内容安全** + P6-08 剩下的限流 + 真 PG 上验条件 UPDATE ③ **P8 质量体系启动** |
| **2026-09-17（P6-08 配额 / P6-13 内容安全 / P8 质量体系，三批离线收口）** | ① **P6-13 内容安全（输入侧）**：敏感词/合规词过滤 + 开关 + 留痕 + 422 `CONTENT_BLOCKED`；词表 `content_blocklist.json`（5 规则 / 38 词条，硬拒只放法律红线）② **P6-13 的 NSFW 视觉检测 V1 未实现**：`NullNsfwDetector` **永返 `unknown`、不返 `safe`**（有测试钉死，刻意不造假）③ **P8-02 评分标准**（4 维度 × 1-5，判废词不另造、**刻意不加权**）④ **P8-06 缺陷知识库 44 条**（六类全覆盖；`verified_by` = `l3 33 / doc 10 / none 1 / gpu 0`）⑤ **P8-01 golden set 42 例**（`expected` 全 `null`）⑥ **两个机械校验器**（`check_defect_kb.py` / `check_golden_set.py`）+ 4 次变异验证 ⑦ **P8-04 抽检 SOP / P8-09 可复现性报告 / P8-10 测试计划 + 用例索引 + 23 条边界用例** ⑧ 更正 `PRD_v1.md` 把 Must 级 FR-9.4 标 ✅ 的失实、`system_flow.md` 合规表「待实现」的过期记载 ⑨ 三份进度文档同步 | `backend/app/services/content_safety.py`（新）、`backend/app/data/content_blocklist.json`（新）、`backend/tests/test_content_safety.py`（新，728 行）、`docs/sop/quality_rubric.md`（新）、`docs/sop/defect_kb.md`（新，44 条）、`docs/sop/quality_sampling_sop.md`（新）、`docs/review/reproducibility_report.md`（新）、`docs/review/test_plan.md`（新）、`tests/README.md`（新）、`tests/golden_set/`（42 例 + README）、`engine/tools/check_defect_kb.py`（新）、`engine/tools/check_golden_set.py`（新）、`engine/tests/test_quality_tools.py`（新，584 行）、`backend/tests/test_boundary_values.py`（新，23 条）、`docs/prd/PRD_v1.md`、`docs/flow/system_flow.md`、三份进度文档 | ⚠️ **P8 十项一次真实评测都没跑过**（无 GPU、无凭据）⇒ 任何「通过率 / 得分 / 良品率 / 覆盖率」数字**一律不存在**；P8-09 的「覆盖 2/5」是**归档此前 GPU 窗口的结果**，不是本批跑出的。⚠️ **P6-08 的 4 项口径待拍板**（重试二次扣额 / 失败退额 / `queue_max_depth` 死配置 / 每用户在途任务数上限）。⚠️ 三份文档的**文件仍在被并行代理改写**，本次同步只提交自己独占的部分 | ① **给 P6-08 的 4 项口径拍板**（纯决策，不需 GPU）② 补两个**纯离线就能补**的 AC 用例：`AC-F6`（模板库）、`AC-N5`（接口全部鉴权）③ **真实 PG 上验一次条件 UPDATE 的并发行为**（#34）④ 继续 P8 离线侧（P8-05 看板 / P8-07 对比工具）；⚠️ GPU 侧三件仍待凭据 |
| **2026-09-17（P6-08 收口 / 四个缺陷修复 / ADR-008 / 跨目录口径清理）** | ① **P6-08 三件套落地**：配额扣减改**单条原子条件 UPDATE**（`app/services/quota.py`，4 个调用点统一、删 `_assert_quota`、**显式回读**）、**EX-5 队列深度闸门**（软阈值告警仍接单 / 硬阈值 2× 熔断 422 `queue_full`，四条提交与重试路径全覆盖）、**每用户在途任务数上限 1000**（422 `TOO_MANY_IN_FLIGHT`，整批判定，只加两条提交路径）② **四个缺陷修复**：`alembic/env.py` 的 `fileConfig` 不再禁用已有 logger（此前让 `caplog` 静默失效）· `params` 文本字段**只查上界**（`TEXT_LENGTH_OUT_OF_RANGE`；下界在 Schema 无标记可依）· `retry` / `retry-failed` 补全局队列闸门 · **补 `AC-F6`（模板库）与 `AC-N5`（鉴权/端口）用例**，并顺带修 `conftest.py` 拆卸时外键环导致的 `drop_all` 顺序不合法 ③ **ADR-008 三条范围裁定**（风格迁移顺延 V2 / V1 不做 NSFW 视觉检测 / 不引图像重依赖）④ **清掉跨目录同源口径不一致**：「6 条工作流」最后 3 处 + 给 ADR-0005 加勘误注 ⑤ 三份进度文档同步 | `backend/app/services/quota.py`（已有，本轮补测试与文档）、`backend/app/api/v1/tasks.py`（两道闸门 + 文本长度校验）、`backend/tests/test_queue_guard.py`（新，**14 条**）、`backend/tests/test_in_flight_limit.py`（新，**10 条**）、`backend/tests/test_alembic_logging.py`（新，2 条）、`backend/tests/test_catalog_templates.py`（新，10 条）、`backend/tests/test_auth_coverage.py`（新，36 条）、`backend/tests/conftest.py`、`backend/alembic/env.py`、`docs/adr/0008-v1-scope-reduction.md`（新）、`docs/adr/0005-base-model-selection.md`（勘误注）、`engine/model_registry.yaml` / `workflows/registry.yaml`（**仅注释/字符串**）、`docs/prd/PRD_v1.md`、`docs/sop/contracts.md`、`tests/README.md`、`docs/review/test_plan.md`、`docs/flow/system_flow.md`、三份进度文档 | 无功能阻塞。⚠️ **未验证**：真实 PG 上的并发行为（条件 UPDATE 与两道闸门都只在 SQLite 上验过）· 生成分辨率的 `params` 通道缺口 · `task_max_retries_hard_limit` 无消费者 · 真实 MinIO / Redis · `maintenance` 队列是否被 worker 监听 · CI 从未运行 · 浏览器走查 · **三条工作流 L4 从未执行**。⚠️ **GPU 机时 0**，实例全程关机（卡点仍是 SSH 凭据）| ① **解决凭据**（唯一最高优先阻塞项）→ 跑 GPU 侧三件 ② **两个待拍板的取舍**：生成分辨率缺口是否修、`task_max_retries_hard_limit` 是否补消费者 ③ 真实 PG 上验条件 UPDATE 与闸门的并发行为 |
| **2026-09-17（15:40~15:55 · 三条新工作流的渲染路径 L4 首次跑通 + 蒙版极性结案 + registry 回填）** | ① **凭据就位**（写入 `~/.autodl_sshpass`，600 / 12 字节；用完 `shred -u` 删除并复核 `CONFIRMED_DELETED`）② **首见实例已在「运行中」**（本次**未执行开机动作**，谁启动未知）⇒ 开机前读数 `GPU空闲/总量` = **`2/10`**，关机后复核 **`3/10`** ③ **跑通三条新工作流的渲染路径 L4**（`deploy/autodl/36_l4_three_workflows.sh`，先修脚本两个 bug：`--launch-args` 必须用**等号**形式否则 argparse 报错；退出码 2 的原因列逐级回退 `fatal→error:→usage:→日志末行`）④ **inpaint 蒙版极性专项**（`37_*`，只给客观信号，语义由人判读）⑤ **稳态基准复测**（`36_bench_recheck.log`）⑥ **registry 回填三条**（`render_path_l4: false → true`，`status`/`verification` **不动**）⑦ 三份进度文档同步 + `SUMMARY.md` | `deploy/autodl/36_l4_three_workflows.sh`（修 2 bug）、`deploy/comfyui/evidence/`（**13 个证据文件**：`20260917_l4_fourth_attempt_passed.log` / `36_l4_{i2i,inpaint,upscale}_v1.log` / `36_bench_recheck.log` / `36_comfyui_effective_switches.txt` / `36_inpaint_mask_polarity.json` / `36_inpaint_polarity.log` / `36_inpaint_polarity_source.txt` 等）、`workflows/registry.yaml`、三份进度文档 | 无功能阻塞。⚠️ **L4 通过 ≠ 可放行**：三条登记牌**仍 `disabled` + `pending_gpu`**（缺 per-workflow baseline + `verification` 枚举缺口）。🔴 **`inpaint_v1` 本次 L4 是 no-op**（参考图无 alpha ⇒ 全零 mask ⇒ **产出与输入逐像素相同**）⇒ **不能**说「局部重绘可用」。⚠️ **两处已入库失实只登记未修**（`37_probe_*.py:398` 的错文案、`engine/validate.py:917` 的过期提示）。⚠️ 实例**已关机**（两遍读数交叉验证），本轮持有运行态**约 15 分 20 秒**；**凭据已销毁** ⇒ 下次跑 GPU 要重新就位 | ① **6 项待拍板**（见 §0「下一步」；其中 `inpaint` 无 alpha 静默 no-op 最该先定）② **无卡可做**：真 PG 上验条件 UPDATE 与两道闸门的并发行为 + P8 离线侧 + 修 `validate.py:917` 与 `37_probe:398` 两处文案 ③ 若要补 per-workflow baseline：**先解决凭据 → 再确认有卡 → 才点开机** |
| **2026-09-19** | 上午：① 新增 `scripts/reproduce_env.sh`（CPU 侧环境复现：backend venv + SQLite 初始化/种子 + engine 自检查 + 前端 pnpm 安装与静态检查）② 新增 `docker-compose.yml`（PG 16 + Redis 7 + MinIO 2024-09）③ 新增 `docs/deploy.md`（P11-02 部署文档：中间件一键起、前后端启动、常见坑、生产 checklist）④ `engine/tools/check_defect_kb.py` 与 `check_golden_set.py` 接入 `.github/workflows/ci.yml` 的 `quality-gates` job 与 `scripts/pre-push.sh` 本地可选钩子。午后：⑤ 用户通知 GPU 可用并提供 SSH 密码 → 建立 SSH 连接 → 发现 ComfyUI 未运行，执行 `03_start_comfyui.sh` 启动 → 发现服务端无 `backend/.venv`，改用 `/root/miniconda3/bin/python` ⑥ 跑 `40_repro_workflow.py` 验证 `i2i_v1` / `inpaint_v1` / `upscale_v1` 同 seed 两次出图 IDAT 一致 → **P8-09 覆盖 5/5** ⑦ 下载证据到本机 `deploy/comfyui/evidence/repro_20260919/` ⑧ 实例关机（机时约 10 分钟）⑨ 三份进度文档同步 | `scripts/reproduce_env.sh`、`docker-compose.yml`、`docs/deploy.md`、`.github/workflows/ci.yml`、`scripts/pre-push.sh`、`deploy/comfyui/evidence/repro_20260919/`（含 `repro_results.json` 与 6 张 PNG）| 无功能阻塞。⚠️ **docker-compose 未实测 `up`**（本机无 docker daemon），仅静态校验。⚠️ **P8-09 5/5 只是「可复现性」**，不等于画质 / 不等于工作流可放行。⚠️ **凭据用完即毁**，下次 GPU 仍需重新就位 | ① **P8 画质评审**（人工对 42 例 golden set 打分，≥80% 一次通过才改 `status: enabled`）② **基础设施验证**（真实 PG/Redis/MinIO/CI 首次运行/画廊浏览器走查）③ **P4 控制体系**（ControlNet / IP-Adapter / LoRA 矩阵与权重区间）|

---

## 5. 风险登记

| # | 风险 | 概率 | 影响 | 状态 | 应对措施 | 负责人 | 更新 |
|---|---|---|---|---|---|---|---|
| R1 | 范围蔓延 | 高 | 高 | 🟡 监控中 | 严守 V1「不做清单」，新需求只进池不改范围 | — | 09-15 |
| R2 | 环境不可复现 | 高 | 高 | 🔴 **已触发** | 原措辞「节点只增不删」已失效：升级核心引擎确实会破坏节点兼容性。**改为**：① 保留 tag `v0.3.75` 作回退锚点 ② 升级前后各存一份 `pip freeze` 对比 ③ 建立节点白名单（P2-05），**只保 P3/P4 真正要用的**，其余禁用而不删除 ④ 升级后必须重跑双底模冒烟才算通过 | — | 09-16 |
| R3 | autoDL 实例被抢占/断连 | 中 | 中 | 🟢 低 | 关机不计费 + 快照 + 代码实时推远端 | — | 09-15 |
| R4 | 模型商用许可踩坑 | 中 | 极高 | 🟡 监控中 | 许可矩阵，遇「未知许可」一律替换 | — | 09-15 |
| R5 | 批量必 OOM | 高 | 高 | 🟢 低 | 显存基线 + 加载策略 + 自动降级 | — | 09-15 |
| R6 | 良品率不足 80% | 中 | 高 | 🟢 低 | W6 起评测，留 W7-W8 调优窗口 | — | 09-15 |
| R7 | 前端工作量低估 | 高 | 中 | 🟢 低 | 动态表单优先，必要时砍 P7-09/P7-10 | — | 09-15 |
| R8 | 产品文档流于形式 | 中 | 中 | 🟡 监控中 | 文档作为阶段 DoD，不完不成不进下一阶段 | — | 09-15 |
| R9 | 单人全链路疲劳 | 中 | 中 | 🟢 低 | 每周强制半天复盘，重大决策隔夜再定 | — | 09-15 |
| R10 | 技术选型反复 | 中 | 中 | 🟢 低 | 写 ADR，V1 内不推翻 | — | 09-15 |
| R11 | 内测找不到人 | 中 | 中 | 🟢 低 | W4 开始约人，备选社区互换内测 | — | 09-15 |
| R12 | 开源泄露敏感信息 | 低 | 极高 | 🟡 监控中 | 全仓库扫描 + pre-commit 密钥检查。⚠️ **如实登记：pre-commit 这道门禁目前并不存在**（仓库无 `.pre-commit-config.yaml`），当前只有 pytest 级哨兵 `backend/tests/test_env_example.py` 守住"示例里不得出现像真密钥的东西"（`JWT_SECRET` 必须是 `CHANGE_ME_IN_ENV`）。**待另立任务，或并入 P11-11 脱敏检查** | — | 09-16 |
| R13 | ComfyUI 为 GPL-3.0，私有化交付时触发传染 | 低 | 高 | 🟢 低 | 不 fork、仅 API 调用（ADR-002）；V2 做私有化前重新评估 | — | 09-15 |
| R14 | **清理任务静默不执行**（`maintenance` 队列无人监听） | 中 | 中 | 🟡 监控中 | 仓库里**没有 Celery worker 的启动脚本 / compose / runbook**。若部署时用 `-Q` 排除了 `maintenance`，`cleanup_assets` 会躺在队列里**永不执行** → 存储只增不减，直到磁盘满（EX-4，磁盘满了连出图都做不了），且**不会有任何报错**。应对：① 起 worker 的 `-Q` 必须包含 `maintenance` ② 写进 P2-09 compose 与启动 checklist（P11-02）③ 在 `/health/ready` 或监控里增加"队列有消费者"检查 | — | 09-16 |

| R15 | **GPU 侧验证 —— 本轮已从「悬置」降级为「放行前置未满足」**（原题：卡点从「无空闲卡」变为「无 SSH 凭据」） | 高 | 中 | 🟡 **本轮已缓解（不再悬置，但未消除）** | ✅ **2026-09-17 15:40~15:55 凭据齐备，三条新工作流的渲染路径 L4 实际跑通**（各 **通过 11 · 失败 0 · 跳过 0**）+ 蒙版极性专项 + 稳态基准复测（地板 SDXL **4.54s** / klein **1.52s**，**无回归**），随后**已确认关机**（控制台两遍读数均为「已关机」）。⚠️ **仍未消除的两点**：① **放行前置未满足** —— 三条的 **per-workflow baseline 未补**（要按各自口径跑 N=12 warmup=1），登记牌仍 `disabled` + `pending_gpu`；`inpaint_v1` 另有三条放行前置（无 alpha 退化 / 探针绕过素材管线 / 无输入 alpha 校验）② **凭据用完即毁**（`shred -u ~/.autodl_sshpass`，复核 `CONFIRMED_DELETED`）⇒ **下次跑 GPU 必须重新就位凭据**；控制台上密码是**掩码**的，浏览器技能**禁止**提取 ⇒ 只能用户导出或代跑。历史（保留原文以供对照）：凌晨第 1 次**先开机才发现没凭据**，白烧 **2 分 47 秒**；第 2/3 次改为**先验凭据再决定开机**，凭据不满足 ⇒ **未开机、机时 0**、¥0。应对/纪律：① 顺序永远是 **先验凭据 → 再确认有卡 → 才点开机** ② 无凭据时只推进不依赖 GPU 的任务 ③ 需 GPU 时**提前约 10 分钟预警**（切换必须先关机）④ ⚠️ **不要把「L4 跑通」读成「可以放行」**（见 #51）| — | 09-17 |

| R16 | **内容安全只覆盖「输入侧可枚举文本」，无任何视觉检测** | 中 | 极高 | 🟡 监控中 | P6-13 交付的是**输入侧**敏感词/合规词过滤（5 规则 / 38 词条，硬拒只放法律红线）；`NullNsfwDetector` **永远返回 `unknown`、永不返回 `safe`** —— 这是**刻意不造假**（一个返回"安全"的空检测器会让下游把"未检测"当成"已检测且通过"，从而让风险隐形）。⇒ **违规画面（含 NSFW / 侵权实拍 / 伪造标识）系统当前拦截不了**。三条已知边界：① 词表是**窄而可解释**而非完备 —— 绕写（拆字 / 拼音 / 谐音 / 插引号）覆盖不到 ② `warn` 级仍有已知误报（`血腥玛丽`）③ 图片内容完全不覆盖。应对：① 短期靠**人工核查**（`license_matrix.md` §6 指出的手段）② 中期需**先立决策**是否引入图像分类依赖（当前项目纪律是「避免重依赖」）③ 产物侧拦截（R-33）归 V2 / P8-03 ④ **上线前不得声称"NSFW 过滤已上线"** ⑤ ✅ **V1 口径已由 `docs/adr/0008-v1-scope-reduction.md` 裁定 2 写死**：V1 **只做输入侧文本过滤 + 人工抽检**，NSFW 视觉检测**顺延 V2**（走「构建期下载离线小分类器」，**引入前必须先核权重许可**并登记进 `engine/model_registry.yaml`）⑥ 🔒 **在 V2 引入真检测器之前，禁止把 `NullNsfwDetector.detect()` 改成返回 `safe`** | — | 09-17 |

状态：🟢 低 · 🟡 监控中 · 🔴 已发生

| R17 | 🔴 **`inpaint_v1` 在「输入无 alpha」时静默 no-op** —— 用户传普通 JPEG ⇒ 引擎收到无 alpha 图 ⇒ `LoadImage` 返回**全零 mask** ⇒ 非选区贴回覆盖整张图 ⇒ **产出与输入逐像素相同，而任务报「成功」** | 高（**必然发生**：普通 JPEG 是最常见输入） | 高（**用户拿到的是"成功"的废图**，量产场景下会静默放大成整批废品，比报错更危险） | 🟡 **已识别 · 未处置** | **本轮实测坐实**（#49）：`inpaint_v1` 的 L4 **11/11 全绿却是一次 no-op**。「成功但没干活」对量产平台**比报错更危险** —— 报错会有人管，静默成功不会。三条同名前置：① 本次 L4 因参考图无 alpha 而退化 ② 极性探针**绕过了平台素材管线**（未走 `/upload/image` / `asset_resolver`）⇒ 证明不了「用户上传的带透明区图能活到 ComfyUI」 ③ `engine/` 与后端**都没有校验 inpaint 输入必须含 alpha**。应对（**待拍板**）：① 在某一层补校验并明确报错（上传 / 提交 / 渲染期三选一）② 或明确裁定「由前端保证素材含 alpha」并写进契约；**在裁定前不得声称"局部重绘可用"** | — | 09-17 |

---

## 6. 决策记录（ADR）

完整记录见 [`docs/adr/`](./docs/adr/)。

| # | 日期 | 决策 | 备选方案 | 理由 | 状态 |
|---|---|---|---|---|---|
| [ADR-001](./docs/adr/0001-delivery-form-and-scope.md) | 2026-09-15 | 交付形态：Web 平台（前端 + API + 异步队列），ComfyUI 作底层引擎；首发场景电商视觉 | 纯 API / 工作流包 / 桌面端；IP形象 / 全场景平铺 / 视频 | 最贴近商用、最能完整体现产品与工程能力；电商需求最标准化、最易量化良品率 | 已定 |
| [ADR-002](./docs/adr/0002-open-source-and-license.md) | 2026-09-15 | 自研代码 MIT；不 fork ComfyUI，仅 API 调用；模型权重不入库 | Apache-2.0 / GPL-3.0；开源核心+闭源平台 | MIT 采用门槛最低利于作品集传播；规避 GPL 传染；模型许可必须隔离 | 已定 |
| [ADR-003](./docs/adr/0003-v1-timeline-and-pace.md) | 2026-09-15 | V1 周期 10 周（09-14 → 11-22），全职投入，以周为粒度 | 16-20 周兼职 / 6 个月+ | 按用户投入节奏排期；兼职易因上下文切换烂尾 | 已定 |
| [ADR-004](./docs/adr/0004-tech-stack.md) | 2026-09-15 | React+TS+antd / FastAPI / Celery+Redis / PostgreSQL / MinIO / Prometheus | Vue3+Element、Django、RQ、云 OSS | 详见 ADR-004；核心约束是 ComfyUI 与主服务进程隔离 | 已定（前端框架 P2-10 前可调整） |
| [ADR-005](./docs/adr/0005-base-model-selection.md) | 2026-09-15 | 底模选型：SDXL 主 + FLUX.2 [klein] 4B 辅；明确排除 FLUX.1-dev / FLUX.2-dev / 9B | 只用 SDXL / 只用 klein / 用 FLUX.1 schnell | 三关筛选：商用许可 × 可下载性 × 生态成熟度。SDXL 打「可控」，klein 打「一致性与效率」 | 已定 |
| ADR-006 | — | 放大与 tile 分块策略 | — | 待 P5-05 后决定 | 待定 |
| ADR-007 | — | 部署形态（compose vs k8s） | — | 待 P11-02 后决定 | 待定 |
| [ADR-008](./docs/adr/0008-v1-scope-reduction.md) | 2026-09-17 | **V1 范围收缩三条裁定**：① `style_transfer_v1`（P3-07）**顺延 V2** —— V1 **5 条上线**，`AC-F1` 口径随之修正 ② V1 **不做 NSFW 视觉检测**，只做**输入侧文本过滤 + 人工抽检**（V2 走「构建期下载的离线小分类器」，引入前必须先核权重许可）③ V1 **不引入 numpy / Pillow 等图像重依赖**（将来用 optional extra，不进必装依赖；`P8-03` 拆成 `03a` / `03b`） | ① 下载三类权重（IP-Adapter / CLIP-Vision / LoRA）并过许可审查 → 保住「6 条」口径 ② 云内容安全 API 做视觉检测 ③ 把 Pillow 加进后端**必装**依赖 | ① CLIP-Vision 编码器许可**未核实**（`license_matrix.md:171` 标 `unknown`）而铁律是「未知即不用」；IP-Adapter 是 P4 控制体系的正主而 P4 目前 **0/10**；第 6 条在注册表里只是**没有定义文件的占位条目** ⇒ 「6 条全部上线」在 V1 **不可能达成** ② 云 API 与「运行时不访问外网」「不上传商品图内容」两条硬约束**直接冲突** ③ 阈值标定必须用真实产物，而 golden set 的 `expected` **全为 `null`** ⇒ 现在做只会得到一个**「阈值未标定」的空壳**。⚠️ 三条都选了「**不做**」而非「做出一个空壳」，代价已逐条写明 | 已定 |

> 决策一旦写入，V1 内不推翻。若要推翻，新增 ADR 说明代价并记录。

---

## 7. 数据看板（上线后启用）

### 7.1 北极星指标

**周有效交付图片数**（生成且被下载/采纳）

| 周 | 数值 | 环比 | 备注 |
|---|---|---|---|
| W9 | — | — | 内测开始 |
| W10 | — | — | V1 发布 |

### 7.2 一级指标

| 指标 | 目标 | W9 | W10 | 说明 |
|---|---|---|---|---|
| 任务成功率 | ≥98% | — | — | 排除用户取消 |
| 良品率 | ≥80% | — | — | golden set + 抽样 |
| P95 出图时长 | 按工作流设定 | — | — | 分位数 |
| 单张成本（元） | 建基线 | — | — | GPU 秒折算 |
| WAU | 建基线 | — | — | — |
| 7 日留存 | ≥30% | — | — | — |

### 7.3 核心漏斗

```
访问 → 注册 → 上传 → 选模板 → 调参 → 提交 → 查看 → 下载 → 复购
 ⬜      ⬜      ⬜      ⬜       ⬜      ⬜      ⬜      ⬜      ⬜
```
上线后逐环填转化率，优先优化最低的两环。

### 7.4 失败原因分布

| 原因 | 占比 | 趋势 |
|---|---|---|
| OOM | — | — |
| 超时 | — | — |
| 参数非法 | — | — |
| 引擎崩溃 | — | — |
| 用户取消 | — | — |

---

## 8. 复盘模板（阶段结束必填）

```markdown
## {阶段} 复盘 · {日期}

### 1. 目标与结果
- 原定目标：
- 实际结果：
- 差距：

### 2. 做得好（继续保持）
- 
### 3. 做得不好（下次改进）
- 
### 4. 数据发现
- 
### 5. 关键决策与代价
- 
### 6. 下一步（写进 todolist）
- 
```

---

## 9. 更新规范

1. **每天收工**：填「每日记录」，改「一屏速览」的当前阶段与阻塞。
2. **每周五**：填「周记」，更新「阶段进度汇总」百分比，更新风险状态。
3. **任务完成**：在 `todolist.md` 打勾，同时在本文件对应阶段更新完成数。
4. **发生阻塞**：立即在「一屏速览」标注，并在风险登记新增条目。
5. **重大决策**：写入 ADR，不同意的备选方案也要记录，方便日后回看。
6. **计划变更**：在本文件末尾「变更记录」登记，不静默改计划。

---

## 10. 变更记录

| 日期 | 变更 | 原因 | 影响 |
|---|---|---|---|
| 2026-09-15 | 建立进度基线，13 个阶段 / 8 个里程碑 | 项目启动 | — |
| 2026-09-15 | git 仓库初始化（main 分支）+ .gitignore，完成首次提交 | 保存进度、防模型/密钥误提交 | 模型与产物不入库，仅提交 registry 清单 |
| 2026-09-15 | 新增里程碑 M0.5（仓库就绪）与风险 R13（ComfyUI GPL-3.0） | 细化 P0 交付与开源合规边界 | — |
| 2026-09-15 | 新增第三份文档 `项目进展.md`（纪实 + 问题处置台账） | 计划与基线之外，需要记录「实际发生了什么、问题怎么解的」 | 三份文档分工：todolist=计划，进度=度量，进展=纪实 |
| 2026-09-15 | P2-03（基础镜像 Dockerfile）推迟到 P11-02 | autoDL 自带 CUDA 12.4 + torch 2.5.1，先用现有环境跑主线 | 避免前期把时间花在环境美化上 |
| 2026-09-15 | 新增工程量纪律：远端操作一律「本地写脚本 → scp → 远端执行」 | 多层 shell 转义事故（详见 `项目进展.md` #3） | 同时提升可复现性与可审计性 |
| **2026-09-16** | **推翻 09-15 的「复用现有 ComfyUI v0.3.75」决策**，升级到 v0.36.0 | 该决策的前提「v0.3.75 版本够新」被证伪：它早于 FLUX.2 klein 发布 2 个月，且本版 flux2 实现是 `return None # TODO` 的半成品（详见 `项目进展.md` #14） | 需重装依赖、复验 33 个自定义节点；P2-06 版本锁与 P2-04 冒烟结论都要重做 |
| 2026-09-16 | 性能指标口径修正：NFR-1 单张出图 12.0s → **稳态 4.6s / P95 5.07s** | 原 12.0s 是冷启动值（含模型加载），混入稳态统计会高估 2.6 倍；已新增 `07_bench_workflow.py` 固定「warmup 不计入统计」的口径 | 成本模型与吞吐预估需按新口径重算 |
| **2026-09-16 晚** | **P2-10 前端骨架落地**（React 18 + TS + Vite 5 + antd 5 + pnpm），并**顺带解掉两个后端硬阻塞**：`registry.yaml`→`workflows` 表**从来没有装载器**（`GET /workflows` 恒为空）、app 无法在 SQLite 上启动（编译适配只存在于 `conftest.py`） | 「下一步 3 个动作 ①」的执行；两个阻塞都是**修 P2-10 时才暴露**的 —— 不修则 P2 的 DoD「三件套互相调通一次」根本无法成立（详见 `项目进展.md` #28） | P2 由 7/13 → **8/13**；新增 P7 2/12；后端测试 215 → **256**；新增前端测试 72 条 |
| **2026-09-16 晚** | **修正总进度分母与阶段计数**：§2 汇总表与「一屏速览」自相矛盾（P2/P5），且分母原写「约 131」实为 **153** | 同一件事在两个地方各算一遍必然对不上（与 #21/#25 同类）。总进度 41% → **35%**，属**分母修正而非进度倒退** | 后续所有百分比以 §2 表求和为准 |
| 2026-09-16 | GPU 使用策略细化：**「慢而便宜」的工作放无卡，「需 GPU 的验证」攒到最后一次性切** | 无卡模式 0.5 核/2GB 只影响速度不影响正确性；而切模式必须先关机，频繁切换既费时又可能没卡 | 减少开关机次数，降低 R3 风险 |
| **2026-09-16 夜** | **P6-10 收口**（受控取图 / 标记采纳 / 打包下载 / 生命周期清理）；取图方案由「MinIO 签名 URL」**有意改用受控代理接口**；**P2-12 核实后完成**（`.env.example` ↔ `Settings` 68↔68，双向漂移门禁）；更正 P7-05「依赖未实现接口」的过期表述 | ① P6-10 的原始措辞在 V1 部署形态下不可用（SSH 隧道下浏览器到不了 MinIO）② P2-12 的原记载「30 个变量全部对不上」**已被门禁消除**，属过期记载必须修正（否则与节点 49 自相矛盾） | P6 6→**7**、P2 8→**9**；完成数 54→**56**；总进度 35%→**37%**（⚠️ **分母仍为 153，未变**）；新增风险 **R14**（清理任务静默不执行）；后端测试 256→**307** |
| **2026-09-17 凌晨·续** | **修掉配额并发超发缺陷**（#34）：扣减由 Python「读-改-写」改为**单条条件 UPDATE**（新增 `app/services/quota.py::charge()`，4 个调用点统一，删除 `_assert_quota`）+ **显式回读** `quota_used`；**402 的 `detail` 改对象形式**（契约 §2.2 / §7 同步）；补 `backend/tests/test_quota.py` **8 条**（此前配额**零覆盖**）。⚠️ 同批记录两次 GPU 尝试（**有卡 `3/10`、开机成功但无 SSH 凭据**，机时 **约 2 分 47 秒**；第 2 次先验凭据故 **0**） | ① `get_db()` 每请求一个 session + 同步路由跑线程池 ⇒ `quota_used += N` **丢更新、额度被超发**；全仓库**零条件 UPDATE**，该模式根本不存在 ② `FOR UPDATE` **不可用**（SQLite 不支持，而本地开发与全部单测跑 SQLite）③ 402 原为纯字符串，与契约 §2.2 自己的规定冲突，前端只能按状态码判断 ④ GPU 卡点由「没卡」变为「没凭据」，`0/10` 的旧结论已过期 | **P6-08 更新为「部分完成」但完成数不变（仍 58/153）** —— 标题含「限流」，而限流一行代码都没写；配额部分也只是**修缺陷**而非新能力。新增未验证项 **⑪（PG 上的条件 UPDATE 原子性）**；后端测试 355→**363**、前端 **80→82**；⚠️ **重试二次扣额 / 失败退额 / 限流口径仍待拍板** |
| **2026-09-17（P6-08 收口 / 四个缺陷修复 / ADR-008）** | **① P6-08 标为完成**：配额原子化（条件 UPDATE + 显式回读 + 结构化 402）+ **EX-5 队列深度闸门**（软阈值告警仍接单 / 硬阈值 2× 熔断 `queue_full`）+ **每用户在途任务数上限 1000**（`TOO_MANY_IN_FLIGHT`）；**「按接口限流」经裁定 V1 不做**（需求无 QPS / 频次 / 窗口条款，GPU 已全局串行化）② **两项计额口径裁定**：`retry-failed` 保持按新一次生成计额；提交时预扣、终态失败 / 取消不退还（🔶 协调者裁定，可被推翻；已落表 FR-1.2，V2 补偿机制仅登记）③ **四个缺陷修复**：`alembic/env.py` 的 `fileConfig` 不再禁用已有 logger · `params` 文本字段只查上界（`TEXT_LENGTH_OUT_OF_RANGE`）· `retry` / `retry-failed` 补全局队列闸门 · 补 `AC-F6` / `AC-N5` 用例并顺带修 `conftest.py` 拆卸缺陷 ④ **ADR-008 三条范围裁定**（风格迁移顺延 V2 / V1 不做 NSFW 视觉检测 / 不引图像重依赖）⑤ 新增风险 **R16** 的 V1 口径补充；**清掉跨目录同源口径不一致**（「6 条工作流」最后 3 处 + ADR-0005 勘误注）| ① P6-08 的「限流」半边**不能再以"没做"结案** —— 要么补、要么明确不做；裁定结论是**不做**（依据：需求无相关条款 + GPU 已全局串行化），而**公平性**那一半（单用户独占队列）改由「每用户在途上限」承担 ② **测试基础设施本身会制造假证据**（`fileConfig` 禁用 logger ⇒ `caplog` 静默拿到空列表；`conftest` 的外键环拆卸缺陷被 `templates` 零覆盖掩盖）⇒ 必须把"测量工具"也纳入验证范围 ③ ADR-008 已把「6 条」定为不可达成，**口径必须以「5 条 + 1 条顺延」为准**，故清理最后几处旧计数并给历史 ADR 加勘误注 | **P6 7/13 → 8/13**；完成数 **61 → 62**；总进度 **40% → 41%**（62 ÷ 153 = 40.52%）；⚠️ **分母仍为 153，未变**；**P8 仍 3/10**；后端测试 **469 → 547**、引擎 **169 / 1 skipped**（两 CWD 一致）、`VALIDATE=PASS`；新增未验证项 **⑯⑰⑱⑲**；台账新增 **#42~#47**；⚠️ **未 push**（仓库无远端），**GPU 机时 0**、实例保持关机 |
| **2026-09-19** | **P2/P8/P11 无 GPU 交付 + P8-09 5/5 可复现性补齐**：① `scripts/reproduce_env.sh`（CPU 侧一键环境复现）② `docker-compose.yml`（PG + Redis + MinIO）③ `docs/deploy.md`（P11-02 部署文档）④ `.github/workflows/ci.yml` 新增 `quality-gates` job + `scripts/pre-push.sh` 本地可选钩子（P8-08a）⑤ 用户开机并提供 SSH 密码 → 跑 `40_repro_workflow.py` 验证 `i2i_v1` / `inpaint_v1` / `upscale_v1` 同 seed 两次出图 IDAT 一致 → P8-09 **5/5**；证据下载到本机后关机（机时约 10 分钟）| ① P2 的 DoD「环境可一键重建」已有 CPU 侧脚本；中间件部署文档与 compose 补齐 ② P8-08a 让离线质量门禁从"有工具"变成"有机制" ③ P8-09 从 2/5 补齐到 5/5，且是**真实 GPU 验证**；但**画质评审仍未做**，工作流 `status` 仍 disabled | **P2 10 → 12**；**P8 3 → 5**；**P11 0 → 1**；完成数 **62 → 66**；总进度 **41% → 43%**（66 ÷ 153 = 43.14%）；⚠️ **分母仍为 153，未变**；GPU 机时约 **10 分钟**；实例已关机 |
