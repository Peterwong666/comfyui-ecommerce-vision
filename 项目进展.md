# 项目进展纪实与问题处置（项目进展.md）

> **本文件与另外两份的区别**
> - `todolist.md` —— 计划：要做哪些事（未来）
> - `项目进度.md` —— 基线：里程碑在哪、偏差多少（度量）
> - **`项目进展.md` —— 纪实：实际发生了什么、遇到什么问题、怎么分析、怎么解决的**（过去）
>
> 更新规则：**每完成一个动作、每解决一个障碍，立刻登记。** 解决问题的过程本身就是这个项目的核心资产 ——「商用级」的证据往往藏在问题处置里，而不在功能列表里。

---

## 0. 速览

| 项 | 状态 |
|---|---|
| **最新进展** | ✅ **引擎升级完成，待 GPU 验证**：ComfyUI **v0.3.75 → v0.36.0**、torch **2.5.1+cu124 → 2.13.0+cu130**、节点 **2102 → 2530**；已确认 klein 路由打通（`KleinTokenizer`）；两个工作流静态校验 PASS。三个失败节点：nunchaku / TeaCache / smZNodes（详见 #16） |
| **已完成阶段** | ✅ P0 全部（8 项）· ✅ P2-04 ComfyUI 部署 · ✅ P2-06 版本锁定（**待按新版本重做**）· ✅ P2-05 节点审计（部分） · 🔵 P2 其余进行中 |
| **当前阻塞** | 🟡 仅剩「切回有卡跑 GPU 验证」这一步（用户操作）。其余全部就绪 |
| **待解决** | 3 项：① nunchaku/TeaCache/smZNodes 兼容性 ② P1-08 用户访谈 ③ P1-17 高保真原型 |
| **今日新增工程纪律** | ① 引入新模型前**先探测节点 + 读源码确认路由**，再下权重（#14）② 性能数字**必须带口径**，首个样本一律剔除（#15）③ **判据应是文档声明的最低版本，不是 `requirements.txt`**（#16）④ 大版本升级后**必须用 `--cpu` 模式做节点审计**（#16）⑤ **有官方模板不要自己拼工作流**（#16）⑥ 「慢而便宜」的工作放无卡，「需 GPU 的验证」攒到最后一次性切机 |

---

## 1. 进展时间线

### 2026-09-15

| 时间 | 事件 | 产出 |
|---|---|---|
| 上午 | 确认四项前提（交付形态 / 首发场景 / 投入节奏 / 仓库定位） | ADR-001、ADR-003 |
| 上午 | 编制执行计划与进度基线 | `todolist.md`、`项目进度.md`（13 阶段 / 8 里程碑 / 约 131 项任务） |
| 上午 | git 初始化 + `.gitignore` + 首次提交 | commit `7778ee5` |
| 下午 | **P0 全部完成**：目录骨架、README、LICENSE(MIT)、贡献规范、PR/Issue 模板、ADR-001~004、模型商用许可矩阵、`.env.example` | commit `d5474df` |
| 下午 | 用户提供 autoDL 实例 SSH 访问 | — |
| 下午 | 环境盘点：4090 24G / 128 核 / 1TB RAM / ComfyUI v0.3.75 / 31 节点 / torch 2.5.1+cu124 | — |
| 下午 | 决策：复用现有 ComfyUI、模型迁数据盘、同时下 SDXL + Flux | — |
| 下午 | **P2-06 版本锁定**：生成 `versions.lock`（398 行） | `/root/autodl-tmp/comfyui-data/versions.lock` |
| 下午 | **P2-02 模型盘迁移**：models/output/input → 数据盘 + 软链 | 数据盘可用 49G |
| 下午 | 下载测速：HF 直连不通，hf-mirror 3MB/s | — |
| 下午 | **SDXL 1.0 后台下载启动**（6.9GB） | `models/checkpoints/sd_xl_base_1.0.safetensors` |
| 下午 | 建立本文档 | `项目进展.md` |
| 傍晚 | SDXL + VAE fp16 fix 下载完成；ComfyUI 启动成功（v0.3.75 / 24.8G 显存空闲） | — |
| 傍晚 | 验证软链生效：ComfyUI 能同时看到 `sd_xl_base_1.0.safetensors` 与 `sdxl_vae_fp16fix.safetensors` | 证明 #4 的数据盘迁移方案可行 |
| 傍晚 | **P2-04 冒烟测试通过**：文生图 12.0s，产物 1085KB | `deploy/autodl/04_api_smoke_test.py` |
| 傍晚 | 探索 FLUX.2：确认 **klein 4B 为 Apache-2.0 且未 gated，仓库自带 VAE** → 替换原 Flux.1 方案 | 见 #7 与 #9 |
| 傍晚 | 启动 FLUX.2 [klein] 4B 三组件下载（unet / vae / text encoder fp4） | `deploy/autodl/05_download_flux2_klein.sh` |
| 晚上 | **切换无卡模式**（¥0.1/h）续传下载；确认数据保留、cgroup 限制为 2GB/0.5 核 | `docs/sop/gpu_cost_strategy.md` |
| 晚上 | 修正误判：aria2c `-k` 预分配导致文件大小虚高，实际未下完 | 见 #10 |
| 晚上 | 复查下载状态时**误判模型目录为空**（实际在 `comfyui-data/models/`），一度有重下风险 | 见 #11 |
| 晚上 | **P1-04/05/06 完稿**：MRD + 竞品分析 + 用户画像/旅程 | `docs/mrd/`、`docs/persona/` |
| 晚上 | **P1-07 需求池**：45 条需求 + RICE 打分 + 6 项「前提型需求」 | `docs/prd/requirement_pool.md` |
| 晚上 | **P1-09~13 PRD v1.0 成稿**：44 FR + 7 类 NFR + 8 状态机 + 14 异常 + 19 AC + 12 项自评审 | `docs/prd/PRD_v1.md` |
| 晚上 | **P1-14/15 流程文档**：用户流程图（2 路径 + 11 异常分支）、系统流程图（拓扑/时序/数据流/状态恢复） | `docs/flow/` |
| 晚上 | **P1-16 线框图**：7 个核心页面低保真 | `docs/prototype/wireframes.md` |
| 晚上 | **P1-18 项目管理图**：WBS 12 分支/146 任务 + 甘特图 + 关键路径分析 + 缓冲设置 | `docs/prd/gantt.md` |
| 晚上 | **P1-19 资源预算**：人力/GPU/存储/软件 + 三种成本情景（GPU 单价待核实） | `docs/prd/resource.md` |
| 晚上 | **P1-20 埋点方案**：12 事件 + 漏斗 + 9 指标口径 + 表结构 + 自测清单 | `docs/prd/tracking_plan.md` |
| 晚上 | 修正 todolist.md P1 段落编辑失败（旧串过期） | 见 #12 |
| 晚上 | 建立「需要 GPU 时通知用户」机制 + 后台监听下载完成 | `deploy/autodl/watch_flux2_download.sh` |

### 2026-09-16

| 时间 | 事件 | 产出 |
|---|---|---|
| 上午 | 用户开 GPU；核查发现**全精度文本编码器只下到 29%**（09-15 22:04 关机时被中断，aria2c 进程被杀，`.aria2` 控制文件保留） | — |
| 上午 | 新增幂等续传脚本并启动；实测峰值 **17 MiB/s**（比之前的 3.7 MiB/s 快 4.6 倍） | `deploy/autodl/06_resume_tenc_download.sh` |
| 上午 | 启动 ComfyUI（4090 可用，会话后释放） | `deploy/autodl/03_start_comfyui.sh` |
| 上午 | 新增通用基准脚本，标定 SDXL **P95 = 5.07s**；发现历史 12.0s 是冷启动值 | `deploy/autodl/07_bench_workflow.py` |
| 上午 | 新增节点探测脚本，替代"盲跑工作流" | `deploy/autodl/08_probe_nodes.py` |
| 上午 | 文本编码器下载完成（7.4GiB，09:15:22） | — |
| 上午 | **FLUX.2 冒烟失败**（`KeyError: 't5xxl'`）→ 逐层读源码定位根因：引擎版本早于模型发布 | 见 #14 |
| 上午 | 决策：升级 ComfyUI **v0.3.75 → v0.36.0**（用户选定） | 见 #14、`项目进度.md` 变更记录 |
| 上午 | 用户切无卡模式（cgroup 确认 2GB / 0.5 核 / GPU 不可见）；调整策略为"慢而便宜的放无卡" | `项目进度.md` 变更记录 |
| 上午 | 升级前只读侦察：回退锚点可靠、无源码改动、**发现软链隐患**、发现 `$LOCK` 脏目录 | `deploy/autodl/09_recon_upgrade.sh` |
| 上午 | 修正 #13 的归因（只对了一半）；登记 #14 与 #15 | #13 修正、#14、#15 |
| 上午 | `git fetch` v0.36.0（加内存护栏防 2GB OOM） | `deploy/autodl/11_fetch_target.sh` |

---

## 2. 环境现状（已建成）

### 2.1 硬件与运行时

| 项 | 值 |
|---|---|
| 平台 | autoDL（西部区 / seetacloud） |
| OS | Ubuntu 22.04.4 LTS，kernel 5.15.0-78 |
| GPU | NVIDIA GeForce RTX **4090 24G**，驱动 580.76.05 |
| CPU / 内存 | 128 核 / **1007 GB** |
| Python | 3.12.3（miniconda3 base） |
| torch | **2.5.1+cu124**，CUDA 12.4，可用 ✅ |

### 2.2 ComfyUI

| 项 | 值 |
|---|---|
| 路径 | `/root/ComfyUI` |
| 版本 | **v0.3.75**（commit `8402c8700a29a97bc5d706d6a0b14c41bc2c2d8a`） |
| 自定义节点 | **31 个**，全部 git clone（非 pip），commit 已锁定 |

已包含本项目 **P4 控制体系所需的全部核心节点**（这是复用现有环境的最大收益）：

| 用途 | 节点包 |
|---|---|
| ControlNet 增强 | `ComfyUI-Advanced-ControlNet` |
| **IP-Adapter** | `ComfyUI_IPAdapter_plus` |
| **SAM / 分割 / 自动遮罩** | `ComfyUI-Impact-Pack`、`ComfyUI_BiRefNet_ll` |
| **DWPose / 预处理器** | `comfyui_controlnet_aux` |
| 杂项必备 | `ComfyUI-KJNodes`、`ComfyUI-Easy-Use`、`rgthree-comfy`、`was-node-suite` |
| **加速** | `ComfyUI-TeaCache`、`ComfyUI-nunchaku`、`ComfyUI-Upscaler-Tensorrt` |
| 管理 | `ComfyUI-Manager` |

### 2.3 存储布局

```
/root/autodl-tmp/comfyui-data/        ← 数据盘 /dev/md0 50G（可用 49G）
├── versions.lock                      ← 版本基线（398 行）
├── models/    ← /root/ComfyUI/models  (软链)
├── output/    ← /root/ComfyUI/output  (软链)
└── input/     ← /root/ComfyUI/input   (软链)

/root/ComfyUI/                        ← 系统盘 overlay 30G（可用 ~15G，仅放代码）
```

> ⚠️ **数据盘持久性提醒**：autoDL 的 `autodl-tmp` 在关机后通常保留，但**释放实例 / 换机型可能丢失**。
> 长期看，模型 weights 应从 HF 可重建（清单 + hash 已登记），真正需要备份的是
> `versions.lock`、工作流 JSON、提示词库 —— 这些都在 GitHub 仓库里，OK。

### 2.4 已登记的模型（待下载完成补充）

| 模型 | 许可证 | 商用 | 目标路径 | 状态 |
|---|---|---|---|---|
| **SDXL 1.0 base** | CreativeML OpenRAIL++-M | ✅ | `models/checkpoints/` | ✅ 已就位（6.9GB） |
| SDXL VAE fp16 fix | 待核 | ✅ | `models/vae/` | ✅ 已就位（335MB） |
| **FLUX.2 [klein] 4B**（unet） | **Apache-2.0** | ✅ | `models/unet/` | ✅ 已就位（7.3GB） |
| FLUX.2 VAE | Apache-2.0 | ✅ | `models/vae/` | ✅ 已就位（321MB） |
| Qwen3-4B 文本编码器（fp4） | 待核 | ⚠️ | `models/text_encoders/` | ❌ 本机 ComfyUI v0.3.75 不兼容（hidden 1280 vs 2560，见 #13） |
| **Qwen3-4B 文本编码器（全精度）** | 待核 | ✅ | `models/text_encoders/` | 🔵 下载中（7.4GiB，~30min @3.8MiB/s） |
| ~~Flux schnell fp8~~ | Apache-2.0 | ✅ | — | ❌ 已放弃：gated 仓库取不到（见 #7） |

---

## 3. 问题处置台账

> 格式：**现象 → 分析 → 决策 → 处置 → 验证 → 沉淀**

---

### #1 本机未配置 git 身份，无法提交

| | |
|---|---|
| **现象** | `git config --global user.name` 返回空，首次 `git commit` 会因缺少 author 而失败 |
| **分析** | 本机无 `~/.gitconfig`、无 SSH key，属全新环境。而这个仓库是要与 GitHub 账号 `Peterwong666` 关联的**作品集仓库**，commit author 必须干净且正确关联贡献记录 |
| **决策** | 向用户确认后，使用 GitHub noreply 邮箱（`Peterwong666@users.noreply.github.com`），本地仓库级配置（不污染全局） |
| **处置** | `git config user.name "Peterwong666"` + `git config user.email "...noreply.github.com"` |
| **验证** | `git log` 显示 author 正确；后续提交均正常 |
| **沉淀** | 无代码改动，纯一次性环境操作。若将来换机器，同样用 noreply 邮箱 |

---

### #2 本机缺少 sshpass，无法免交互 SSH

| | |
|---|---|
| **现象** | `which sshpass` 无输出；autoDL 用**密码登录**（22910 端口），无法在脚本里交互输入 |
| **分析** | 需要在自动化流程里登录远端，必须有非交互方式。可选：装 sshpass、或配置 SSH key |
| **决策** | 装 sshpass，**不留下 SSH key** —— 避免在本机和服务器上留下长期凭据；密码用完即弃 |
| **处置** | `sudo apt-get install -y sshpass`（sshpass 1.09） |
| **验证** | `sshpass -e ssh ...` 成功连入 `autodl-container-be8347bd6f` |
| **沉淀** | 统一用 `SSHPASS` 环境变量传密码，避免在命令历史里明文出现 |

---

### #3 远端多层 shell 转义导致脚本语义错乱 ⭐ **本项目第一条工程纪律**

| | |
|---|---|
| **现象** | 执行内联远端命令生成 `versions.lock` 时，输出打印出字面量 `已生成 $(wc -l < $LOCK) 行`；真正要写的目标文件没生成，反而在 `/root` 下产生了一个名叫 **`$LOCK`** 的诡异文件 |
| **分析** | 命令经过三层解释：本地 bash → ssh → 远端 bash。为了让 `$LOCK` 不被本地展开，我写了 `\$LOCK`；但反斜杠连同 `$` 一起被送到远端，导致远端看到的是字面的 `\$LOCK`。**变量重定向到一个叫 `$LOCK` 的文件名**，而 shell 变量赋值 `LOCK=...` 本身是生效的，所以排查时容易误判「变量没生效」。同时 `(...)` 命令替换链的转义也全部失效 |
| **决策** | **彻底放弃内联长命令**：改为「本地写 `.sh` → `scp` 上传 → 远端 `bash xxx.sh`」。脚本文件里保留原生 `$VAR` 语义，不经二次转义。副作用是这些脚本天然可以入库，成为环境重建文档的一部分（正好满足 P2-07） |
| **处置** | 产出 `deploy/autodl/01_lock_versions.sh`、`02_migrate_models.sh`，scp 后执行；并清理了误生成的 `$LOCK` 文件（脚本内含清理逻辑） |
| **验证** | `versions.lock` 正常生成，398 行；迁移脚本幂等二次执行不报错 |
| **沉淀** | ⭐ **纪律：所有对远端的操作一律写成脚本文件再上传执行。禁止在 SSH 命令里塞多行逻辑。** 这条同时解决可复现性与可审计性 |

---

### #4 系统盘仅剩 13G，模型放不下 ⭐ **关键基础设施决策**

| | |
|---|---|
| **现象** | 根分区 overlay 30G 已用 18G，**可用仅 13G**；而 SDXL base 6.9G + Flux fp8 11.9G + 控制模型 + LoRA 明显超出 |
| **分析** | 逐个排查了三处可用空间：① `/dev/md0` 是独立 xfs 数据盘 50G，挂在 `/root/autodl-tmp`，几乎全空；② `/autodl-pub/data` 20T 是**只读**公共数据集，`find` 确认其中没有 SDXL/Flux 现成的底模；③ 系统盘必须留给代码与运行时。结论：唯一正解是把**体积会持续增长的目录**迁到数据盘 |
| **决策** | `models` / `output` / `input` 三大目录迁到数据盘，原位保留软链（对用户透明的零侵入改动）。经用户确认 |
| **处置** | `02_migrate_models.sh`，设计成**幂等**（已是软链则跳过、目标已存在则合并），避免重复执行破坏数据 |
| **验证** | 数据盘使用 1.7G / 可用 49G；三个软链指向正确；`du` 显示 input/output/models 均已落位 |
| **沉淀** | 脚本入库 `deploy/autodl/`；这条同时是 P2-02 的交付物 |

---

### #5 HuggingFace 直连不通

| | |
|---|---|
| **现象** | 服务器上 `curl` 访问 `huggingface.co` 下载 SDXL，**耗时 2m9s 后 speed=0 B/s**（超时失败） |
| **分析** | autoDL 国内机房到 HF 官方 CDN 不通，属网络可达性问题而非 URL 或权限问题（同一 URL 换域名即通） |
| **决策** | 改用 `hf-mirror.com`（HF 社区镜像），URL 其余部分完全一致，替换成本为零 |
| **处置** | 所有下载统一走 hf-mirror；实测速度 **~3.0 MB/s**，50MB 区间测试 17 秒完成 |
| **验证** | 模型 URL 逐个 HEAD 校验通过；SDXL 已用 aria2c 16 连接开始下载，稳定 3.4MiB/s |
| **沉淀** | 后续 `model_registry.yaml` 里记录来源 URL 时注明使用镜像加速；README 部署章节需提示国内用户替换域名 |

---

### #6 服务器上已存在 InsightFace 权重 → 商用红线

| | |
|---|---|
| **现象** | `/root/ComfyUI/models/insightface`（601M）随环境自带存在 |
| **分析** | InsightFace 预训练模型为**非商用 / 仅研究用途**。而 InstantID 依赖它 —— 意味着 **InstantID 整条链路在商用交付中不可用**。这与 ADR-002 的合规红线、以及 `license_matrix.md` 里标记的结论完全一致，是「文档预测到的风险在现实中被验证」的第一个案例 |
| **决策** | ① 不删除（其他节点可能依赖，删除有破坏风险）；② 标记**禁用**，V1 不使用 InstantID 相关链路；③ 替代方案 `PuLID`（Apache-2.0，无 InsightFace 依赖）排入 V2 的 IP 一致性场景 |
| **处置** | 在 `license_matrix.md` 中已登记；本项目推进过程中主动规避 |
| **验证** | 已确认 V1 的六条工作流与 P4 控制清单均不依赖 InsightFace |
| **沉淀** | 又一次证明「下载即登记、未知即不用」这条铁律的价值 —— 环境自带的模型同样要审 |

---

### #7 Flux 的 VAE（ae.safetensors）官方仓库 403 🔴 **待解决**

| | |
|---|---|
| **现象** | `black-forest-labs/FLUX.1-schnell` 与 `FLUX.1-dev` 仓库的 `ae.safetensors` 均返回 **403** |
| **分析** | 已排除「URL 写错」（同一 URL 的 `flux1-dev-fp8.safetensors` 返回 200，说明路径规则正确）。因此这是 **gated repo**：BFL 要求登录并接受条款后才允许下载。AE 权重本身 Apache-2.0 可商用，**卡的是取用通道，不是许可本身** |
| **已尝试的替代源** | `Comfy-Org/flux1-schnell`（无此文件）· `city96/FLUX.1-schnell-gguf` · `city96/FLUX.1-dev-gguf` · `camenduru/FLUX.1-schnell` · `shiimizu/flux-ae` · `lllyasviel/misc` —— **全部 404**；`Comfy-Org/flux_text_encoders` 的 `clip_l` 与 `t5xxl_fp8` 则 **200 可用** |
| **最终解决** ✅ | **不再需要 HF token —— 改用 FLUX.2 [klein] 4B**，问题从根上绕开。详见 #9 |
| **用户补充的线索** | 用户提供了 BFL 官方仓库清单与 `HF_ENDPOINT` + `huggingface-cli download` 思路。经验证：**镜像也绕不过 gating**（`GatedRepoError: Cannot access gated repo ... you are not in the authorized list`），因为 gating 是授权校验，与走哪个域名无关 |
| **沉淀** | 教训：**在 autoDL 上开始一个模型之前，先验证它所有必需组件的可下载性**（checkpoint / text encoder / VAE 三类缺一不可）。已把这条写进模型登记流程 |

---

### #8 本机 HF API 不可达

| | |
|---|---|
| **现象** | 在本地执行 HF API 查询返回非 JSON（`Expecting value: line 1 column 1`），无法用本地网络校验模型文件清单 |
| **分析** | 本地沙箱网络受限，对 HF API 的访问被拦截或返回替代内容 |
| **决策** | 不在本地网络上继续纠缠，改为在**能连通的服务器上**查询（走 hf-mirror 的 `/api/models/` 端点） |
| **处置** | 在远端用 `curl + tr ',' '\n' + grep` 提取文件清单（避免 Python 内联引号问题，同 #3 的教训） |
| **验证** | 成功列出 `Comfy-Org/flux1-schnell`（2 个文件）与 `comfyanonymous/flux_text_encoders`（4 个文件） |
| **沉淀** | 「哪个网络能通就在哪个网络查」；同时再次验证了 #3 的脚本化原则 |

---

### #8.5 冒烟测试通过 ✅（里程碑而非问题）

| | |
|---|---|
| **结果** | SDXL 1.0 文生图成功：`1024×1024 / dpmpp_2m / karras / 25 步 / cfg 6.5`，**耗时 12.0 秒**，产物 1085 KB |
| **意义** | 一次性验证了整条链路：**软链后的数据盘模型可读 → ComfyUI 引擎正常 → GPU 推理正常 → HTTP API 提交/轮询/取图全通**。P2-04 完成 |
| **副产品** | `04_api_smoke_test.py` 就是未来 `engine/driver.py`（P6-01）的雏形：健康检查、任务提交、轮询、产物下载四段逻辑已跑通 |
| **下一步** | 用同一套脚本跑 FLUX.2 klein，对比两者耗时与画质（P5-02 的雏形） |

---

### #9 换用 FLUX.2 [klein] 4B —— 同时解决许可与可下载性 ⭐ **本轮最有价值的决策**

| | |
|---|---|
| **现象** | 用户询问 FLUX.2 是否可用于本项目（并提供了 BFL 官方仓库与官方推理代码） |
| **分析** | 查官方仓库 `black-forest-labs/flux2` 的模型总表后，发现 **FLUX.2 [klein] 4B 同时命中本项目三个硬需求**：<br>① **Apache-2.0** —— 唯一可商用的 FLUX.2 变体（9B 与 dev 均为 Non-Commercial）<br>② **显存友好** —— 官方称 4B 可在约 8GB VRAM 运行，我们的 4090 24G 绰绰有余，且支持亚秒级生成，契合「量产」目标<br>③ **统一生成与编辑** —— 一个模型同时支持文生图、**单参考图编辑**、**多参考图编辑**，天然覆盖本项目的图生图 / 局部重绘 / 风格迁移 / 商品一致性场景<br>而 FLUX.1 系里，唯一商用的 schnell 仍在 gated 仓库里（#7） |
| **关键验证** | ① `black-forest-labs/FLUX.2-klein-4B` **未 gated**（返回 404 而非 403，可自由下载）<br>② 该仓库**自带 `vae/diffusion_pytorch_model.safetensors`**，不再需要单独找 VAE<br>③ `Comfy-Org/flux2-klein-4B` 有 ComfyUI 专用打包（unet + vae + qwen3-4b 文本编码器分目录）<br>④ 本机 ComfyUI v0.3.75 的提交历史中已含 `Fix Flux2 reference image mem estimation`（#10905），说明**已内建 FLUX.2 支持** |
| **决策** | 用 **FLUX.2 [klein] 4B（Apache-2.0）** 取代 FLUX.1 schnell 作为第二底模；SDXL 仍是 V1 主底模（控制生态成熟）。**明确排除** FLUX.2-dev / 9B / Kontext 等 Non-Commercial 变体 |
| **处置** | `05_download_flux2_klein.sh` 并行下载三组件（unet 7.75GB / vae 320MB / text encoder fp4 3.5GB），分别落到 `models/unet`、`models/vae`、`models/text_encoders` |
| **验证** | 待下载完成后用同一套冒烟脚本跑通 |
| **沉淀** | ⭐ **选模型先看「许可证 × 可下载性 × 生态」三件事，再看画质。** 这次正是靠前两项把 FLUX.1 排除掉、把 FLUX.2 klein 选出来的。已写入选型 SOP |

**FLUX.2 家族许可一览（官方 README，2026-09 核对）**

| 模型 | 蒸馏 | 文生图 | 编辑 | 许可证 | 本项目 |
|---|---|---|---|---|---|
| **FLUX.2 [klein] 4B** | 步数+引导 | ✅ | 单/多参考 | **Apache-2.0** | ✅ 采用 |
| FLUX.2 [klein] 4B Base | ❌ | ✅ | ✅ | **Apache-2.0** | V2 微调候选 |
| FLUX.2 [klein] 9B / 9B KV | ✅ | ✅ | ✅ | Non-Commercial | ❌ 禁用 |
| FLUX.2 [klein] 9B Base | ❌ | ✅ | ✅ | Non-Commercial | ❌ 禁用 |
| FLUX.2 [dev] 32B | 引导蒸馏 | ✅ | ✅ | Non-Commercial | ❌ 禁用 |
| FLUX.2 autoencoder (VAE) | — | — | — | **Apache-2.0** | ✅ 含在包内 |

---

---

### #10 GPU 成本策略：切无卡模式省钱 ⭐ **运营决策**

| | |
|---|---|
| **现象** | 用户发现 GPU 一直开着但只是下载模型，问能否关掉 GPU、只开 CPU |
| **分析** | 查官方核实（见 `docs/sop/gpu_cost_strategy.md`）：① 无卡模式 **¥0.1/h**，配置为 **0.5 核 / 2GB 内存 / 无 GPU**，数据完全不受影响，但**切换需先关机**（会杀进程）② 关机后**停止计算计费**，仅数据盘存储费 ③ 无卡模式会释放 GPU，**切回时可能没卡可用** ④ 连续关机 15 天实例会被释放。<br>cgroup 实测确认：`memory.max=2147483648`（2GB）、`cpu.max=50000 100000`（0.5 核） |
| **决策** | 立即切无卡模式完成剩余下载；**下载类任务一律在无卡模式做**。P3 进入连续出图期后保持有卡，避免频繁切换带来的「没卡」风险 |
| **处置** | 用户已切无卡模式。实测：数据全部保留（SDXL + VAE + 已下部分），GPU 不可见（`No devices found`），重跑 `05_download_flux2_klein.sh` 续传成功 |
| **副带发现** ⭐ | 之前误判「unet 已下完」—— 因为 `ls -la` 显示文件已是 7.75GB。实际 aria2c 用了 `-k 1M` **预分配**磁盘空间，**文件大小不能作为完成依据**，实际只下了 13%。<br>**正确判断方式**：看 aria2c 日志的 `Download complete`，或 `.aria2` 控制文件是否消失 |
| **沉淀** | 已写入 `docs/sop/gpu_cost_strategy.md`，含三种模式对比、各阶段推荐模式、5 条省钱纪律、成本记录表 |

---

### #11 复查时误判「模型目录为空」，一度有重下 11GB 的风险 ⚠️ **差点浪费 1.5 小时**

| | |
|---|---|
| **现象** | 晚 21:18 复查下载状态，执行 `ls -la /root/autodl-tmp/models/unet/ ...`，返回**空结果**；随后 `find` 才在 `comfyui-data/models/` 下找到全部文件。若当时按「目录不存在」直接重下，会白白重传 11GB |
| **分析** | 根因是**路径假设错误**：本项目实际布局是 `/root/autodl-tmp/comfyui-data/models/{unet,vae,text_encoders}`，而我在命令里写的是 `/root/autodl-tmp/models/`。`ls` 对不存在的路径**不报错、只返回空**，于是「路径写错」被静默地伪装成「目录是空的」。<br>叠加因素：`02_migrate_models.sh` 迁移的是 `comfyui-data/{models,input,output}`，而 `05_download_flux2_klein.sh` 用 `${COMFY_MODELS:-/root/autodl-tmp/comfyui-data/models}` 作默认值 —— 两处都正确，错的是我的复查命令 |
| **决策** | 不重下。先用 `find -name "*.safetensors" -size +100M` 反查真实位置，确认三件套都在。同时建立「**关键路径不靠记忆，靠反查**」的纪律 |
| **处置** | ① `find` 定位到真实路径 ② 复核对账：unet 7.75GB / tenc 3.85GB / vae 336MB 均在位 ③ 用 `.aria2` 控制文件判断真实进度（unet 28%、tenc 72%），确认仍在下载中 |
| **验证** | `ls` 目标路径 + `.aria2` 存在性 + aria2c 日志三者一致 → 判定「下载进行中，无需干预」 |
| **沉淀** | **纪律**：① 复查远端状态时，路径必须**从脚本里读**，不要凭记忆写 ② `ls` 空结果 ≠ 目录为空，可能是路径错 —— 用 `find` 兜底 ③ 判断下载完成**只看 `.aria2` 是否消失 + 日志 `Download complete`**（与 #10 同源） |
| **代价** | 约 10 分钟排查；若误判则会浪费 1.5 小时 + 15GB 流量 |

---

### #12 todolist.md 的 P1 段落编辑失败（旧匹配串过期）⚠️ **文档维护纪律**

| | |
|---|---|
| **现象** | 用 Edit 工具把 P1 的任务表格转成 checkbox 格式时报错：`String to replace not found in file`，编辑未生效。而同批的 git commit **已经成功提交**，造成「提交信息说完成了 P1，但文件里 P1 还没打勾」的短暂不一致 |
| **分析** | 根因：P0 与 P2 段落**此前已被转成 checkbox 格式**，而 P1 段落仍是**原始表格格式**。我用于替换的 `old_string` 是**基于记忆中的表格内容**构造的，与文件当前真实内容不一致，因此匹配失败。<br>本质是**「读」与「写」之间隔了多轮对话**，记忆漂移不可避免 |
| **决策** | 不猜、不试错。**先 Read 当前真实内容，再基于原文构造替换串** |
| **处置** | ① `Grep` 定位 P1 各行行号（223~257）→ ② `Read` 读取 205~274 行的原文 → ③ 基于真实原文重新执行 Edit → 成功 |
| **验证** | 编辑成功 + `git status` 显示文件已修改 + 后续可正常提交 |
| **沉淀** | **纪律**：① **编辑前必读** —— 任何 Edit 之前先 Read 目标区段，不用记忆构造 `old_string` ② 涉及格式转换（表格→checkbox）这类**结构性改动**时，宁可整段替换，不要逐行拼 ③ 若「编辑」与「提交」并行执行，必须确认编辑成功后再提交，否则提交信息会失真 |

---

### #13 FLUX.2 fp4 文本编码器与本机 ComfyUI v0.3.75 不兼容（hidden 1280 vs 2560）⭐ **踩坑：三件套都下完才发现有问题**

| | |
|---|---|
| **现象** | unet + vae + fp4 文本编码器三件套都下完后，跑冒烟测试在 `CLIPLoader` 节点直接报 `size mismatch for model.layers.1.self_attn.k_proj.weight: copying a param with shape [1024, 1280] from checkpoint, the shape in current model is [1024, 2560]` |
| **分析** | 报错的不是 unet，而是**文本编码器**：`qwen_3_4b_fp4_flux2.safetensors`（fp4 量化版）隐藏维是 **1280**，而本机 ComfyUI v0.3.75 的 `CLIPType.FLUX2` 构建的是 `Qwen3_4B`（隐藏 **2560**）。fp4 版是另一种更小架构/量化方案，与该版本 flux2 加载路径不匹配（注：该版本 `Flux2.clip_target` 仍是 `return None # TODO`，flux2 支持尚属半成品）。<br>旁证：本版 ComfyUI 把 flux2 文本编码器按单一 SD1 风格编码器加载（只暴露 `clip_l`，无 `t5xxl`），与 fp4 版的预期不一致 |
| **决策** | 放弃 fp4 版，改用**全精度** `qwen_3_4b.safetensors`（仓库经重定向落在 `Comfy-Org/vae-text-encorder-for-flux-klein-4b`）。已读其 safetensors 头校验隐藏维为 **2560**，与本机 `Qwen3_4B` 一致 |
| **处置** | `05` 脚本只下了 fp4 版；新增全精度版下载（`aria2c -c` 续传）。冒烟工作流 JSON 里 `CLIPLoader` 的 `clip_name` 改为 `qwen_3_4b.safetensors`、`type=flux2` |
| **验证** | 读取部分下载文件的 safetensors 头：`model.layers.0.self_attn.k_proj.weight [1024, 2560]`、`q_proj [4096, 2560]` → 确认隐藏 2560 匹配；下载完成后用冒烟脚本跑通出图 |
| **沉淀** | ⭐ **该版本 ComfyUI 的 flux2 支持是半成品**（fp4 文本编码器与全精度路径不互通）。后续在这台机器上跑 FLUX.2，**文本编码器一律用全精度 `qwen_3_4b.safetensors`**，不要为省显存用 fp4 版。<br>再次印证 #10/#11：判断下载完成**只看 `.aria2` 是否消失**，文件大小因 `-k 1M` 预分配恒为最终值，绝不可信 |
| **⚠️ 事后修正（09-16，见 #14）** | 本条当时的归因**只对了一半**。真实原因是 **ComfyUI v0.3.75 早于 FLUX.2 klein 发布 2 个月**：`QWEN3_4B` 在这个版本里被路由去 `z_image` 而不是 flux2。所以换全精度版**并不能解决问题** —— 全精度版跑起来会以另一种方式失败（`KeyError: 't5xxl'`）。<br>教训：遇到 `shape mismatch` 时，不要停留在"文件版本不对"这一层，要往上追问"**这个引擎版本到底支不支持这个模型**" |

---

### #14 ComfyUI v0.3.75 物理上跑不了 FLUX.2 klein —— 引擎版本早于模型发布 2 个月 ⭐⭐ **决策级阻塞：推翻 09-15 的「复用现有环境」决策**

| | |
|---|---|
| **现象** | 全精度文本编码器下完后重跑冒烟，`CLIPTextEncodeFlux` 节点报 `KeyError: 't5xxl'`（`comfy_extras/nodes_flux.py:30` → `clip.tokenize(t5xxl)["t5xxl"]`）。即：不是模型文件问题，是**节点拿不到它要的那个编码器输出** |
| **分析** | 逐层读源码，链条完全闭合：<br>① `comfy/supported_models.py:770` —— `Flux2.clip_target()` 直接 `return None # TODO`，**本版 flux2 的内置文本编码器接线是被禁用的半成品**<br>② 只能靠外部 `CLIPLoader`，而 `comfy/sd.py:1112-1120` 的路由是：`MISTRAL3_24B / MISTRAL3_24B_PRUNED_FLUX2 → flux.flux2_te + Flux2Tokenizer`，`QWEN3_4B → z_image.te + ZImageTokenizer`<br>③ `flux.py:126` 的 `Flux2Tokenizer` 写死 `name="mistral3_24b"`；`z_image.py:14` 的 `ZImageTokenizer` 写死 `name="qwen3_4b"`<br>④ 我们下的 `qwen_3_4b.safetensors` 被识别为 `QWEN3_4B` → **被路由去 z_image（那是另一个模型）**，于是 `CLIPTextEncodeFlux` 找不到 `t5xxl`<br>⑤ 根因在版本：本机 `v0.3.75 / commit 8402c870 / **2025-11-26**`，而 FLUX.2 klein 官方文档标注日期为 **2026-01-15** —— **引擎比模型早 2 个月** |
| **旁证** | 官方文档（docs.comfy.org/zh/tutorials/flux/flux-2-klein）明确写「请确保你的 ComfyUI 已经更新」，并提示「节点缺失通常是用的不是最新版（每夜版）」；同时确认 klein 4B 的文本编码器**就是** `qwen_3_4b.safetensors` —— 说明**我们的模型下载是对的，错的是引擎** |
| **决策** | 用户选定：**升级到最新稳定版 v0.36.0**（而非"最小升级"或"V1 放弃 klein"）。理由：① 现在是项目第 3 天，是升级破坏面最小的时刻 ② 10 个月前的引擎在 P4 控制体系（新 ControlNet / SAM2）阶段必然再卡一次 ③ 可顺带完成积压的 P2-05 节点白名单裁剪 |
| **处置** | ① 升级前**只读侦察**：确认回退锚点可靠（tag `v0.3.75` → `8402c870`）、无源码级本地改动（33 个改动全是 models 迁移导致的删除，可安全丢弃）② 发现并规避软链隐患（见下）③ `git fetch` 显式压 `pack.threads=1 / pack.windowMemory=32m` 等参数，防无卡模式 2GB cgroup 上限下 OOM |
| **发现的隐患** ⚠️ | 侦察时发现：`models`/`input`/`output` 已迁到数据盘并软链回原位，git 因此把它们视为"已删除"。若直接 `git checkout -f v0.36.0`，git 会重建真实目录 → **切断数据盘模型挂载**，模型全部"消失"。规避：**先摘链 → checkout → 删掉新建的真实目录 → 挂回软链**（见 `deploy/autodl/12_upgrade_comfyui.sh`） |
| **顺带发现** | `custom_nodes/$LOCK` 是一个**目录**，是 09-15 那起多层 shell 转义事故的残留物（#3）。升级时一并清理 |
| **验证** | 待升级后：① ComfyUI 能启动且无 import 错误 ② SDXL 冒烟**不回归**（重跑对比 5.07s 基线）③ FLUX.2 klein 冒烟出图 ④ 重生成 `versions.lock` |
| **沉淀** | ⭐ **选型时必须核对"引擎版本 vs 模型发布时间"**。09-15 判断"v0.3.75 版本够新"时，只看了版本号数字，没查发布日期，也没验证目标模型在本版是否真的可用 —— 结果把「半成品实现」当成了「已支持」。<br>⭐ **纪律**：引入新模型时，**先用 `/object_info` 探测节点 + 读源码确认路由**，再决定要不要下载权重（本次先下了 11GB 才发现，虽然模型文件最终有用，但顺序是错的）。<br>⭐ 「复用现有环境」这类省时决策**必须带失效条件**：本次若写明"前提是目标模型在本版可用"，09-15 就不会草率下这个结论 |

---

### #15 性能指标口径错误：把冷启动值当稳态值，高估了 2.6 倍 ⭐ **方法论：首个样本必须剔除**

| | |
|---|---|
| **现象** | 09-15 记录的 NFR-1「单张出图（SDXL 1024²/25 步）= **12.0s**」，回填进了 `docs/prd/PRD_v1.md`。09-16 用新写的 `07_bench_workflow.py` 做 1024²/30 步标定，跑出来是 **4.6s 中位数 / P95 5.07s** —— 步数更多反而快 2.6 倍，明显不合理 |
| **分析** | 矛盾的来源是**口径**：09-15 的 12.0s 是 ComfyUI 启动后的**第一次**出图，包含了模型加载（SDXL 6.9GB 从磁盘读入 + 显存分配）；本次标定的 `[warmup] 14.78s` 正好复现了这个量级。**12.0s 与 4.6s 其实测的是两件事**：前者是冷启动，后者是稳态 |
| **决策** | 统一口径为**稳态**（业务上用户关心的是"提交第 N 张要等多久"，N>1 是常态）。在 `07_bench_workflow.py` 里把「**先跑 1 次 warmup 且不计入统计**」写成硬约束，并在脚本注释里说明原因 |
| **处置** | ① 新脚本采样 N=12 + warmup=1 ② P95 用 **nearest-rank 法**（小样本下比插值法更保守、更可解释）③ 每次运行改 seed，避免 ComfyUI 侧缓存整条执行结果导致耗时失真 |
| **验证** | 12 个稳态样本高度收敛：min 4.54 / median 4.60 / P95 5.07 / max 5.07，**stdev 仅 0.20s** —— 说明稳态口径稳定可信；同时 `[warmup] 14.78s` 与历史 12.0s 同量级，反证了冷启动解释 |
| **沉淀** | ⭐ **性能数字必须带口径**：冷启动 / 稳态、并发度、分辨率、步数、是否含模型加载。没有口径的数字比没有数字更危险 —— 它会被当成事实写进 PRD、被用来算成本模型，然后误导排期。<br>⭐ 影响面：NFR-1 需按新口径改写；`docs/prd/resource.md` 的成本/吞吐预估也要重算（吞吐被低估了 2.6 倍，单张成本相应高估） |

---

### #16 引擎升级执行纪实：v0.3.75 → v0.36.0（含 torch 2.13.0+cu130）⭐ **一次跨 10 个月、1696 个提交的大跨度升级**

| | |
|---|---|
| **目标** | 让 FLUX.2 klein 能跑起来（见 #14）。执行方式：把「慢而便宜」的部分放在无卡模式，把「需要 GPU」的验证攒到最后一次性切机 |
| **处置 1 · 拉取** | `git fetch --tags origin v0.36.0`。**给 git 加内存护栏**（`pack.threads=1`、`pack.windowMemory=32m`、`core.packedGitLimit=128m`）—— 无卡模式 cgroup 硬限 2GB，默认参数有 OOM 风险。实测峰值仅 357MB，护栏生效 |
| **处置 2 · 切换前侦察** | `09_recon_upgrade.sh` 确认：回退锚点 `v0.3.75`→`8402c870` 可靠；本地改动只有 33 个删除（models 迁移的预期结果），**无源码级改动**，故可安全 `checkout -f` |
| **处置 3 · 软链保护** ⚠️ | **发现并规避一个会导致"模型全部消失"的隐患**：`models/input/output` 已迁数据盘并软链回原位，git 视其内文件为已删除；直接 `git checkout -f` 会**重建真实目录顶掉软链**。<br>脚本流程改为：**摘链 → checkout → 校验新建目录体积 <10MB 才敢删 → 挂回软链**。实测 git 建了 36 个占位文件（56KB），安全删除后软链完好，4 个关键模型文件全部可见 |
| **处置 4 · 依赖预演** ⭐ | 先 `pip install --dry-run` 而非直接装，**在不破坏现有环境的状态下**发现两个问题：<br>① `comfyui-workflow-templates==0.11.62` **阿里云镜像尚未同步**（镜像最高 `0.11.60`）—— 因 v0.36.0 是「昨天」发布的<br>② 解法：主源仍用镜像（快），加 `--extra-index-url https://pypi.org/simple` 兜底，**只有镜像缺的那一个包走官方源** |
| **处置 5 · 代码切换** | `git checkout -f v0.36.0` 成功，HEAD `ee71d5c4` |
| **处置 6 · 依赖安装** | 16 个包，`pip exit=0`。torch/numpy **未变更**（`requirements.txt` 里 torch 是不固定版本的裸 `torch`）—— 这点后来被证明是**盲区**，见处置 7 |
| **处置 7 · torch 的真实要求** ⭐ | 装完依赖后 `import comfy.sd` 报 `ValueError: infer_schema(func): Parameter stride has unsupported type list[int]`。<br>**逐层追调用链**：`comfy.sd → model_management → memory_management → quant_ops → import comfy_kitchen → backends/cuda → backends/eager → conv3d.py:25` 的 `@torch.library.custom_op` 用了 PEP-585 的 `list[int]`，而 torch 2.5.1 的 `infer_schema` 只认 `typing.List[int]`。<br>**先验证「降级 comfy-kitchen」这条捷径是否可行**：下载 0.1.0/0.2.0/0.2.6/0.2.10/0.2.26 五个版本的 wheel 检查，发现**它们根本没有 `backends/eager/conv3d.py`** —— 该文件是 0.2.34 才引入、与 v0.36.0 同步发布的，降级会直接 ImportError。**捷径是死的** |
| **处置 8 · torch 选型** | 依据 v0.36.0 `README.md:195`：「最低 torch 2.7；**20 系及以上必须 cu130+**；推荐最新大版本但回避发布不足 2 周的」。<br>实测各版本发布日期：2.14.0 = 2026-09-02（**距今恰 14 天，卡在边界**）、2.13.0 = 2026-07-08（70 天）→ **选 2.13.0**，稳妥且够新。<br>驱动 580.76.05 属 CUDA 13 分支，满足 cu130 要求 |
| **处置 9 · 磁盘策略** | 系统盘仅剩 14G。改为**先卸载旧 torch+nvidia 释放 4.4G 再安装**（若"先装后卸"，峰值 ≈ 旧4.4G + 新6G = 10.4G，风险过高）。安装 24 个包、**下载约 3.2GB**，最终系统盘剩 9.4G |
| **处置 10 · 节点兼容修复** | GPU 校验前先用 **`--cpu` 模式做节点审计**（不耗 GPU 就能查出 import 级故障）。<br>发现 `flash_attn_2_cuda.so`（1GB）**undefined symbol** —— 它是针对 torch 2.5.1 编译的 C++ 扩展，torch 2.13 的 libtorch ABI 已变。它拖垮了 `comfy_extras/{nodes_latent,nodes_post_processing,nodes_morphology}` 三个**核心**模块 + `ComfyUI_essentials`。<br>**决策：卸载 flash-attn 而非重编译** —— ① 它对 ComfyUI 是可选加速，缺失时自动回退 PyTorch 原生注意力 ② 重编译需 1-3 小时且极易失败 ③ 保住核心模块远比保住 flash-attn 重要。卸载后 4 个模块全部恢复 |
| **验证 1 · klein 路由** ✅ | 源码确认 `comfy/sd.py:1888`：`QWEN3_4B` 现在会判断 `clip_type == CLIPType.FLUX2` → `klein_te(model_type="qwen3_4b")` + **`KleinTokenizer`**。`supported_models.py:822` 的 `Flux2.clip_target` 也已从 `return None # TODO` 变成**显式构造 klein 的 ClipTarget**。**根因确认被解决** |
| **验证 2 · 节点生态** ✅ | CPU 模式实测：节点类总数 **2102 → 2530**；16 个关键节点全部就位；`CLIPLoader.type` 枚举从 17 项扩到 29 项 |
| **验证 3 · 工作流结构** ⭐ | 发现原工作流用错了节点：`CLIPTextEncodeFlux` 依赖 `t5xxl` 键，而 klein 的 tokenizer 输出键是 `qwen3_4b`。<br>**不去猜，直接取官方权威来源** —— 随升级装好的 `comfyui-workflow-templates` 包里有 `image_flux2_klein_text_to_image.json`。展开其 subgraph 得到正解：**`CLIPTextEncode` + `CFGGuider` + `ConditioningZeroOut`**。<br>**并发现一个重要事实**：该模板含 Base 与 Distilled 两个子图，其中 Distilled 用的正是我们下载的 `flux-2-klein-4b.safetensors`（`-base-` 才是另一个文件）→ **我们下的是蒸馏版，只需 4 步、cfg=1**，官方数据 5090 上约 1.2s |
| **验证 4 · 提交前静态校验** ⭐ | 新增 `26_validate_workflow.py`，在**不花 GPU 机时**的前提下校验：节点类型是否存在、必填入参是否齐备、入参名是否被接受、连线目标与输出序号是否越界、枚举取值是否合法。两个工作流均 `VALIDATE=PASS` |
| **验证 5 · GPU 侧一次过验证** ✅ | 切回有卡后 `27_gpu_verify.sh` 一条命令跑完：① torch 2.13.0+cu130 **真正用上 4090**（`sm=(8,9)`、矩阵乘自测通过）② ComfyUI v0.36.0 启动正常 ③ SDXL 出图成功 ④ **FLUX.2 klein 蒸馏版首次出图成功**（median 1.83s / P95 2.36s，4 步/cfg=1）⑤ 显存峰值 15.8G/24G。<br>⚠️ 唯一遗留：SDXL 出现 **1.58x 性能回归**（median 4.60s → 7.26s），见 #17 |
| **沉淀** ⭐ | ① **`requirements.txt` 里写裸 `torch` 是个陷阱**：它让 pip 判定"已满足"而不升级，但代码实际要求更新的 torch。**判据应是 README/文档声明的最低版本，不是 requirements**。<br>② **大版本升级的连带损伤是系统性的**：torch 动 → C++ 扩展 ABI 动（flash-attn）→ 依赖核心 API 的自定义节点动（TeaCache 的 `precompute_freqs_cis`）。**必须用 `--cpu` 模式做节点审计**，否则这些错会以"跑到一半才炸"的形式消耗 GPU 机时。<br>③ **镜像同步延迟是「追最新版」的隐性成本**：升级目标定在"昨天发布的版本"，就会撞上镜像未同步。**可复用解法**：`--extra-index-url` 官方源兜底，主源仍是镜像。<br>④ **有官方模板时不要自己拼工作流**：官方模板直接给出了正确的节点组合与参数，省掉若干轮 GPU 试错。<br>⑤ **无卡模式的价值被低估**：git fetch（带内存护栏）、依赖预演、节点审计、工作流校验 —— 这些都在无卡模式下完成，GPU 机时只留给真正的推理 |

### #17 v0.36.0 升级后 SDXL 出图回归 1.58x —— 元凶是新增且默认开启的 Dynamic VRAM ⭐⭐ **升级的隐性代价：默认值变更**

| | |
|---|---|
| **现象** | 升级前 SDXL 1024²/30 步稳态 **median 4.60s / P95 5.07s**；升级后同工作流同参数变为 **median 7.26s / P95 7.72s（1.58x）**。旁证：klein 升级后首次实测就有 median 1.83s —— 说明**不是整机变慢**，而是 SDXL 这条路径特有 |
| **分析 ① 先撞上"假因"** | 第一反应是"注意力加速丢了"：`flash_attn_2_cuda.so` 与 `sageattention` 都是针对 torch 2.5.1 编译的 C++ 扩展，torch 升到 2.13 后双双 ABI 失配（`undefined symbol: _ZN3c106detail14torchCheckFailEPKcS2_jRKSs`）。为救回加速还专门写了 `28_try_sage_attention.sh` 尝试 SageAttention，同样失败并回退 |
| **分析 ② 用硬证据否掉假因** ⭐ | **"报错"≠"原本在用"**：① xformers **从未安装** ② flash-attn 仅在显式传 `--use-flash-attention` 时启用，而 `03_start_comfyui.sh` 从未传过该参数 ③ 升级前后日志均为 `Using pytorch attention`。<br>结论：**注意力后端自始至终没变过**，ABI 失配只是"一个没被使用的可选加速装不上了"，与回归无关 |
| **分析 ③ 转向"版本默认值变化"** | 比对 `comfy/cli_args.py` 发现 v0.36.0 新增两个**在 Nvidia 上默认开启**的开关：<br>· `--async-offload` —— 异步权重卸载，默认 2 条 CUDA 流<br>· **dynamic VRAM** —— 按需流式加载权重（`enables_dynamic_vram()` 默认返回 True）<br>SDXL 仅 6.9GB，24G 卡完全放得下，若仍做权重流式搬运就是**纯开销** |
| **决策** | 设计**单变量对照实验**（`29_ab_offload_vram.sh`）：固定工作流/分辨率/步数，仅切换启动参数，四变体各跑 N=8 基准。不去读源码猜内存管理逻辑 —— 猜的成本高且可能与实际不符 |
| **处置 · A/B 结果** ⭐ | 四变体各跑 N=8（同工作流/同分辨率/30 步）：<br>· 升级前基线 v0.3.75 —— median **4.60s** / P95 5.07s<br>· 默认（无额外参数）—— median **7.10s** / P95 7.57s<br>· `--disable-async-offload` —— median **7.18s** / P95 7.84s<br>· `--disable-async-offload --disable-dynamic-vram` —— median **4.92s** / P95 5.40s<br>· `--highvram` —— median **4.72s** / P95 5.47s<br>**判读**：单关 async offload **毫无改善**（7.18 ≈ 默认 7.10）；一旦关掉 **dynamic VRAM** 立刻回到 4.7~4.9s → **元凶是 Dynamic VRAM，async offload 与本次回归无关**。残余的 4.72 vs 4.60（2.6%）落在 stdev 0.32~0.38 的噪声带内，可视为已回到基线 |
| **验证** | 见 #18（补测 `--disable-dynamic-vram` 单用 + 复测 klein，确认修复不牺牲升级目标） |
| **沉淀** ⭐⭐ | ① **升级后必须做性能防回归，且必须与升级前同口径对照**。本次若只做"能出图"的冒烟，7.26s 就会被当成基线写进 PRD，成本模型与排期会被一起带偏（与 #15 同源教训）。<br>② **"版本号变新"不等于"行为不变"**：本次回归不是代码 bug，而是**新版本的默认值变更**。因此 `versions.lock` 只锁包版本远远不够 —— 必须同时锁**启动参数与生效的运行时开关**（已据此增补 `01_lock_versions.sh` 的 `[launch]` 段）。<br>③ **"可选加速装不上了"极易被误判为性能回归的原因**：它报错很显眼，但报错不等于原本在用。推翻它靠的是一条硬证据 —— 升级前后日志同为 `Using pytorch attention`。<br>④ **单变量对照的性价比极高**：从现象到定位只花了 4 次重启、约 6 分钟。若改为逐层读 ComfyUI 的显存管理源码，成本高得多且结论未必可靠。<br>⑤ **升级破坏面是"显性故障 + 隐性性能"两层**：显性故障（ABI/import 错误）在 CPU 模式审计时就会炸出来，隐性性能回归**只有跑基准才能发现** —— 两者必须都做 |

---

### #18 生产配置定案：`--highvram` —— 并用「看 min 而非 median」识破共享宿主机上的周期性干扰尖峰 ⭐⭐ **方法论：噪声环境下的稳态测量**

| | |
|---|---|
| **目标** | 从两档候选（`--disable-dynamic-vram` 保留 async offload / `--highvram` 关掉 dynamic VRAM 且模型常驻）中选定 V1 生产默认，并确认修复**不牺牲 FLUX.2 klein**（klein 才是本次升级的唯一目的） |
| **现象（反常）** ⚠️ | N=12 复测结果比上一轮 A/B（N=8）整体慢了约 0.3s：dynvram_off 4.92s → **5.26s**、highvram 4.72s → **5.00s**。若照 median 直接判读，会得出"**仍有约 8% 残留回归**"的结论，从而去改根本没问题的代码 |
| **分析** ⭐ | 翻原始样本发现是**双峰结构**：绝大多数样本落在"地板"，但每约 3 个样本出现一次尖峰。<br>· 升级前基线：地板 4.54~4.62s，尖峰 5.05~5.07s（第 6/9/12 个）<br>· `--highvram`：地板 4.59~4.90s，尖峰 5.25~5.48s（第 5/7/9/11 个）<br>· `--disable-dynamic-vram`：地板 4.85~5.15s，尖峰 5.83~6.17s（第 6/8/10 个）<br>**关键：尖峰在"升级前的基线"里同样存在** → 它是**共享宿主机的周期性干扰**（该机 128 核、load average 18.95，多容器共用磁盘与内存带宽），**不是升级引入的**。median 会被尖峰出现频率的随机波动带偏；**min 才是"稳态能力"的稳健估计** |
| **决策** | 改用 **min 作为稳态能力的判据**，median / P95 仅作参考。定案 **`--highvram`**，三条理由：① 地板 **4.59s** vs 升级前基线 **4.54s**，**仅差 1% —— 回归已完全修复** ② 明显优于 `--disable-dynamic-vram`（地板 4.85s）③ 双底模常驻仅 **13.1GB/24GB**，且与 NFR-2「并发=1、模型常驻显存」的设计一致 |
| **处置** | 把 `--highvram` 固化进 `03_start_comfyui.sh`（新增 `COMFY_EXTRA_ARGS`，默认 `--highvram`，注释写明理由、实测数据与失效条件），并**按生产路径重启**验证 —— 避免"验证过的配置 ≠ 实际发布的配置" |
| **验证** | ① SDXL 地板 4.59s（基线 4.54s，差 1%）② **FLUX.2 klein 两档均为 median 1.83s、地板 1.52s** —— 升级目标未被牺牲 ③ 按生产脚本重启后 `argv` 含 `--highvram`、日志不再出现 `DynamicVRAM ... enabled`、健康检查通过 |
| **参考值** | 生产配置（`--highvram`，SDXL 1024²/30 步，N=12 warmup=1）：**median 5.00s / P95 5.48s / min 4.59s**；FLUX.2 klein（4 步）：**median 1.83s / P95 2.67s / min 1.52s** |
| **沉淀** ⭐⭐ | ① **共享 GPU 实例上的性能测量必须区分「地板」与「尖峰」**：median/P95 同时反映外部干扰，只有 min（或分段统计）逼近真实稳态。本次若盲信 median，会把宿主机噪声误判成自己的回归。<br>② **同一配置跨轮次复测是必要的**：N=12 与 N=8 对同一配置差了 0.3s —— **单轮结果不足以定论**。<br>③ **性能修复必须复测「另一个模型」**：修 SDXL 不能拿 klein 当代价。两档配置下 klein 都稳定在 1.83s，才敢下结论。<br>④ **定案后要把配置固化进启动脚本并按其重启**，否则"验证通过的组合"与"实际运行的组合"会悄悄分家（这正是 #17 的成因本身） |

---

### #19 `pkill -f` 内联在 ssh 命令里，把执行它的 shell 自己杀了 ⚠️ **旧坑复发：又一次因「内联远端逻辑」翻车**

| | |
|---|---|
| **现象** | 执行一条含 `pkill -f "main.py --listen"` 的 ssh 内联命令后，**输出完全为空、退出码却是 0**。复查发现 ComfyUI 已被杀掉但**没有重新启动**，服务处于空窗（`http=000`） |
| **分析** | `pkill -f` 匹配的是**完整命令行**。远端执行这条命令的是 `bash -c '<整条命令>'`，而这条命令的**文本里就包含** `main.py --listen` → **pkill 把执行它的那个父 shell 一并杀掉**，于是 shell 在跑到"重新启动"那一步之前就死了。<br>注：`pkill` 会排除自己，但**不排除父 shell**。此坑与 #3 的 `$LOCK` 转义事故同属一类 —— **把多层 shell 逻辑内联进 ssh 命令** |
| **决策** | ① 把动作写成脚本文件执行（既有纪律，因已二次翻车故重申）② 把停进程的匹配模式改为**锚定正则**，从根上消除自匹配可能 |
| **处置** | 新增 `31_restart_prod.sh`：① 逻辑落在脚本文件中，父进程命令行不含模式串 ② 停止进程改用 `pkill -f '^/root/miniconda3/bin/python main\.py'`，锚定行首可执行路径，绝不匹配 shell ③ 脚本内做完整健康检查（生效开关 + `argv` + 版本号） |
| **验证** | 重启后 `argv=["main.py","--listen","127.0.0.1","--port","8188","--highvram"]`、日志无 `DynamicVRAM enabled`、健康检查通过、本地隧道 `http=200` |
| **沉淀** | ⭐ **`pkill -f` 的模式串一旦出现在「执行它的那个进程」的命令行里，就会自杀。** 安全写法二选一：① 用锚定正则限定到真实二进制路径 ② 先 `pgrep` 拿 PID 再按 PID 杀。<br>⭐ **远端多步逻辑一律落成 .sh 文件再执行**（#3 之后的既定纪律）—— 内联命令的坑不只是转义，还有"模式串自匹配"这类隐蔽陷阱。<br>⭐ **静默失败当红灯**：退出码 0 + 零输出本该立刻引起怀疑（一条有 6 个 `echo` 的命令不可能没有任何输出），这次是因为多等了一轮才去复查，白丢了几分钟服务空窗 |
### #20 「未提交、未入文档」的工作被遗留在工作区（后端骨架）⚠️ **过程可证明性的执行缺口**

| | |
|---|---|
| **现象** | 准备提交时 `git status` 显示 `backend/` 是一整块未跟踪目录。核查发现里面是**一套完整可运行的 FastAPI 骨架**：约 3100 行、31 个测试全绿、18 条路由（auth/tasks/batches/workflows/templates/models/health），完全符合 ADR-004 的技术栈选型。但它在 09-15 21:53 建好后**既没有提交，也没有写进任何文档** —— `todolist.md` 里 P2-08 仍是未打勾 |
| **分析** | 直接原因是**上一轮会话中断**（网络 502）恰好发生在建完骨架之后、提交之前。但更值得注意的是它暴露了一个系统性缺口：<br>① **提交动作没有和"做完"绑定** —— 上一批提交（334946a，21:24）是文档提交，骨架在其后 29 分钟建成，就再也没有触发过提交<br>② **文档更新依赖对话流程** —— 而会话中断会直接切断这条流程，于是"已完成"的事实没有任何落盘<br>③ 若此后长时间不开工，这块工作会**事实上消失**：既不在版本历史里，也不在进度文档里 |
| **决策** | 补交而不是丢弃（31 个测试全绿的代码优于悬空的工作区），同时**先验证再登记**：不因为"文件看起来完整"就标注完成，而是实际启动服务、拉 `/openapi.json`、跑测试与 lint，用证据说话 |
| **处置** | ① 修复 6 个 ruff 问题（其中 2 处非纯风格：`ContextVar` 用可变 dict 作默认值的共享隐患、`not x in y` 的否定作用域误读）② 实测启动：`/health` 正常返回、结构化 JSON 日志生效、18 条路由枚举完整 ③ 补文档：`todolist.md` P2-08 打勾并写明证据、P2-13 标注"部分完成（`alembic/versions/` 为空，尚无初始迁移）"、P2 由 3/13 校正为 5/13 ④ 单独提交并说明是补交 |
| **验证** | 31 tests passed · ruff All checks passed · `uvicorn` 启动成功且 `/health` 返回 ok · openapi 18 条路由 |
| **沉淀** | ① ⭐ **"做完"的定义必须包含"已提交 + 已登记"**，否则一次意外中断就能让整块工作变成不存在。这是本项目"过程可证明"目标的结构性漏洞。<br>② ⭐ **发现遗留工作时要先验证再登记**，不要凭目录完整度判断完成度 —— 本次核实过程同时查出了 ruff 问题、缺失的 mypy、以及空的 `alembic/versions/`，这些都是"看起来完成了"掩盖掉的真实缺口。<br>③ 附带发现：`pyproject.toml` 声明了 `mypy` 但 `.venv` 里**并未安装** —— 类型检查这条防线目前形同虚设，已登记进 `todolist.md` 待办 |

---

## 4. 待解决清单

| # | 问题 | 阻塞程度 | 需要谁 | 下一步 |
|---|---|---|---|---|
| — | **3 个自定义节点因升级失效**：`nunchaku`（torch ABI）· `TeaCache`（`precompute_freqs_cis` 被移除）· `smZNodes`（diffusers） | 中（能力缺口） | 我 | P2-05：① nunchaku 需按 torch 2.13 重装（不在 V1 关键路径，可先禁用）② TeaCache 查上游是否有新版 ③ smZNodes 非必需，直接禁用 |
| — | **P2-13 只完成一半**：11 张表模型已就位，但 `alembic/versions/` **为空，尚无初始迁移**；另 PRD §9 标注缺 ER 图 | 中 | 我 | 生成初始迁移 + 补 ER 图并回写 `PRD_v1.md` |
| — | **`mypy` 已声明但未安装**（`backend/.venv`），类型检查防线形同虚设 | 低 | 我 | 补装 `mypy` 并跑通 `app/` |
| — | **P1-08 用户访谈未做**（≥5 位电商从业者） | **中高** | 需本人约人 | 影响 MRD/画像/需求池的置信度；约不到则用二手资料补足并在文档标明 |
| — | P1-17 高保真原型未做 | 低 | 我 | 需选原型工具（Figma/即时设计），覆盖主流程即可 |
| — | GitHub 远端仓库未创建 | 低 | 需 `gh auth login` | 不影响本地开发 |
| — | GPU 实际单价 / 存储费未核实 | 低 | 我 | 查 autoDL 控制台，回填 `docs/prd/resource.md` §8 |
| — | 仓库根目录混入个人运维便签 `切换 AutoDL 网站的 无卡模式和 GPU.txt` | 低 | 需本人定 | 内容（模式切换步骤 + GPU 空闲评估）有归档价值，建议移入 `docs/sop/`；因其属个人笔记故未擅自提交 |
| — | **系统盘余量约 11G** | 中（运维） | 我 | torch 升级消耗约 4.6G；后续有大件安装前需先评估，必要时把环境迁到数据盘 |
| — | **autoDL SSH 密码曾在对话中出现** | 中（安全） | 需本人在控制台改 | 建议尽快修改密码 |

**已解决**：#7 Flux VAE 403（改用 FLUX.2 klein，自带 VAE）· #8 本机 HF API 不可达（改为在服务器查询）· #11 模型目录误判（find 反查定位）· #12 todolist 编辑失败（改为先读后写）· #13 fp4 兼容（并入 #14）· **#14 引擎版本不支持 klein（升级 v0.36.0 解决）** · #15 性能口径错误（已统一为稳态）· **#17 SDXL 1.58x 回归（根因 = 新版默认开启的 dynamic VRAM）** · **#18 生产配置定案（`--highvram`，地板 4.59s ≈ 基线 4.54s）** · **#19 pkill 自匹配自杀（改锚定正则 + 脚本化）** · **#20 遗留未提交的后端骨架（已补交 + 已验证）**

---

## 5. 决策变更记录

| 日期 | 原计划 | 实际决策 | 原因 |
|---|---|---|---|
| 2026-09-15 | 新建全新 ComfyUI 环境 | **复用现有 v0.3.75 + 31 节点** | v0.3.75 版本够新，且 P4 所需核心节点已齐全；重装节点依赖至少多花 1-2 天且有版本冲突风险 |
| 2026-09-15 | 模型放 ComfyUI 默认目录 | **迁到数据盘 + 软链** | 系统盘仅剩 13G，放不下；数据盘 50G |
| 2026-09-15 | 同时下载 SDXL + Flux | **先 SDXL，Flux 延后** | Flux 缺 VAE，官方源 403；SDXL 是 V1 前四周唯一需要的底模 |
| 2026-09-15 | 第二底模用 FLUX.1 schnell | **改用 FLUX.2 [klein] 4B（Apache-2.0）** | schnell 在 gated 仓库取不到；klein 4B 不仅可商用、未 gated、自带 VAE，还统一支持生成与多参考图编辑，更契合电商场景（见 #9） |
| 2026-09-15 | 内联 SSH 命令 | **脚本文件 scp 上传执行** | #3 转义事故；同时提升可复现性 |
| **2026-09-16** | 复用现有 ComfyUI v0.3.75 | **升级到 v0.36.0** | 前提「v0.3.75 版本够新」被证伪：它早于 klein 发布 2 个月，且 flux2 实现是 `return None # TODO`（见 #14） |
| **2026-09-16** | 「升到最新稳定版」= 只装 pip 依赖 | **还必须升级 torch 到 2.13.0+cu130** | v0.36.0 README 明确要求「最低 torch 2.7；20 系以上必须 cu130+」。`requirements.txt` 里裸 `torch` 掩盖了这个要求（见 #16 处置 7） |
| **2026-09-16** | 保留 flash-attn 以获加速 | **卸载 flash-attn** | 它针对 torch 2.5.1 编译，torch 2.13 下 ABI 失配，且拖垮 3 个**核心** comfy_extras 模块；它是可选加速，ComfyUI 会自动回退原生注意力（见 #16 处置 10） |
| **2026-09-16** | FLUX.2 klein 用 `CLIPTextEncodeFlux` | **改用 `CLIPTextEncode` + `CFGGuider` + `ConditioningZeroOut`** | 前者的 `t5xxl` 键在 klein 链路不存在；后者结构取自官方模板，且发现我们下的是**蒸馏版**（4 步、cfg=1）（见 #16 验证 3） |
| **2026-09-16** | GPU 策略：需 GPU 就切 | **「慢而便宜」放无卡，「需 GPU」攒到最后一次性切** | 切模式必须先关机，频繁切换既费时又可能没卡；本次把 fetch/预演/审计/校验全放无卡，GPU 机时只留给推理 |

| **2026-09-16** | 沿用 v0.36.0 的默认显存策略 | **启动脚本固定加 `--highvram`** | 新版把 dynamic VRAM 设为 Nvidia 上默认开启，直接导致 SDXL 回归 1.58x；单变量 A/B 证明它是唯一元凶（见 #17）。定案 `--highvram` 而非 `--disable-dynamic-vram`：稳态地板 4.59s vs 4.85s（见 #18）。**只锁包版本不足以复现环境，必须同时锁启动参数** |
| **2026-09-16** | 用 median 判读性能回归 | **用 min（地板）判读稳态能力** | 共享宿主机存在每约 3 样本一次的干扰尖峰，**升级前的基线里同样有** —— median 会把宿主机噪声误判成自身回归（见 #18） |

---

## 6. 下一步

### 6.1 引擎升级与回归收口 ✅ 已完成（2026-09-16）

1. ✅ 升级 **v0.3.75 → v0.36.0**，torch **2.5.1+cu124 → 2.13.0+cu130**，GPU 侧一次过验证通过
2. ✅ **FLUX.2 klein 蒸馏版出图成功** —— 本轮升级的唯一目的达成（median 1.83s / min 1.52s）
3. ✅ SDXL 回归定位为 dynamic VRAM 并修复，生产配置 `--highvram` 已固化进 `03_start_comfyui.sh`
4. ✅ `docs/prd/PRD_v1.md` NFR-1 已按**稳态口径**重写（含口径说明与冷启动值说明）

### 6.2 无 GPU / 无卡模式可做（优先推进这些）

| 任务 | 说明 |
|---|---|
| **P2-06 版本锁重做** | 🔵 进行中：`01_lock_versions.sh` 已增补 `[launch]`（启动参数 + 生效开关）与 `[models]`（sha256）两段，正在服务器上生成；完成后回传本地 `deploy/` |
| **P2-05 节点白名单** | 失败清单已明确（nunchaku/TeaCache/smZNodes），据此定白名单与禁用策略 |
| **P1-08 用户访谈** | 最高杠杆：能同时提升 MRD/画像/需求池三份文档的置信度 |
| P1-17 高保真原型 | 需先选定原型工具 |
| P2-08 后端骨架 FastAPI | 单进程，无卡模式可跑 |
| P2-11 CI 配置 / P2-12 配置管理 | 纯代码 |
| 回填 GPU 单价到资源预算 | 查控制台即可 |

### 6.3 需要你操作的

| # | 事项 | 紧急度 |
|---|---|---|
| 1 | **改 autoDL SSH 密码**（已在对话中出现过） | 建议尽快 |
| 2 | 若方便，核对 autoDL 的 GPU 单价与存储费单价 | 低 |
| 3 | 帮忙约 5 位电商从业者做访谈（或授权我用二手资料代替） | 中高 |
| 4 | ⚠️ **实例当前处于有卡（GPU）模式并在计费**。GPU 侧工作已收口，**可切回无卡模式省机时** —— 后续 P2-05/P2-06/P2-08 都不需要 GPU | 中 |
