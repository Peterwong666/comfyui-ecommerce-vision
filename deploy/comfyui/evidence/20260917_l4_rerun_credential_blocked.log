# ============================================================
# 2026-09-17 L4 三条工作流「重跑」尝试（第 2 次）—— 再次因无可用 SSH 凭据而中止
#
# ⚠️ 结论：**未开机、未执行任何出图、未上传任何文件**。
#    三条工作流 i2i_v1 / inpaint_v1 / upscale_v1 的
#    render_path_l4 / status / verification **不得**因本次运行发生任何改变。
#
# 本次与 2026-09-17 05:36 那次（20260917_l4_boot_cycle_blocked.log）的区别：
#    那次是「先开机 → 才发现没凭据」；
#    本次按新纪律「**先验证凭据 → 再决定是否开机**」执行，
#    凭据检查未通过 ⇒ **根本没有点开机按钮** ⇒ GPU 机时消耗为 0。
#
# 本文件以客观原文为主；凡属推断的内容，一律放在标注为「判读」的小节里。
# ⚠️ 全文不含任何凭据值（按 runbook 红线 1）。
# ============================================================

[0] 机时窗口
  开机动作          : **未执行**（凭据检查未通过，按指令第 0 步第 3 条不进入第 1 步）
  关机动作          : **未执行**（实例本就处于「已关机」，无需动作）
  实例状态复核时刻  : 2026-09-17 06:15 (CST/UTC+08:00)
  运行态机时        : **0 秒**
  计费              : **¥0**（实例全程「已关机」；autoDL 仅收数据盘存储费）
  ⚠️ 本次**未产生任何开关机费用**。与上次（白烧 2 分 47 秒）形成对照。

[1] 第 0 步：凭据检查 —— 逐位置结论（**只报存在性与长度，不含值**）

  ① 环境变量 SSHPASS
     $ if [ -n "${SSHPASS:-}" ]; then echo SET; else echo NOT SET; fi
     → SSHPASS: **NOT SET**

  ② /proc 全进程 environ 扫描（验证「是否只是没传给我的子 shell」）
     $ for e in /proc/[0-9]*/environ; do grep -qz 'SSHPASS=' "$e" && echo "$e"; done
     → **none found**（无任何进程持有 SSHPASS）
     方法有效性自证（避免"扫描本身是坏的"导致假阴性）:
       $ grep -qz 'PATH=' /proc/8668/environ   → yes      （pid 8668 = CodeBuddy node 主进程）
       $ grep -qz 'SSHPASS=' /proc/8668/environ → no
       → 该进程 environ 可读、PATH 能被检出、SSHPASS 检不出 ⇒ 扫描有效，结论可信。
     node 进程导出的变量名全集（节选，仅名字）:
       DISPLAY / HOME / LANG / PATH / PWD / SHELL / SSH_AUTH_SOCK / TERM / USER …
       —— 其中**没有任何** SSHPASS 或同类密钥变量。

  ③ 仓库内未追踪的凭据文件
     $ for f in .env .env.local .sshpass .sshpass.env .credentials .secrets; do ... done
     → absent: .env / .env.local / .sshpass / .sshpass.env / .credentials / .secrets
     $ git status --short
     → 只有 `?? SUMMARY.md`（夜间长任务的交付物，无凭据）
     $ find ~ -maxdepth 4 -type f \( -iname "*sshpass*" -o -name ".env" -o -iname "*credential*" -o -iname "*secret*" \)
     → 命中 4 个，**均属其它项目**，与本项目/实例无关：
         ./py/saas_ai_toy_platform/.env
         ./参考实现的运行时快照/lan/.env
         ./temperature-monitor/.env
         ./早期可运行 MVP/.env

  ④ ~/.ssh/config
     → **absent**（文件不存在）

  ⑤ ~/.ssh/ 私钥
     $ ls -la ~/.ssh/
     → 仅 `id_ed25519`（+ .pub / known_hosts / authorized_keys），无其它私钥
     → 该唯一公钥**已被实例拒绝**，有上次的原始证据（见下「判读」）

  ⑥ 其它可能被忽略的位置（本次额外排查）
     · /tmp 下按 sshpass|cred|.env|autodl 过滤 → 只命中上次的 autodl_*.html 页面快照，无凭据文件
     · 今天(2026-09-17)被修改的文件（depth<=3）→ 与本仓库相关的只有
       nightly-20260917.log / SUMMARY.md / 项目进展.md / todolist.md / 项目进度.md，
       **无凭据文件**
     · ~/.bashrc / ~/.profile → sshpass 提及数 = 0（无 profile 级导出）
     · ~/.bash_history → 'SSHPASS' 匹配数 = 0
     · ~/.zshrc / ~/.bash_profile → 不存在

  ⑦ ⚠️ 一个**形似凭据的诱饵**，已排除（记录下来避免下次重踩）
     位置: ~/.config/marktext/editorStates/c1b53d39-…_editor_buffer_store.json
       （marktext 编辑器的自动保存缓冲，内容为 `docs/sop/quickstart.md` 的**旧版本文本**）
     现象: 含 `export SSHPASS='……'` 且**非空**，初看像真凭据
     求证: 取出该值做**掩码**统计 → 长度 **6**
     比对: 同长度的占位串在本仓库出现于
             docs/sop/quickstart.md(6) / README.md(6) / docs/sop/runbook.md(6)
           —— 三者均为占位符 `<实例密码>`（恰为 6 个字符）
     结论: 该值是**占位符 `<实例密码>`**，**不是**凭据。已排除。

  ⑧ 本机具备的密码认证工具（能力是齐的，缺的只是密码）
     $ command -v sshpass; sshpass -V
     → /usr/bin/sshpass · sshpass 1.09   ✅ 已安装

[2] 第 1 步「开机 + 一次认证探测」—— **未执行**
  按指令第 0 步第 3 条：「如果找不到可用凭据 → 不要开机 … 这一步就结束，不进入第 1 步」。
  故：未点开机按钮、未等实例启动、未做认证探测。

[3] 实例当前状态（**只读**控制台复核，未做任何写操作）
  采集方式: browser-skill（bsk），只 navigate + observe + get-html，**未点击任何按钮**
  session : qbxw（已 stop，未留残留会话）
  控制台原始行文本（observe 语义树）:
    row "西北B区 / 329机 be8347bd6f-2101a80b comfyUI 已关机 GPU充足 RTX 4090 * 1卡
          系统盘 65.65% 数据盘 54.62% 正常 按量计费 14天23小时24分后释放"
  get-html 去标签后片段:
    |西北B区 |/ 329机|be8347bd6f-2101a80b|comfyUI|已关机|GPU充足|RTX 4090 * 1卡|查看详情|
  同页 GPU 资源指标（规则 1 所需，**仅记录、未据此开机**）:
    |西北B区 |/ 329机|…|GPU空闲/总量：|3/10|GPU驱动：|580.76.05|CUDA版本：|≤ 13.0|
    （旁证：605机 |GPU空闲/总量：|2/12|，非本项目主机）
  → 结论：**329机 / comfyUI = 「已关机」**，本次全程保持关机。GPU空闲 3/10（当时若开机是有卡的，但凭据不具备 ⇒ 按纪律不开机）。

[4] 零成本探测：凭据缺失 + 实例关机状态的可复现证据（未开机即可取证）
  probe A —— 公钥直连（BatchMode，不进入密码阶段）
    $ ssh -o BatchMode=yes -o PreferredAuthentications=publickey -o ConnectTimeout=20 \
        -p 22910 root@connect.westc.seetacloud.com 'echo SSH_OK; hostname'
    → Connection closed by 198.18.0.36 port 22910
    判读: 连接在 **SSH 握手前即被关闭**（连 `Permission denied` 都没到）——
          与「实例运行中」时的表现（上次记录：`Permission denied (publickey,password)`）
          **明显不同**，是本实例**当前处于关机态**的旁证。
          （注意：这与 runbook §4.4 的"网关在关机时仍完成 TCP 握手"并不矛盾 —— 
            TCP 层通，但其后没有 SSH 服务应答。）

  probe B —— sshpass 在 SSHPASS 未设置时的**精确报错原文**（供下次快速判别）
    $ env -u SSHPASS sshpass -e ssh … 'echo SSH_OK'
    → SSHPASS: -e option given but SSHPASS environment variable not set
    ⚠️ 这条消息与上次记录里那句 `At most one of -f, -d, -p or -e should be used`
       **不是同一条** —— 上次那条对应的是「多个口令来源」，本次是「环境变量未设置」。
       两者可借此区分「没给密码」与「给了多个来源」，以后排查可据此定位。

  probe C —— TCP 可达性与电源状态解耦（用实测坐实这条纪律）
    $ cat < /dev/null > /dev/tcp/connect.westc.seetacloud.com/22910
    → TCP connect: OK
    判读: **实例已关机，TCP 仍可连** —— 直接坐实 feedback_remote_ssh_credentials 里
          「SSH 端口 TCP 可连 ≠ 实例开机」这条纪律。**不能拿 port check 判断开关机。**

[5] 未执行的部分（明确留痕）
  未上传:  engine/ → $RD/l4_repo/ ；deploy/autodl/36_l4_three_workflows.sh ；
           deploy/autodl/37_probe_inpaint_mask_polarity.py ；deploy/autodl/03_start_comfyui.sh ；
           参考图 → $RD/l4_assets/ref_product.png
  未执行:  bash $RD/36_l4_three_workflows.sh
  未产出:  三条工作流的渲染路径 L4 结论；蒙版极性客观信号与源码摘录；基准复测数字
  未改动:  workflows/registry.yaml（未改，也不需要改 —— 本次无结论可回填）
  未 git commit / 未 git add 任何文件（工作区唯一新增见 [7]）

[6] 为什么本次仍然卡住：结构性原因，不是"再试试就好了"
  · autoDL 控制台的实例密码是**掩码**显示，且 browser-skill **明确禁止**从页面提取凭据
    （见该技能原文：Never extract credentials, cookies, tokens, or other secrets from pages）。
    ⇒ 「让 agent 自己去控制台读出密码」这条路是**堵死的**。
  · 本机唯一私钥 `id_ed25519` 已被实例拒绝（上次原始证据：
      debug1: Authentications that can continue: publickey,password
      root@connect.westc.seetacloud.com: Permission denied (publickey,password).）
    ⇒ 现状是服务器**只认密码**，而密码不在本机任何可达位置。
  · 因此本次**没有任何"值得修就修"的空间**：连不进机器，改脚本/改路径都传不上去。
    按指令「同一件事连续失败 3 次就停手」的精神，本次**第 1 次检查即判定不可能**，未反复试探。

[7] 本次仓库新增/改动文件
  新增: deploy/comfyui/evidence/20260917_l4_rerun_credential_blocked.log  (本文件)
  改动: 无
  未追踪且非本次产物: SUMMARY.md（2026-09-17 夜间长任务交付物，此前已存在）

[8] 解除阻塞所需的**任一**条件（给用户的操作指引）
  方案 A（推荐 · 最省事）：在**你自己的终端**里执行
      export SSHPASS='<实例密码>'
    并**确保后续交付任务的那个 CodeBuddy 进程是从这个 shell 启动/继承该环境的**
    （关键：光在别的窗口 export 没用 —— 本次已用 /proc 全进程 environ 扫描证明
      运行中的 CodeBuddy 进程环境里**没有** SSHPASS）。
    然后重跑本次任务即可。

  方案 B：由用户代跑 `deploy/autodl/36_l4_three_workflows.sh`（连同其依赖一起上传），
    再把 `$RD/l4_evidence/` 与 `$RD/l4_out/` 取回，落到
    `deploy/comfyui/evidence/` 与 `render_l4_out/`。

  ⚠️ 无论哪种方案，**凭据检查都必须排在点开机之前**（本次已如此执行）。
  ⚠️ 不要把密码写进仓库任何文件（runbook 红线 1）。
