# ============================================================
# 2026-09-17 L4 三条工作流实跑尝试（第 3 次）—— 再次因「凭据文件不存在」而中止
#
# ⚠️ 结论：**未开机、未执行任何出图、未上传任何文件、未回传任何产物**。
#    三条工作流 i2i_v1 / inpaint_v1 / upscale_v1 的
#    render_path_l4 / status / verification **不得**因本次运行发生任何改变。
#
# 本次与 05:36 那次（20260917_l4_boot_cycle_blocked.log）的区别：
#    那次是「先开机 → 才发现没凭据」，白烧 2 分 47 秒；
#    本次按纪律「**先验证凭据 → 再决定是否开机**」执行，
#    凭据检查未通过 ⇒ **根本没有进入第 1 步，更没有点开机按钮**
#    ⇒ GPU 机时消耗为 0。
#
# 本次与 06:15 那次（20260917_l4_rerun_credential_blocked.log）的区别：
#    那次凭据检查手段齐备（Bash 可用，跑了 env / /proc environ 扫描 / find 等）；
#    本次 **Bash 工具在本会话中持续不可用**（安全分类器故障），
#    故改用**只读工具（Glob / Grep）**完成判定。手段不同，结论一致：**无凭据**。
#
# ⚠️ 全文不含任何凭据值（按 runbook 红线 1）。
# ============================================================

[0] 机时窗口
  开机动作          : **未执行**（凭据文件不存在 ⇒ 按指令第 0 步判据「不要开机，直接结束」）
  关机动作          : **未执行**（本次从未开机，无运行态可关）
  运行态机时        : **0 秒**（本次会话未发出任何开机指令）
  计费              : **¥0**（本次未产生任何开关机费用）
  ⚠️ 本次**未产生任何开关机费用**。与 05:36 那次（白烧 2 分 47 秒）形成对照。

[1] 第 0 步：凭据检查 —— 逐位置结论（**只报存在性与长度，不含值**）

  ⚠️ 方法学说明（诚实留痕，避免"看起来验过"）：
     本会话中 **Bash 工具持续返回**
       "The auto mode safety classifier is temporarily unavailable ..."
     共尝试 4 次（含 2 次极简只读命令），**全部被拒**。
     ⇒ 因此指令里给的
         `ls -l ~/.autodl_sshpass` / `stat -c '%a %s %U'` /
         `python3 -c "...len..."` / `printenv SSHPASS | wc -c`
       **这四条命令本次一条都没能执行**，本文**不谎称**执行过。
     改用**只读工具 Glob / Grep** 取证；它们的原始输出逐条列在下面，
     判据不变（「文件是否存在」这一事实与工具无关）。

  ① ~/.autodl_sshpass  —— **不存在**（本次阻塞的直接原因）
     证据 1（Grep，stat 级）:
       $ Grep(pattern=".", path="/home/peter/.autodl_sshpass", mode=count)
       → Error: Search path is not accessible: /home/peter/.autodl_sshpass.
         ENOENT: no such file or directory, stat '/home/peter/.autodl_sshpass'
       ✅ 这是 **ENOENT（stat 失败）**，不是"文件存在但空" —— 判据明确命中「不存在」。
     证据 2（Glob，精确名）:
       $ Glob(path="/home/peter", pattern=".autodl_sshpass")
       → No files found
     证据 3（Glob，模糊名）:
       $ Glob(path="/home/peter", pattern=".*sshpass*")  → No files found
       $ Glob(path="/home/peter", pattern=".autodl*")    → No files found
       $ Glob(path="/home/peter", pattern="*sshpass*")   → No files found
     方法有效性自证（避免"Glob 根本匹配不到隐藏文件"造成假阴性）:
       $ Glob(path="/home/peter", pattern=".bashrc")  → ["/home/peter/.bashrc"]   ✅
       $ Glob(path="/home/peter", pattern=".profile") → ["/home/peter/.profile"]  ✅
       ⇒ Glob **能**匹配点开头文件 ⇒ 上面那些 "No files found" 是真阴性。
     ⇒ 长度：**不适用（文件不存在，无长度可取）**；权限/属主：**不适用**。

  ② 环境变量 SSHPASS
     → **未能测得**（`printenv` 需 Bash，本会话不可用）。
     旁证：仓库内两处历史记录（06:15 那次）已实测 **NOT SET**，
       且用户本次明确指示「**不要**指望 SSHPASS 环境变量，以文件为准」。
       ⇒ 即便环境变量存在，**按判据也不改变结论**（文件不存在 = 硬停）。
     ⚠️ 未读取 /proc/*/environ —— 那会把可能存在的口令值带进上下文，
       与「不打印凭据」冲突；且判据不依赖它，故**刻意不做**。

  ③ ~/.ssh/config
     → **不存在**。证据（Glob，整个 .ssh 目录全量列举）:
       $ Glob(path="/home/peter", pattern=".ssh/*")
       → ["/home/peter/.ssh/authorized_keys",
          "/home/peter/.ssh/id_ed25519",
          "/home/peter/.ssh/id_ed25519.pub",
          "/home/peter/.ssh/known_hosts.old",
          "/home/peter/.ssh/known_hosts"]
       列表中**没有 config**。
     ~/.ssh 私钥：仅 `id_ed25519` —— 该公钥**已被实例拒绝**
       （05:36 那次原始证据：`Permission denied (publickey,password)`）。

  ④ 仓库根未追踪的 .env / .sshpass
     → **不存在**。证据:
       $ Glob(path="/home/peter/py/comfyUI_project",
              pattern="{.env,.env.local,.sshpass,.credentials,.secrets}")
       → No files found
       $ Glob(path="/home/peter/py/comfyUI_project", pattern=".*")
       → 仅命中 .gitignore 与 .env.example（**模板，非凭据**），
         以及各目录的 .gitkeep / web 下的 .env.example / .prettierrc.json 等。

  ⑤ 其它可能位置（本次额外排查，均为只读）
     · /home/peter 下 `{ .sshpass.env, .credentials, .secrets, .env,
                            .autodl_pass, .autodl_password }` → **全部不存在**
     · shell profile 级导出:
       $ Grep(pattern="SSHPASS|sshpass", path="/home/peter/.bashrc",  mode=count) → No matches
       $ Grep(pattern="SSHPASS|sshpass", path="/home/peter/.profile", mode=count) → No matches
     · 本机其它项目的 .env（**只统计、不读值**，避免误当本实例凭据）:
       $ Grep(pattern="SSHPASS|autodl", path="/home/peter/temperature-monitor/.env") → No matches
       $ Grep(pattern="SSHPASS|autodl", path="/home/peter/ai-toy-platform/.env")   → No matches
       $ Grep(pattern="SSHPASS|autodl", path="/home/peter/aitoy-deploy/lan/.env")  → No matches
       ⇒ 这三个文件与本项目/实例**无关**，已排除。
     · /tmp 下 `*sshpass*` → Glob 因 /tmp 内多个 systemd-private-* 目录
       **Permission denied** 而报错返回，**未能得出有效结论**（如实标注为"未验"）。
       但 ①③④⑤ 已足以判定，且历史两次检查 /tmp 亦无凭据。

[2] 第 1 步「查有无空闲卡」—— **未执行**
  按指令判据：「如果 ~/.autodl_sshpass 不存在或长度为 0 → 不要开机，直接结束」。
  结论既已判定为"不存在" ⇒ **不再进入第 1 步**，未调用 autodl-mode-switch 技能，
  **未读取 GPU 空闲/总量**。
  ⚠️ 附带事实：即便想读也读不到 —— 该技能依赖 Bash/bsk，而 Bash 本会话不可用。

[3] 第 2 / 3 / 4 / 5 步 —— **全部未执行**
  未上传:  engine/ → $RD/l4_repo/ ；deploy/autodl/36_l4_three_workflows.sh ；
           deploy/autodl/37_probe_inpaint_mask_polarity.py ；deploy/autodl/03_start_comfyui.sh ；
           参考图 → $RD/l4_assets/ref_product.png
  未执行:  bash $RD/36_l4_three_workflows.sh
  未做认证探测（因为从未开机）
  未产出:  三条工作流的渲染路径 L4 结论；蒙版极性客观信号与源码摘录；基准复测数字
  未改动:  workflows/registry.yaml（未改，也不需要改 —— 本次无结论可回填）
  未回传:  deploy/comfyui/evidence/ 与 render_l4_out/ **无本次新增产物**
           （除本文件本身）

[4] 第 6 步「关机」—— 不适用
  本次从未开机，**不存在运行态**，故无"关机"动作。
  ⚠️ 但**必须诚实标注一点**：本会话 **Bash 不可用 ⇒ 无法调用 autodl-mode-switch 技能
     去控制台复核实例当前电源状态**。因此本文**不声称**"已确认实例为已关机"。
     可依据的最近一次控制台事实：2026-09-17 06:15
       row "西北B区 / 329机 be8347bd6f-2101a80b comfyUI 已关机 GPU充足 RTX 4090 * 1卡
            系统盘 65.65% 数据盘 54.62% 正常 按量计费 14天23小时24分后释放"
     且本次**未发出任何开机指令** ⇒ 实例不可能因本次而变为运行中。

[5] 第 7 步「凭据文件收尾」—— 无对象可处置
  本次**提前中止**（未走到第 3 步）。按指令应**保留**凭据文件以便重试 ——
  但事实上 **~/.autodl_sshpass 从来就不存在**，故：
    · 无文件可保留
    · 无文件可 `shred -u` / `rm -f`
    · 未删除任何东西（也不存在"已删"可报告）
  ⇒ 待用户补齐凭据后重试。

[6] 为什么这不是"一次可修的配置问题"
  ComfyUI 未起 / 模型路径不对 / 远端缺文件 / 脚本 bug —— 这些本可修（改脚本→重传→重跑）。
  但本次卡在**更前置的一层**：连凭据都没有，根本进不了机器，
  任何远端动作（含"重传脚本"）都无从执行，
  故不满足「值得修就修」的前提。

[7] 本次仓库新增/改动文件
  新增: deploy/comfyui/evidence/20260917_l4_third_attempt_credential_blocked.log（本文件）
  改动: 无
  ⚠️ 未执行 git add / git commit（按指令：提交由用户统一处理）。

[8] 解除阻塞所需条件（给用户的操作指引）
  ✅ 唯一必要动作：在**本机**创建凭据文件（推荐，因为本次任务已改为「以文件为准」）
      printf '%s' '<实例密码>' > ~/.autodl_sshpass
      chmod 600 ~/.autodl_sshpass
      # 校验（应输出 600 与 >0 的字节数，**不要** cat 它）:
      stat -c '%a %s %U' ~/.autodl_sshpass
  备选（若坚持用环境变量）：在**将要启动 CodeBuddy 的那个 shell** 里
      export SSHPASS='<实例密码>'
      # ⚠️ 关键：必须在**启动 CodeBuddy 之前**导出/继承；
      #    光在别的窗口 export 对已运行的进程无效（06:15 已用 /proc environ 坐实）。
  ⚠️ 两条路都**不要把密码写进仓库任何文件**（runbook 红线 1）。
  ⚠️ 凭据检查必须**排在点开机之前**（本次已如此执行；05:36 那次反例代价 = 2 分 47 秒机时）。

[9] 与「过程可证明」相关的自我约束（本次实际执行到的）
  · 不谎称跑过没跑的命令（[1] 方法学说明）
  · 不把"没跑通"写成"跑通"
  · 不在无凭据情况下点开机（本次执行到位，机时 0）
  · 不读取/回显/落盘任何凭据值
  · 不改 registry.yaml、不 git commit
