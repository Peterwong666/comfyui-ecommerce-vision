# ComfyUI 源码摘录（只读，单变量：不改任何文件）
# 采集时间: 2026-09-17T15:48:14+08:00
# ComfyUI HEAD: ee71d5c4993f29086b27fde1629a945ae48425bf
# 目的：为『蒙版极性』的**机制假设**留下可复核依据。
#  ⚠️ 这是源码推断，**不等于**出图实测 —— 语义结论仍需人工看图。

=== class LoadImage:  （/root/ComfyUI/nodes.py） ===
1738:class LoadImage:
1739-    @classmethod
1740-    def INPUT_TYPES(s):
1741-        input_dir = folder_paths.get_input_directory()
1742-        files = [f for f in os.listdir(input_dir) if os.path.isfile(os.path.join(input_dir, f))]
1743-        files = folder_paths.filter_files_content_types(files, ["image"])
1744-        return {"required":
1745-                    {"image": (sorted(files), {"image_upload": True})},
1746-                }
1747-
1748-    CATEGORY = "image"
1749-    ESSENTIALS_CATEGORY = "Basics"
1750-    SEARCH_ALIASES = ["load image", "open image", "import image", "image input", "upload image", "read image", "image loader"]
1751-
1752-    RETURN_TYPES = ("IMAGE", "MASK")
1753-    FUNCTION = "load_image"
1754-
1755-    def load_image(self, image):
1756-        image_path = folder_paths.get_annotated_filepath(image)
1757-
1758-        dtype = comfy.model_management.intermediate_dtype()
1759-        device = comfy.model_management.intermediate_device()
1760-
1761-        components = InputImpl.VideoFromFile(image_path).get_components()
1762-        if components.images.shape[0] > 0:
1763-            return (components.images.to(device=device, dtype=dtype), (1.0 - components.alpha[..., -1]).to(device=device, dtype=dtype) if components.alpha is not None else torch.zeros((components.images.shape[0], 64, 64), dtype=dtype, device=device))
1764-
1765-        # This code is left here to handle animated webp which pyav does not support loading
1766-        img = node_helpers.pillow(Image.open, image_path)
1767-
1768-        output_images = []
1769-        output_masks = []
1770-        w, h = None, None
1771-
1772-        for i in ImageSequence.Iterator(img):
1773-            i = node_helpers.pillow(ImageOps.exif_transpose, i)
1774-
1775-            image = i.convert("RGB")
1776-
1777-            if len(output_images) == 0:
1778-                w = image.size[0]
1779-                h = image.size[1]
1780-

=== class ImageCompositeMasked:  （/root/ComfyUI/nodes.py） ===
（未匹配到 class ImageCompositeMasked: —— 该定义可能已移动，本身就是一条事实）

=== class SetLatentNoiseMask:  （/root/ComfyUI/comfy_extras/nodes_mask.py） ===
（未匹配到 class SetLatentNoiseMask: —— 该定义可能已移动，本身就是一条事实）

=== class GrowMask:  （/root/ComfyUI/nodes.py） ===
（未匹配到 class GrowMask: —— 该定义可能已移动，本身就是一条事实）

=== LoadImage 里 mask 由 alpha 导出的那几行（关键）===
1785-            image = torch.from_numpy(image)[None,]
1786-            if 'A' in i.getbands():
1787-                mask = np.array(i.getchannel('A')).astype(np.float32) / 255.0
1788:                mask = 1. - torch.from_numpy(mask)
1789-            else:
1790-                mask = torch.zeros((64, 64), dtype=torch.float32, device="cpu")
1791-            output_images.append(image.to(dtype=dtype))
